# Implementation Plan

> amendment: 買い物リスト（`ShoppingListService`）・外食時の代替提案（`EatingOutTipService`）・ダイエットインサイト（`DietInsightsService`）を算出する本spec専用のバックエンド（`server/src/dashboard/`、対応するGateway群、`shared/src/dashboard.schema.ts`）は全て廃止した。対応する算出ロジックは`nutrition-engine`（`GET /api/nutrition/diet-insights`）と`menu-generation`（`GET .../shopping-list`, `GET .../eating-out-suggestion`）の承認済みamendmentとして実装されることになったため、本specはこれら3つのAPIをフロントエンドから直接呼び出し、レスポンスをそのまま表示するのみとなった。以下のタスク一覧はこの方針に基づき全面的に再構成している。

- [ ] 1. Foundation: フロントエンド共通データ取得フック
- [x] 1.1 (P) フロントエンド共通データ取得フックの作成
  - `web/src/hooks/useAsyncData.ts` を作成し、非同期取得関数を受け取りローディング状態・データ・エラーを統一的に返す共通フックを実装する
  - 取得失敗時にエラー状態のみが更新され、直前に取得済みのデータが破棄されないことをテストで確認する
  - _Requirements: 16.4, 16.5_
  - _Boundary: useAsyncData_

- [ ] 2. Core: フロントエンドAPIクライアント
- [x] 2.1 (P) nutritionClientの作成
  - `web/src/api/nutritionClient.ts` に `getSummary(date)` と `getDietInsights(date)` を実装し、`GET /api/nutrition/summary` / `GET /api/nutrition/diet-insights` をそれぞれ型付きで呼び出す
  - `getSummary`の409応答（`CalculationUnavailableError`）、`getDietInsights`の409応答（`profile_missing` / `diet_mode_disabled` / `incomplete_diet_mode_data`の各`reason`）が、例外ではなく判別可能な戻り値として呼び出し元に伝わることをテストで確認する
  - _Requirements: 1.1, 2.4, 10.3, 10.5, 13.1, 13.3, 14.1, 14.2, 15.1, 15.2, 16.2_
  - _Boundary: nutritionClient_
- [x] 2.2 (P) menuPlanClientの作成
  - `web/src/api/menuPlanClient.ts` に `getWeekPlan(weekStartDate)` / `regenerateWeek(weekStartDate)` / `regenerateDay(weekStartDate, dayIndex)` / `getShoppingList(weekStartDate)` を実装し、対応する`menu-generation`のエンドポイントを型付きで呼び出す
  - 各メソッドが成功時に型付きレスポンスを、失敗時に判別可能なエラーを返すこと、`getShoppingList`が対象週の献立未生成時の200 + `null`を判別可能な戻り値として返すことをテストで確認する
  - _Requirements: 4.3, 5.3, 5.4, 5.6, 8.1, 8.5_
  - _Boundary: menuPlanClient_
- [x] 2.3 (P) mealSlotClientの作成
  - `web/src/api/mealSlotClient.ts` に `generateRecipeDetail(week, day, meal)` / `submitFeedback(week, day, meal, liked)` / `getEatingOutSuggestion(week, day, meal)` を実装し、対応する`menu-generation`のエンドポイントを型付きで呼び出す
  - `generateRecipeDetail`/`submitFeedback`の404/502応答、`getEatingOutSuggestion`の404（対象食事枠なし）と200 + `suggestion: null`（代替案なし）が、それぞれ判別可能な戻り値として伝わることをテストで確認する
  - _Requirements: 6.1, 6.6, 7.2, 7.4, 9.1, 9.4_
  - _Boundary: mealSlotClient_

- [ ] 3. Core: 表示モード・プロフィール概要・推奨栄養量セクション
- [x] 3.1 (P) ModeToggleの実装
  - `web/src/components/dashboard/ModeToggle.tsx` に「栄養評価」/「ダイエット状況」の表示モード切替コントロールを実装する。ダイエットモードが無効なプロフィールの場合はダイエット状況画面への切替を案内表示に置き換える
  - モードを切り替えると対応するセクション群のみが表示されることを確認する
  - _Requirements: 2.1, 2.2, 2.3, 2.6_
  - _Boundary: ModeToggle_
- [x] 3.2 (P) ProfileStripの実装
  - `web/src/components/dashboard/ProfileStrip.tsx` に、既存の`profileClient.getProfile()`の結果と`nutritionClient.getSummary()`の`activityLevelLabel`から、身長・体重・年齢・性別・活動レベル表示ラベルのchipを実装する
  - ダイエットモード有効時のみ目標体重・目標達成期間のchipが追加表示されることを確認する
  - _Requirements: 2.4, 2.5, 2.6_
  - _Depends: 2.1_
  - _Boundary: ProfileStrip_
- [x] 3.3 (P) RestrictionChipの実装
  - `web/src/components/dashboard/RestrictionChip.tsx` に、既存の`profileClient.getProfile()`の結果から食事制限タイプ・強度・自由記述のchipを実装する
  - 制限タイプが「制限なし」の場合に制限なしである旨の表示になることを確認する
  - _Requirements: 3.1, 3.2, 3.3_
  - _Boundary: RestrictionChip_
- [x] 3.4 (P) NutritionSummarySection・PfcBarChart・NutrientSufficiencyListの実装
  - `web/src/components/dashboard/NutritionSummarySection.tsx` と `web/src/components/dashboard/charts/PfcBarChart.tsx` / `NutrientSufficiencyList.tsx` に、推奨エネルギー量カード・PFC構成比・微量栄養素充足率一覧を実装する
  - `NutrientSufficiencyList`は`menuPlanClient.getWeekPlan`が返す当日の`DayMenu.dayNutrition`（実績値）と`nutritionClient.getSummary`が返す目標値の比（実績÷目標）を算出して充足率として表示する（実績値・目標値そのものの計算は行わない）
  - 充足率が100%を超える栄養素で実際の値（100%超）がそのまま表示されることを確認する
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_
  - _Depends: 2.1_
  - _Boundary: NutritionSummarySection_
- [x] 3.5 (P) PeriodToggleの実装（今日/今週の計画平均）
  - `web/src/components/dashboard/PeriodToggle.tsx` に「今日」/「今週の計画平均」の切替コントロールを実装し、`NutritionSummarySection`に組み込む。「今週の計画平均」選択時は`menuPlanClient.getWeekPlan`が返す7日分の`DayMenu.dayNutrition`を単純平均し、平均実績÷目標で平均充足率を算出する
  - 今週の週間献立プランが未生成の場合に「今週の計画平均」の選択が利用不可になるか、未生成である旨が表示されることを確認する
  - _Requirements: 18.1, 18.2, 18.3, 18.4_
  - _Depends: 3.4_
  - _Boundary: PeriodToggle_
- [x] 3.6 (P) AdviceCalloutの実装（栄養評価・ダイエット状況共通）
  - `web/src/components/dashboard/AdviceCallout.tsx` に、栄養評価画面用（微量栄養素充足率のうち最小値の項目を定型文に当てはめる）とダイエット状況画面用（曜日別カロリー収支のうち超過が最大の曜日を定型文に当てはめる）の2種類の一言アドバイスを実装する
  - 対象データが存在しない場合（週間献立未生成、カロリー収支データなし等）にアドバイスが表示されないことを確認する
  - _Requirements: 19.1, 19.2, 19.3, 19.4_
  - _Depends: 3.4_
  - _Boundary: AdviceCallout_

- [ ] 4. Core: 1週間のおすすめ献立とレシピ詳細
- [x] 4.1 WeeklyMenuSection・DayColumnの実装
  - `web/src/components/dashboard/WeeklyMenuSection.tsx` と `DayColumn.tsx` に、週全体差し替えボタン（曜日カード列の上部・右寄せ）と各曜日カードの日単位差し替えボタン（曜日名・カロリー行の下）を含む献立セクションを実装する。本セクションは栄養評価画面にのみ配置し、ダイエット状況画面には配置しない
  - 差し替え処理中は対象範囲（週全体または該当曜日）が処理中表示になり再クリックが防止されること、成功時に対象範囲のみ更新後の内容に置き換わること、失敗時に直前の表示が保持されることを確認する
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5, 5.1, 5.2, 5.3, 5.4, 5.5, 5.6, 17.4_
  - _Depends: 2.2_
  - _Boundary: WeeklyMenuSection_
- [x] 4.2 MealCell・RecipeDetailModalの実装
  - `web/src/components/dashboard/MealCell.tsx` と `RecipeDetailModal.tsx` に、食事セルクリックでのモーダル表示、レシピ詳細（材料・手順・カロリー・調理時間・人前）と1〜2件の追加副菜提案の表示、生成中/失敗状態の表示、閉じる操作を実装する
  - 生成成功時に材料一覧・調理手順・追加副菜提案が表示されること、失敗時にモーダル内にエラーが表示されること、閉じる操作で週間献立表示に戻ることを確認する
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 6.6_
  - _Depends: 2.3, 4.1_
  - _Boundary: RecipeDetailModal_
- [x] 4.3 FeedbackControlの実装
  - `web/src/components/dashboard/FeedbackControl.tsx` に、`RecipeDetailModal`内から表示中の料理への好き/苦手の入力・送信・送信済み状態・失敗時の状態保持を実装する
  - フィードバック送信成功時に入力済みであることが分かる表示に切り替わり、失敗時に入力前の状態が保持されることを確認する
  - _Requirements: 7.1, 7.2, 7.3, 7.4_
  - _Depends: 2.3, 4.2_
  - _Boundary: FeedbackControl_

- [ ] 5. Core: 通常モード専用セクション
- [x] 5.1 (P) ShoppingListSectionの実装
  - `web/src/components/dashboard/ShoppingListSection.tsx` に、`menuPlanClient.getShoppingList`の結果（`menu-generation`が集約・カテゴリ分類済みの`ShoppingList`）をそのままカテゴリ別に表示し、`null`（対象週の献立未生成）の場合の代替表示を実装する。各品目は`item.displayQuantity`＋`item.displayUnit`を表示し（例:「白菜 0.5玉」）、独自の単位変換は行わない
  - カテゴリごとにグループ化された品目名・`displayQuantity`+`displayUnit`が`menu-generation`から受け取った通りに表示されること、献立未生成時に買い物リストの代わりに未生成である旨が表示されることを確認する
  - _Requirements: 8.1, 8.2, 8.3, 8.4, 8.5_
  - _Depends: 2.2_
  - _Boundary: ShoppingListSection_
- [ ] 5.2 (P) EatingOutTipSectionの実装
  - `web/src/components/dashboard/EatingOutTipSection.tsx` に、`mealSlotClient.getEatingOutSuggestion`の結果（想定メニューと代替案のカロリー・たんぱく質量の差）をそのまま表示し、404（対象食事枠なし）または`suggestion: null`（代替案なし）の場合はセクション自体を非表示にする実装を行う
  - 対象データがある場合に想定メニューと代替案の比較が表示され、対象食事枠がない場合・代替案がない場合のいずれもセクションが表示されないことを確認する
  - _Requirements: 9.1, 9.2, 9.3, 9.4_
  - _Depends: 2.3_
  - _Boundary: EatingOutTipSection_

- [ ] 6. Core: ダイエットモード専用セクション
- [ ] 6.1 (P) DietGoalStatusSection・GoalProgressBar・GuardrailWarningCalloutの実装
  - `web/src/components/dashboard/DietGoalStatusSection.tsx` / `charts/GoalProgressBar.tsx` / `GuardrailWarningCallout.tsx` に、目標エネルギー量・現在体重/目標体重/残り体重差・進捗バー・安全ペース判定・ガードレール警告または安全表示、および`nutritionClient.getDietInsights`の`goalEta`（見込み期間）を実装する
  - 目標エネルギー量カードに`PeriodToggle`（3.5で実装済み、栄養評価画面と選択状態を共有）を組み込み、「今週の平均」選択時は対象週7日分の日付で`nutritionClient.getSummary`を呼び出し`dietMode.calorieTarget`を単純平均して表示する
  - `nutritionClient.getSummary`の`dietMode.guardrails.warnings`が空の場合に安全表示が、1件以上ある場合に警告と修正提案が表示されること、`goalEta.available`が`false`の場合に見込み期間の代わりに算出できない旨が表示されること、「今週の平均」選択時に7日分の`calorieTarget`平均が正しく表示されることを確認する
  - _Requirements: 10.1, 10.2, 10.3, 10.4, 10.5, 11.1, 11.2, 11.3, 18.5, 18.6_
  - _Depends: 2.1, 3.5_
  - _Boundary: DietGoalStatusSection_
- [ ] 6.2 (P) CalorieBalanceSection・CalorieBalanceChart・入力フォームの実装
  - `web/src/components/dashboard/CalorieBalanceSection.tsx` / `charts/CalorieBalanceChart.tsx` / `ManualCalorieOverrideForm.tsx` / `ExerciseEntryForm.tsx` に、直近1週間の曜日別カロリー差分の発散棒グラフ、既存の`dailyLogClient`を用いた摂取カロリー手動修正フォーム、追加運動記録入力フォームを実装する。`AdviceCallout`（3.6）をダイエット状況画面向けに組み込み、超過が最大の曜日を要約表示する
  - 入力送信が成功した場合にグラフの該当曜日が更新されること、失敗した場合に入力前の表示状態が保持されることを確認する
  - _Requirements: 12.1, 12.2, 12.3, 12.4, 12.5, 12.6, 12.7, 19.2, 19.3, 19.4_
  - _Depends: 3.6_
  - _Boundary: CalorieBalanceSection_
- [ ] 6.3 (P) WeightTrendSection・WeightTrendChartの実装
  - `web/src/components/dashboard/WeightTrendSection.tsx` / `charts/WeightTrendChart.tsx` に、`nutritionClient.getDietInsights`の`weightHistory`/`weightProjection`/目標体重基準線を折れ線グラフで表示する
  - `weightProjection`が空配列の場合に見込み線を表示せず実測推移のみを表示することを確認する
  - _Requirements: 13.1, 13.2, 13.3, 13.4_
  - _Depends: 2.1_
  - _Boundary: WeightTrendSection_
- [ ] 6.4 (P) PlateauAdviceCalloutの実装
  - `web/src/components/dashboard/PlateauAdviceCallout.tsx` に、`nutritionClient.getDietInsights`の`plateau.status`が`"plateaued"`の場合のみ`plateau.message`を表示する実装を行う
  - `"on_track"` / `"insufficient_data"` / `"not_applicable"`のいずれの場合もアドバイスが表示されないことを確認する
  - _Requirements: 14.1, 14.2, 14.3_
  - _Depends: 2.1_
  - _Boundary: PlateauAdviceCallout_
- [ ] 6.5 (P) ExerciseSimulationSection・CompareBarsの実装
  - `web/src/components/dashboard/ExerciseSimulationSection.tsx` / `charts/CompareBars.tsx` に、`nutritionClient.getDietInsights`の`exerciseSimulation`から食事管理のみ/運動併用の見込み週数を比較表示する
  - `exerciseSimulation.available`が`false`の場合にセクションが表示されないことを確認する
  - _Requirements: 15.1, 15.2, 15.3_
  - _Depends: 2.1_
  - _Boundary: ExerciseSimulationSection_

- [ ] 7. Integration: ダッシュボードページの組み立てとナビゲーション
- [ ] 7.1 DashboardPageの実装
  - `web/src/pages/DashboardPage.tsx` に、表示モード状態・プロフィール未登録時の全体ガード（要件16.1）・ダイエットモード無効時のダイエット画面アクセスガード（要件16.3）を実装し、3〜6で実装した各セクションへデータを配線する
  - プロフィール未登録の状態でページを開いた場合に登録案内のみが表示され他セクションが表示されないこと、いずれかのセクションの取得が失敗しても他セクションが独立して表示され続けることを確認する
  - _Requirements: 2.1, 2.2, 2.3, 16.1, 16.3, 16.4, 16.5, 17.1, 17.2, 17.3_
  - _Depends: 3.1, 3.2, 3.3, 3.4, 4.1, 4.2, 4.3, 5.1, 5.2, 6.1, 6.2, 6.3, 6.4, 6.5_
  - _Boundary: DashboardPage_
- [ ] 7.2 App.tsxへの簡易ナビゲーション追加
  - `web/src/App.tsx` に、既存のプロフィール編集画面と`DashboardPage`を行き来する簡易ナビゲーション（ルーティングライブラリを追加しない、React状態によるページ切替）を追加する
  - ナビゲーション操作でプロフィール編集画面とダッシュボードが相互に表示切替できることを確認する
  - _Requirements: 17.1, 17.2_
  - _Depends: 7.1_
  - _Boundary: App.tsx_

- [ ] 8. Validation: 単体テストの拡充
- [ ] 8.1 (P) 新設APIクライアントメソッドの境界値・エラーケーステストの拡充
  - `nutritionClient.getDietInsights`の`profile_missing` / `diet_mode_disabled` / `incomplete_diet_mode_data`の各`reason`、`menuPlanClient.getShoppingList`の対象週未生成（`null`）、`mealSlotClient.getEatingOutSuggestion`の対象食事枠なし（404）・代替案なし（`suggestion: null`）を網羅するテストケースを追加する
  - _Requirements: 8.5, 9.4, 10.5, 16.2_
- [ ] 8.2 (P) useAsyncDataフックの拡充テスト
  - 複数の非同期取得を並行実行した場合に、一部の失敗が他の取得結果に影響しないこと、再実行時にローディング状態が正しく遷移することを網羅するテストケースを追加する
  - _Requirements: 16.4, 16.5_

- [ ] 9. Validation: E2E
- [ ] 9.1 週間献立表示・差し替え・レシピ詳細・フィードバックのE2E確認
  - 栄養評価画面とダイエット状況画面を切り替え、1週間のおすすめ献立セクションが栄養評価画面にのみ表示されダイエット状況画面には表示されないこと、週全体/日単位の差し替えボタンが仕様どおりの位置に配置され処理中は再クリックが防止されること、食事セルクリックでレシピ詳細+追加副菜提案が表示され好き/苦手を送信できることをブラウザ操作で確認する
  - _Requirements: 4.1, 4.2, 4.4, 4.5, 5.1, 5.2, 5.5, 6.1, 6.2, 6.3, 7.1, 7.2_
  - _Depends: 7.2_
- [ ] 9.2 ダイエットモード入力操作・買い物リスト/外食提案/ダイエットインサイト・依存データ欠損時のE2E確認
  - ダイエット状況画面で摂取カロリー手動修正・追加運動記録の入力フォームから送信しカロリー収支グラフに反映されること、買い物リスト・外食時の代替提案・体重推移予測/停滞期アドバイス/運動併用シミュレーションの各セクションが対応する上流APIのレスポンスをそのまま表示すること、プロフィール未登録の状態でダッシュボードを開いた場合に登録案内が表示され他セクションが表示されないこと、ダイエットモード無効なプロフィールでダイエット状況画面へアクセスした場合に設定案内が表示されることをブラウザ操作で確認する
  - _Requirements: 8, 9, 10.3, 10.5, 12.3, 12.4, 12.5, 12.6, 13, 14, 15, 16.1, 16.3_
  - _Depends: 7.2_
- [ ] 9.3 レスポンシブ表示のE2E確認
  - ブラウザ幅をデスクトップ幅からスマートフォン幅に縮小し、主要なコンテンツが読み取り可能なレイアウトを維持すること、1週間のおすすめ献立セクションが横スクロール可能な表示に切り替わることを確認する
  - _Requirements: 17.3, 17.4_
  - _Depends: 7.2_

## Implementation Notes
- (3.4) `web/src/components/dashboard/NutritionSummarySection.tsx`+`web/src/components/dashboard/charts/`（新設ディレクトリ）の`PfcBarChart.tsx`/`NutrientSufficiencyList.tsx`を新規作成。「今日」のみを対象とし、期間切替（task 3.5）は意図的に未実装のまま残した。`MicronutrientTargets`（目標、9項目）と`VerifiedNutritionValues`（実績、`DayMenu.dayNutrition`）の8項目は同名フィールド対応、食塩相当量のみ`saltEquivalentG`（実績）÷`saltEquivalentUpperLimitG`（上限目安、非対称な命名）という特殊対応で、表示も他8項目の「%」ではなく「{実績}/{上限}g」形式。要件1.4（充足率100%超は実際の値をそのまま表示）は、バー幅用の`fillPct`（100キャップ）と表示用の`displayPct`（非キャップ）を独立変数として分離実装し、レビューがmutation testで負荷担保性を確認済み。エネルギーカードの「活動量による加算」表示は`tdee - bmr`という単純な表示演算のみ（要件1.5が禁じる栄養計算の再実装ではないと判断——`NutritionSummary`にこの内訳専用フィールドが存在しない以上、既に確定した2値からの差分表示以外に要件1.1を満たす手段がないため）。**round 1でREJECTED**: 食塩相当量行の表示が実績値・上限値を未フォーマットの生浮動小数点のまま文字列補間しており、`nutrition-verification.service.ts`の丸めなし加算処理により実運用データでは確実に長い小数（例: `6.823476/7.5g`）になる欠陥をレビューが発見（テストのサンプル値がたまたま丸めた値だったため検出されていなかった）。round 2で`formatGrams1()`（`toFixed(1)`）を実績・上限両方に適用し、非整数の食塩相当量を用いた新規テストで固定小数点表示を確認、レビューが独立に再検証してAPPROVED。実装者自身が申告した別の懸念（食塩相当量行のみゼロ除算ガードが非対称に欠如）はレビューがコード確認の結果**事実誤認**と判明（`sufficiencyPercent()`が全9項目に対称的にガード済み）——自己申告を鵜呑みにせず独立検証する規律の実例として記録。全243/243（3.3時点231+11+round2の+1）、typecheck/buildクリーン。
- (3.5) `web/src/components/dashboard/PeriodToggle.tsx`+`.test.tsx`を新規作成（`ModeToggle`と同じ純粋制御コンポーネント方式、内部状態を持たず`weekAvailable=false`時はネイティブ`disabled`+ハンドラ内ガードの二重防御で週選択を無効化——要件18.4）。`NutritionSummarySection.tsx`に`period`/`onPeriodChange`/`weekPlan`の3propsを追加し、期間の状態自体は保持しない（状態リフトは7.1のDashboardPageの責務のまま）。週平均の算出方針は設計文書の根拠に基づき次の通り確定: (a) エネルギーカードの週平均は`weekPlan.days[].dayNutrition.energyKcal`の単純平均（`nutritionClient`の再フェッチは行わない。design.mdの129-130/260/277/298-299/373-375行目が、`DietGoalStatusSection`（6.1、未実装）の`dietMode.calorieTarget`専用の7回`getSummary`呼び出しパターンをこのコンポーネントに拡張しないことを明示）、(b) `PfcBarChart`の週モードはグラム表示を省略し`pfcRatio`のみで「平均{pct}%」表示（`pfc`propを新たにoptional化、積み上げバー自体は`pfcRatio`を使うため常にモード非依存）、(c) `NutrientSufficiencyList`はコアロジック無改修、週モードでは`NutritionSummarySection`側で13フィールド全てを事前平均した`VerifiedNutritionValues`を`actual`propとして渡すのみ。`weekPlan===null`かつ週選択時は未生成メッセージを表示しクラッシュしない。実装ブリーフ内に自己矛盾（`NutrientSufficiencyList.tsx`を「無改修」と指示しつつ`captionOverride`propの追加も許可）があったが、実装者が「今日/今週で異なるキャプション文言をmockup.htmlが要求している以上、両方同時表示という明確に劣るUX以外に無改修での解決策がない」と判断し、デフォルト値が既存文言と完全一致する最小限のoptional prop追加のみで解決（`NutrientSufficiencyList.test.tsx`は無改修のまま0差分）。レビューがこの判断自体を独立に精査し、mockup.html 588-662行目の実文言差分を自ら確認した上でAPPROVED（範囲逸脱ではなく妥当な最小修正と判定）。週平均計算はレビューが実際にmutation testで検証（平均化ロジックを`days[0]`のみの参照に置換→平均依存の2テストのみが期待通り失敗→復元）。全260/260、typecheck/buildクリーン。
- (4.2) `web/src/components/dashboard/MealCell.tsx`+`.test.tsx`、`RecipeDetailModal.tsx`+`.test.tsx`を新規作成し、`DayColumn.tsx`（task 4.1）と`WeeklyMenuSection.tsx`（task 4.1）を改修。`MealCell`は状態を持たない純粋な子コンポーネント（`{mealType, dishName, onClick}`のみ）で、`DayColumn`の各食事行（従来はクリック不可の`<div className="dish">`）を実際にクリック可能な`<button className="dish dish-link">`へ置き換えた（task 4.1では「task 4.2の責務」として意図的に据え置かれていた部分）。`RecipeDetailModal`は本specで唯一`mealSlotClient.generateRecipeDetail`を直接呼び出す例外コンポーネントとして実装した——design.mdのTraceability表（要件6.1-6.6）がこのクライアント呼び出しを他の全セクションと異なり`RecipeDetailModal`自身に割り当てていること、および要件6.1が「食事セルクリック→モーダルを開く→生成処理を呼び出す」を1つのオンデマンド操作として記述していること（週7日×4食=28件のレシピ詳細をDashboardPageが事前に全件取得するのは要件8.1の「オンデマンド」に反し無駄であること）を根拠とする。`useAsyncData`（task 1.1で実装済みだが本specの実コンポーネントでは初めての実使用）を、`Result<T,ApiError>`を`await`後に`!result.ok`なら`throw result.error`する薄いアダプタ経由で用い、`AsyncDataState.error`のドキュメント規約（「上流spec固有のエラー型をそのまま保持する」）どおり実際の`ApiError`をそのまま保持する。材料の数量表示は通常`{quantity}{unit}`だが「大さじ」「小さじ」の2単位のみ日本語の自然な語順に合わせ`{unit}{quantity}`（例:「大さじ1」）とする例外を実装（レビューがこの例外分岐を実際にmutation testで除去し、対応する1テストのみが期待通り失敗することを確認）。追加副菜提案の栄養増分は`NutritionValues`の実4フィールド（エネルギー・PFC）すべてを符号付き表示し、mockup.htmlの例示文言にのみ存在し実データ型には存在しない食物繊維増分は表示しない（要件6.3・menu-generation要件9.3の「エネルギー量・PFC量」のみという文言と整合）。`RecipeDetail`に`dishName`フィールドは存在しない（task 16.1のmenu-generation amendmentで意図的に追加されなかった、既にMealSlot側で判明済みのため）ため、モーダル見出しの`dishName`はpropとして親から渡され、生成中/失敗状態と無関係に即座に表示される。`WeeklyMenuSection`は新たに`selectedMeal: {dayIndex, mealType} | null`のみを保持し、`dishName`はキャッシュせず`data`propから毎回再導出する（背景再取得で`data`が更新された場合も追従するため）。既存の`DayColumn.test.tsx`の5件の`getByRole("button")`アサーションは、食事セルが実ボタン化されたことで曖昧になったため`.regen-btn`への直接クエリに修正（対象アサーションの検証意図自体は変更していないことをレビューが個別に確認）。全308/308、typecheck/buildクリーン。
- (4.3) `web/src/components/dashboard/FeedbackControl.tsx`+`.test.tsx`を新規作成し、`RecipeDetailModal.tsx`（task 4.2）を改修して成功時コンテンツの最後（design.mdのComponents table記載順「材料・手順・カロリー・追加副菜提案・FeedbackControlの表示」どおり）に埋め込んだ。`RecipeDetailModal`の`generateRecipeDetail`直接呼び出し（task 4.2で確立済み）と同じ理由付けにより、`FeedbackControl`自身が`mealSlotClient.submitFeedback`を直接呼び出す（design.mdのComponents table「FeedbackControl: 好き/苦手の選択、送信中/送信済み/失敗状態の表示」という記載が、このコンポーネント自身が送信ライフサイクルを保持することを含意するため）。状態機械は`idle→submitting→submitted|failed`の4状態で、要件7.4「入力前の表示状態を保持する」は「送信前の操作可能な両ボタン表示に戻る」ことと正しく解釈し実装（失敗時に両ボタンを再度クリック可能な状態へ戻し、実際に再送信できることをテストで確認——見た目だけ有効化され実際は送信不可、という誤読を回避）。mockup.htmlには本コンポーネントに対応するマークアップが一切存在しない（好き/苦手/feedback/thumbのいずれで検索してもゼロ件）ため、クラス名・視覚構造は実装者の裁量とした。`<FeedbackControl>`には`key={weekStartDate-dayIndex-mealType}`を付与——モーダルを閉じずに別の食事セルへ直接遷移した場合に前の食事の送信済み状態が新しい食事に漏れることを防ぐ意図だったが、実装者が実際にmutation testで検証した結果、**現状ではこのkeyは当該遷移経路に対して実質的に無効**であることが判明（`useAsyncData`のeffectがdeps変化のたびに同期的に`setIsLoading(true)`するため、`RecipeDetailModal`自身の`!isLoading`ゲートが新しいレシピ詳細の取得完了を待たずに成功時コンテンツ全体を先にアンマウントしてしまい、keyの有無に関わらず`FeedbackControl`は既に再マウントされている）。実装者はこれを隠さず正直に報告し、`RecipeDetailModal`レベルのテストではなく`FeedbackControl.test.tsx`内に本コンポーネント単体でkey機構そのものを直接証明する独立テストを追加（同一keyのまま`dayIndex`/`mealType`だけ変えた場合に古い送信済み表示が漏れることを実際に確認）。keyプロパティ自体は「現状は冗長だが将来`useAsyncData`のローディングゲート挙動が変わった場合の防御・意図の明示」として意図的に残置——レビューが実際にuseAsyncData.tsのソースを読んでこの技術的主張を独立に再現・検証し、正直で誇張のない報告と判断した上でAPPROVED。全321/321、typecheck/buildクリーン。
- (5.1) `web/src/components/dashboard/ShoppingListSection.tsx`+`.test.tsx`を新規作成。`ShoppingListItem.category`が既に人間可読な日本語ラベル文字列そのもの（`z.enum`の値が「野菜・きのこ」等）であるため、ラベルマップ変換は不要で、固定順の`CATEGORY_ORDER`配列でグループ化し、再解釈せずそのまま見出しとして使用。`WeeklyMenuSection`と同じ`{data, isLoading, error}`のみのpropsで、コールバックなし（本セクション自体はユーザー操作を持たない純粋表示）。4分岐（読み込み中/取得失敗/未生成（`menuPlanClient.getShoppingList`の「200+null」規約と一致、生成ボタンなし——`WeeklyMenuSection`の空状態とは異なりこのセクション自体は生成アクションを持たない）/表示）は`WeeklyMenuSection`の確立済みパターンをそのまま踏襲。品目0件のカテゴリは`.shop-cat`ブロックごとスキップ（要件8.2の「分類したグループ構成のまま表示する」は全4見出しの常時表示までは要求しないという読みで、レビューも実装者の判断を独自に妥当と評価）。**round 1でREJECTED**: 「固定カテゴリ順序で描画する」という主張を検証するはずのテストが、フィクスチャの`items`配列がたまたま`CATEGORY_ORDER`と同じ順序で並んでいたため、実際には挿入順反復への退行を検知できないことをレビューがmutation testで発見（描画ループを`Array.from(grouped.keys())`に置換しても全6テストが通過してしまった）。round 2でフィクスチャの品目順序のみをスクランブル（本番コードは無変更）して再検証し、同じmutationが今度は正しく1件失敗することをレビューが独立に再現・確認した上でAPPROVED。全327/327、typecheck/buildクリーン。
- (3.6) `web/src/components/dashboard/AdviceCallout.tsx`+`.test.tsx`を新規作成。栄養評価画面・ダイエット状況画面の両方で使う共通コンポーネントとして、`{variant:"nutrition", targets, actual}` / `{variant:"calorieBalance", dailyVariances}` の判別共用体propsのみを受け取る純粋プレゼンテーションコンポーネントとし、`nutritionClient`/`menuPlanClient`/`dailyLogClient`のいずれも呼び出さない（design.mdのTraceability表がAdviceCalloutの行に3つのAPIクライアントを列挙しているのは機能全体の依存関係であり、本コンポーネント自体の呼び出し義務ではないと判断）。栄養評価用は`NutrientSufficiencyList.tsx`の`NORMAL_NUTRIENT_ROWS`（8項目、食塩相当量を除く）とゼロ除算ガード付き`sufficiencyPercent`を値レベルで複製し、充足率最小の1項目のみを定型文に当てはめる（要件19.1、19.3——mockup.html 664-667行目は2項目+架空の食材提案理由を含む例示文言だが、これは要件19.3の「選択と定型文当てはめ以外の判定・計算を行わない」に反するため、単一項目・単一数値のみの簡潔な文言に意図的に変更）。ダイエット状況用は`dailyVariances`（`{dayLabel, varianceKcal}[]`、曜日整形は将来の呼び出し側の責務として本コンポーネントの範囲外）から`varianceKcal`最大の1日を選び符号付きkcalで表示、値が負でも（=どの曜日も未超過でも）追加の抑制判定は行わない（「表示する価値があるか」の判断は将来の呼び出し側がデータを渡すかどうかで制御する設計とし、要件19.3の「選択+定型文当てはめ以外の判定を行わない」を厳密に遵守）。両バリアントとも対象データがnull/空の場合は何も描画しない（要件19.4）。`NutritionSummarySection.tsx`を含む既存ファイルは一切変更していない（本コンポーネントの実画面への組み込みは、カロリー収支バリアントはtask 6.2、栄養評価バリアントはtask 7.1のDashboardPage組み立て時に行われる想定——tasks.md全体を検索し「AdviceCallout」の言及がtask 3.6自身とtask 6.2以外に存在しないことを実装者・レビュー双方が確認済み）。レビューが2件のmutation test（最小選択→最大選択に反転、null-guard除去）で選択ロジックとnull抑制を独立に再現・復元確認、食塩除外・ゼロ除算ガードもコード直読で確認しAPPROVED。全269/269、typecheck/buildクリーン。
- (4.1) `web/src/components/dashboard/DayColumn.tsx`+`.test.tsx`、`WeeklyMenuSection.tsx`+`.test.tsx`を新規作成。`DayColumn`は`{day, isRegenerating, disabled, errorMessage, onRegenerate}`のみを受け取る状態を持たない純粋な子コンポーネントで、ネットワーク呼び出しは一切行わない。曜日ラベルは固定配列`WEEKDAY_LABELS`（月火水木金土日）を`dayIndex`（0-6）で参照し、この対応はmenu-generation設計文書（design.md 628行目「weekStartDateは月曜日始まり」）で裏付け済み。食事表示順は`DayMenu.meals`の配列順ではなく`mealType`によるルックアップ（`meals.find`）で固定の朝食/昼食/夕食/間食順に整列し、スキーマが`.length(4)`のみを保証し順序は保証しないことに対応（レビューがシャッフル済み配列でこの頑健性を確認）。`WeeklyMenuSection`は`{data, isLoading, error}`（design.mdの`DashboardSectionProps<T>`規約）+`onRegenerateWeek`/`onRegenerateDay`コールバックのみを受け取り、`menuPlanClient`を自ら呼び出さない（データ取得・実際の差し替え結果の反映は将来のDashboardPage、task 7.1の責務）。要件5.5の「同一範囲への重複差し替え防止」は、週全体差し替えと日単位差し替えが同じ`WeekMenuPlan`を変更しうるため単一のグローバルロック（`regeneratingScope: "week"|number|null`）として意図的に保守的に解釈し、いずれか1件が進行中は週ボタン・全曜日ボタンすべてを無効化する設計とした（レビューが実際にmutation testでこの相互ロックを検証: ロックをスコープ単位に緩めると相互ロックの2アサーションのみが失敗することを確認し復元）。失敗時は単一の`scopeError`スロットに簡潔な汎用メッセージを設定し（`ApiError.type`による分岐は行わない）、`data`プロパティ自体は本コンポーネントが書き換えないため、失敗時に元のフィクスチャ内容がそのまま表示され続けることをテストで確認。未生成状態（`data===null, isLoading===false, error===null`——`getWeekPlan`の「200+null」規約と一致）は週差し替えと同一ハンドラを再利用する「生成」ボタンを表示（要件4.5、5.3を同一エンドポイントとして統合）。mockup.htmlの`.restriction-chip`再掲・`.section-caption`案内文・「今日」ハイライト・食事セルのクリック可能化（task 4.2の範囲）は意図的にすべて対象外とした。`<h2>`見出し・`.block-head`ラッパーも本コンポーネントでは描画しない——既存の`NutritionSummarySection.tsx`/`RestrictionChip.tsx`が同様に見出しを自前で持たない前例と、design.mdの`DashboardSectionProps<T>`にtitleフィールドが存在しないことから、共通の見出し描画は将来のDashboardPage（task 7.1）の責務と判断（レビューがこの判断を独立に精査し、要件4.1充足の最終確認はtask 7.1またはfeature-level validationで持ち越すべき事項として明示的に記録した上でAPPROVED）。レビューは再入力ガード（`if (regeneratingScope !== null) return`）を除去しても既存テストは全て通過する（jsdomのネイティブdisabled属性が実質的に同じ保護を提供するため）というテストカバレッジ上のギャップを1件指摘したが、要件5.5の観測可能な挙動自体は満たされているためブロッキングではないと判定。実装者は組織の月次利用上限エラーで中断したが、`git status --porcelain`で汚染がないことを確認した上で同一エージェントを`SendMessage`で再開し、`DayColumn.tsx`（中断前に完成済み）に対する`DayColumn.test.tsx`を後追いで作成・全件通過を確認後、`WeeklyMenuSection.test.tsx`を先に書く正規のRED→GREENで`WeeklyMenuSection.tsx`を実装。全292/292、typecheck/buildクリーン。
- (3.3) `web/src/components/dashboard/RestrictionChip.tsx`を新規作成。`ProfileStrip`/`ModeToggle`と同じ純粋プレゼンテーション方針（`restrictionType`/`restrictionIntensity`/`restrictionNotes`のみをprops化）。`mockup.html`の`.restriction-chip`構造（「現在の食事制限設定：{タイプ}・{強度}」+条件付き`.note`）を再現し、`DietRestrictionSection.tsx`の`RESTRICTION_TYPE_LABELS`/`RESTRICTION_INTENSITY_LABELS`と値レベルで同一のローカル複製を用いる。要件3.3（「制限なし」時の表示）は、タイプ非"none"だが強度が`null`という型上は許容されるが実際には到達しないはずの状態も含め防御的にフォールバックし、「・null」という壊れた表示にならないようにした——レビューが独自のライブmutation test（このガードを削除→2テストが`"制限なし・null"`という具体的な壊れた文字列で失敗することを確認→復元）で負荷担保性を確認済み。全231/231（3.2時点225+6）、typecheck/buildクリーン。
- (3.2) `web/src/components/dashboard/ProfileStrip.tsx`を新規作成。`ModeToggle`と同じ方針で、`profileClient`/`nutritionClient`を自ら呼ばない純粋なプレゼンテーション（`profile: Profile`+`activityLevelLabel: string`のみをprops化。データ取得はDashboardPage/task 7.1の責務）。`mockup.html`の`.profile-strip`構造（常時表示5chip+条件付きの結合`.chip.goal`1件）をそのまま再現し、目標体重・目標期間は2つに分割せず単一chip内に結合表示（レビューが結合フォーマットをmutation testで直接証明: 2つに分割する変更を注入したところ`toContain("目標期間")`アサーションが即座に失敗）。性別ラベルは`BasicInfoSection.tsx`の`GENDER_LABELS`（未export）と値レベルで同一のローカル複製を用いる（値の重複自体は容認済みの前例）。全225/225（3.1時点219+6）、typecheck/buildクリーン。
- (3.1) `web/src/components/dashboard/ModeToggle.tsx`（`web/src/components/dashboard/`初のUIコンポーネント）を新規作成。design.mdのComponents表（ModeToggle=切替コントロールのみ、DashboardPage=表示モード状態管理・セクション出し分け）に従い、`ModeToggle`自体は`mode`/`onModeChange`/`dietModeEnabled`のみを受け取る純粋な制御コンポーネントとし、まだ存在しないセクションコンポーネント（`WeeklyMenuSection`等、task 4-6）は一切importしない。要件2.2/2.3が要求する「モード切替→対応セクションのみ表示」は、テストファイル内のローカルなstateful harness（2つのスタブ`<div>`をuseStateで出し分け）で本タスクの境界内において実証し、実際のセクションへの配線はtask 7.1（DashboardPage）に委ねる。要件2.6（ダイエットモード無効時の案内表示）はネイティブ`disabled`属性+案内メッセージの両方で満たした。レビューが1件、非ブロッキングの指摘を記録: クリックハンドラ内の`dietModeEnabled`ガード（コメントは「二重にガード」と主張）は、ライブmutation testで確認したところ実際には到達不能な死んだコードだった——ネイティブ`<button disabled>`はjsdomでも実ブラウザ同様クリックイベント自体を発火させないため、ハンドラ内ガードを削除しても全8テストが通ってしまう。機能的な欠陥ではない（要件2.6は`disabled`属性で正しく満たされている）が、将来この要素がネイティブbutton以外（例: `<div role="tab">`）に置き換わった場合、このガードが唯一の防御になる——ハンドラを直接呼ぶテストがない限りその退行を検知できない、という点を将来の参考として記録。全219/219（2.3時点211+8）、typecheck/buildクリーン。
- (2.3) `web/src/api/mealSlotClient.ts`（`generateRecipeDetail`/`submitFeedback`/`getEatingOutSuggestion`）を新規作成。task 2.1/2.2と異なり**今回はhttpClient/types.tsの拡張が一切不要**だった——実装前に`feedback.service.ts`/`eating-out-suggestion.service.ts`の実際の戻り値型を確認し、`recordFeedback`は`Result<void, ValidationError | NotFoundError>`のみ、`suggestForMealSlot`は`Result<EatingOutSuggestionResult, NotFoundError>`のみで、いずれも`GenerationError`を返し得ない（Claude APIを呼ばない同期処理のため）ことを確認した上で、既存の`ApiError`（`ValidationError`/`NotFoundError`/`GenerationError`、task 2.1/2.2までに整備済み）だけで全ケースを覆えると判断し、境界を`mealSlotClient.ts`/`.test.ts`の2ファイルのみに収めた。`getEatingOutSuggestion`の「候補なし」は`ShoppingList`/`WeekMenuPlan`（task 2.2、対象週未生成時は裸の`null`ボディ）とは異なり、常に`{suggestion: ... | null}`というオブジェクト形状の200レスポンスであることをテストで明示的に区別。レビューが独自のライブmutation test（`submitFeedback`の`liked`引数を`true`に固定化→`liked:false`のテストのみ失敗確認→復元）で負荷担保性を確認済み。全211/211（2.2時点201+10）、typecheck/buildクリーン。これでタスクグループ2（フロントエンドAPIクライアント3本）が完了。
- (2.2) `web/src/api/menuPlanClient.ts`（`getWeekPlan`/`regenerateWeek`/`regenerateDay`/`getShoppingList`）を新規作成。task 2.1と同種だが異なる理由のギャップ: `menu-generation`の`GenerationError`（409/502、`{type:"generation_failed", reason, message}`）は`@nutrition/shared`に一切公開されておらず（`server/src/menu-generation/menu-plan.service.ts`のローカル型のみ）、task 2.1の`CalculationUnavailableError`（shared公開済み→import再利用）とは異なり、`ValidationError`/`NotFoundError`と同じ「sharedにまだ無いためweb側にローカル複製する」前例に倣い`types.ts`へ8種の`GenerationFailureReason`列挙値を含むローカル型を追加、`httpClient.ts`に対応するガード+分岐を追加した。`regenerateDay`が返し得る404（`NotFoundError`、対象週にプラン自体が存在しない場合）は既存の`isNotFoundErrorPayload`がそのまま認識するため新規コード不要——これをテストで明示的に証明させた。`getWeekPlan`/`getShoppingList`の200+`null`（対象週プラン未生成）は失敗ではなく`{ok:true, value:null}`として明確に区別されることをテストで確認（8.5の明示的な受け入れ条件）。レビューが独自のライブmutation test（`isGenerationErrorPayload`を無効化→関連4テストのみ失敗確認→復元）で負荷担保性を確認済み。`profileClient.ts`/`dailyLogClient.ts`/`nutritionClient.ts`とそのテストは未変更（境界内）。全201/201（2.1時点190+11）、typecheck/buildクリーン。
- (2.1) `web/src/api/nutritionClient.ts`（`getSummary(date)`/`getDietInsights(date)`、`profileClient.ts`と同じ薄い`apiRequest<T>`ラッパー形式）を新規作成。**実装前に発見した既存インフラの真のギャップ**: 共通の`apiRequest`/`ApiError`/`toApiError`（`web/src/api/httpClient.ts`/`types.ts`、`user-profile`スペックで構築済み）は`type: "calculation_unavailable"`（`nutrition-engine`が409で返す`CalculationUnavailableError`）を一切認識しておらず、本タスク自身が要求する「`reason`が判別可能な戻り値として伝わること」を満たす前提が欠けていた（このまま実装すると`reason`が`type:"unknown"`へ握り潰されていた）。`nutritionClient`のboundaryに対する明示的に許可した狭い例外として、`types.ts`の`ApiError`ユニオンへ`@nutrition/shared`が既に公開する実在の`CalculationUnavailableError`型をそのままimportして追加し（`ValidationError`/`NotFoundError`が`shared`未公開のためローカル複製である前例とは異なり、`shared`公開済み型をそのまま再利用）、`httpClient.ts`に`isCalculationUnavailableErrorPayload`ガード+`toApiError`分岐を追加した。レビューが独自に確認: この例外はdesign.mdの明示的な設計意図そのもの（本specは独自のエラー型を持たず上流specのエラー形式をそのまま通過させる、という記述と、本タスクとほぼ同一文言の受け入れテストがdesign.mdに既に書かれている）であり、逸脱ではなく正しい実装。ライブmutation test（`toApiError`の新分岐を削除→関連8テスト中6テストが正しく失敗→復元）で負荷担保性を確認済み。`profileClient.ts`/`dailyLogClient.ts`とそのテストは未変更（境界内）。全190/190（1.1時点182+8）、typecheck/buildクリーン。
- (1.1) `web/src/hooks/useAsyncData.ts`（`web`パッケージ初のカスタムフック）を新規作成。非同期取得関数+依存配列を受け取り`{ data, isLoading, error, refetch }`を返す。フィールド名はdesign.md `DashboardSectionProps<T>`（`data`/`isLoading`/`error`）と構造的に一致させており、後続タスク（3.x以降）が各セクションでこのフックの戻り値をそのまま`DashboardSectionProps<T>`へ展開できる設計。要件16.4（処理中表示）・16.5（失敗時のエラー表示、および本タスク自身が明記する「直前に取得済みのデータの非破棄」）を、レビューが実施したライブmutation test（`setData(null)`を失敗パスに注入→当該テストが`expected null to deeply equal {id:1}`で失敗することを確認→復元）で負荷担保性を確認済み。**このフックはまだどのコンポーネントからも呼び出されていない**（呼び出し配線は3.x以降のタスクの責務）ため、Feature Flag Protocolは適用せず通常のRED→GREENで実装した（design.mdにもこのフック自体の複数セクション間調整は不要と記載されている）。レビューが1件、非ブロッキングの改善提案を記録: アンマウント後の`setState`防止テスト（`isCancelled`ガード）が、実際にはガードを取り除いても全テストが通ってしまう名ばかりの検証だった（React 19は非マウント後`setState`を警告しないため）。ガード自体は実装されており機能的な欠陥ではないが、テストの主張が実態と合っていない——将来テストを強化する際の参考として記録。全182/182（baseline 175+7）、typecheck/buildクリーン。
