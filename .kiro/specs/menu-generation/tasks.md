# Implementation Plan

- [ ] 1. Foundation: データベーススキーマと共有型定義
- [x] 1.1 食品成分・単位正規化テーブルのマイグレーション作成
  - `server/src/db/migrations/005_create_food_items.sql` を作成し、design.md記載の `food_items` テーブル（`food_id`主キー、100gあたり栄養価カラム群、`source_citation`、買い物リスト表示用の`display_unit_code`）を定義する
  - `server/src/db/migrations/006_create_unit_conversions.sql` を作成し、`unit_conversions` テーブル（`food_id`はNULL許容外部キー、`(food_id, unit_code)`ユニーク制約）を定義する
  - アプリ起動時のマイグレーションランナーがこれら2ファイルを順次適用し、`food_items`/`unit_conversions` テーブルがSQLiteファイル上に作成されることを確認する
  - _Requirements: 4.1, 5.1_
- [x] 1.2 献立プラン関連テーブルのマイグレーション作成
  - `server/src/db/migrations/007_create_menu_plan_tables.sql` を作成し、`week_menu_plans` / `day_menus` / `meal_slots` / `meal_ingredients` の4テーブルをdesign.mdの物理データモデルどおりに定義する（CASCADE DELETE、ユニーク制約を含む）。`meal_slots`には要件4.7の微量栄養素9項目（`fiber_g`/`calcium_mg`/`iron_mg`/`vitamin_a_ug`/`vitamin_d_ug`/`vitamin_b1_mg`/`vitamin_b2_mg`/`vitamin_c_mg`/`salt_equivalent_g`）の列を含める。`day_menus`には微量栄養素の列を追加しない（`DayMenu.dayNutrition`は読み取り時に`meal_slots`4行を合算して構築するため）
  - マイグレーション適用後、`day_menus.day_index`と`meal_slots.meal_type`に対するCHECK制約およびユニーク制約がSQLiteスキーマ上に反映されていること、`meal_slots`に微量栄養素9列が存在することを確認する
  - _Requirements: 1.3, 7.1, 4.7_
- [x] 1.3 レシピ詳細・満足度フィードバックテーブルのマイグレーション作成
  - `server/src/db/migrations/008_create_recipe_detail_tables.sql` を作成し、`recipe_details` / `supplementary_suggestions` / `supplementary_ingredients` を定義する
  - `server/src/db/migrations/009_create_satisfaction_feedback.sql` を作成し、`satisfaction_feedback`（`(week_start_date, day_index, meal_type)`ユニーク制約、`meal_slots`へのFKなし）を定義する
  - マイグレーション適用後、`recipe_details.meal_slot_id`がユニーク制約付きであること、`satisfaction_feedback`が`meal_slots`への外部キーを持たないことをスキーマ上で確認する
  - _Requirements: 8.4, 10.2, 10.5_
- [x] 1.4 共有Zodスキーマと型定義の追加
  - `shared/src/menu.schema.ts` を新規作成し、`IngredientSelection` / `NutritionValues` / `MealSlot` / `DayMenu` / `WeekMenuPlan` / `RecipeDetail` / `SupplementarySuggestion` / `FeedbackInput` 等のZodスキーマと推論型をdesign.mdのService Interfaceどおりに定義する
  - `server`パッケージから `shared` の当該スキーマをimportしてビルドが通ることを確認する
  - _Requirements: 1.1, 4.3, 8.1, 10.1_
- [x] 1.5 Claude API連携の基盤設定
  - `server`パッケージに公式TypeScript SDK（`@anthropic-ai/sdk`）を依存関係として追加する
  - `server/src/menu-generation/constants.ts` を作成し、既定モデルID（`claude-sonnet-5`）、週間/日単位生成用のthinking/effort設定（`adaptive`/`medium`）、レシピ詳細生成用のthinking/effort設定（`disabled`/`low`）、既知の単位コード一覧を名前付き定数として定義する
  - APIキーを環境変数（例: `ANTHROPIC_API_KEY`）から読み込む起動時設定を追加し、未設定時にアプリ起動ログへ警告が出力されることを確認する
  - _Requirements: 1.2, 3.4_

- [ ] 2. Foundation: 食品成分参照データの投入
- [x] 2.1 MEXT八訂ベースの厳選食品カタログの投入
  - 「日本食品標準成分表（八訂）増補2023」の一次資料から、主食・肉類・魚介類・卵類・乳類・豆類・野菜類・果実類・調味料類・菓子類等の主要カテゴリを横断する数百件規模の食品を選定し、`food_items`テーブルへ投入するシードデータ（マイグレーションまたは専用シードスクリプト）を作成する
  - 各行に `source_citation` として「日本食品標準成分表（八訂）増補2023年から引用」を設定する
  - 卵・豆腐・パン・葉物野菜等、日常的に個数・束・丁・玉・パック単位で数えられる食品には`display_unit_code`（例: '個'/'丁'/'束'/'玉'/'パック'）を設定し、肉・魚・米・調味料等の重量売り食品は`display_unit_code`をNULLのままにする
  - 投入後、`FoodCompositionRepository`相当の直接クエリで代表的な食品ID（例: 米・鶏むね肉・卵・キャベツ）が正しい栄養価で取得できることを確認する
  - _Requirements: 4.1, 4.2_
- [x] 2.2 分量単位正規化テーブルへのデータ投入
  - `unit_conversions`に汎用エントリ（`food_id = NULL`）としてg・大さじ・小さじ・カップ等の標準的なグラム換算値を投入する
  - 卵・バナナ・玉ねぎ等、個数単位の換算が食材依存になる主要食材について、`food_id`を指定した食材固有エントリを投入する
  - 投入後、食材固有エントリが存在する食品IDについて汎用エントリより優先して取得できることをクエリで確認する
  - _Requirements: 5.1, 5.2, 5.3_

- [ ] 3. Core: 食品成分照合・単位正規化・栄養価検証
- [x] 3.1 (P) FoodCompositionRepositoryの実装
  - `server/src/menu-generation/food-composition.repository.ts` に `findById` / `listAllIds` / `findUnitConversion` / `findGenericUnitConversion` を実装する
  - `listAllIds()` が `food_items` の全食品IDを返し、`findById()` が存在しない食品IDに対して `null` を返すことをテストで確認する
  - _Requirements: 3.1, 4.1, 4.2, 4.6_
  - _Boundary: FoodCompositionRepository_
- [x] 3.2 (P) UnitConversionServiceの実装
  - `server/src/menu-generation/unit-conversion.service.ts` に `toGrams(foodId, quantity, unitCode)` を実装する。design.md記載のインターフェースに対してテスト用のモック実装を用いる（`FoodCompositionRepository`本体は3.1で並行実装されるため、インターフェース契約に対してコーディングする）
  - 単位が `"g"` の場合はそのまま返し、gram以外は食材固有エントリ優先、なければ汎用エントリにフォールバックし、いずれも存在しない場合は `VerificationError(unit_not_found)` を返すことをテストで確認する
  - _Requirements: 5.2, 5.3, 5.4_
  - _Boundary: UnitConversionService_
- [x] 3.3 NutritionVerificationServiceの実装
  - `server/src/menu-generation/nutrition-verification.service.ts` に `verifyDish` / `verifyDay` / `computeVarianceKcal` を実装する。`verifyDish`はエネルギー・PFCに加え、`FoodItemNutrition.per100g`のビタミンA・D・B1・B2・C、カルシウム、鉄、食物繊維、食塩相当量の9項目を`VerifiedNutritionValues`として合算する（該当項目が`null`の食材は0として扱う）
  - `verifyDish`が複数食材の栄養価（PFC・微量栄養素9項目）を正しく合算すること、食品ID不存在時に `VerificationError(food_id_not_found)` を返すこと、`verifyDay`が4食枠分の`VerifiedNutritionValues`を合算し`DayMenu.dayNutrition`として保持されること、微量栄養素が`null`の食材を含む場合でも計算全体が失敗せず該当項目が0扱いで合算されることをテストで確認する
  - _Requirements: 4.3, 4.4, 4.5, 4.6, 4.7_
  - _Depends: 3.1, 3.2_
  - _Boundary: NutritionVerificationService_

- [ ] 4. Core: 上流spec連携用Gateway
- [x] 4.1 (P) ProfileGatewayの実装
  - `server/src/menu-generation/profile.gateway.ts` に `getCurrentProfile()` を実装し、`user-profile`の`ProfileService.getProfile()`をプロセス内呼び出しして`MenuProfileSnapshot`へ射影する
  - プロフィール未登録（`getProfile()`が`null`）の場合に`null`を返すこと、フィールド名・型が`user-profile`の`Profile`型と一致することをテストで確認する
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 12.1_
  - _Boundary: ProfileGateway_
- [x] 4.2 (P) NutritionGatewayの実装
  - `server/src/menu-generation/nutrition.gateway.ts` に `getTargetsForDate(date)` を実装し、`nutrition-engine`の`NutritionService.getSummary(date)`をプロセス内呼び出しして`NutritionTargetSnapshot`へ射影する（`dietMode`が非nullならそちらを、nullなら`normalMode`を採用）
  - `nutrition-engine`が`CalculationUnavailableError`を返す場合にそのままエラーとして伝播することをテストで確認する
  - _Requirements: 1.5, 4.5, 12.2_
  - _Boundary: NutritionGateway_
- [x] 4.3 (P) PlannedCalorieGatewayの実装
  - `server/src/menu-generation/planned-calorie.gateway.ts` に `submitPlannedCalories(date, plannedKcal)` を実装し、`user-profile`の`DailyLogService.setPlannedCalories(date, plannedKcal)`をプロセス内呼び出しする
  - 呼び出し失敗時に例外を投げず`Result`の失敗値として返すことをテストで確認する
  - _Requirements: 11.2, 11.4_
  - _Boundary: PlannedCalorieGateway_

- [ ] 5. Core: 献立プランの永続化
- [x] 5.1 MenuPlanRepositoryの実装
  - `server/src/menu-generation/menu-plan.repository.ts` に `getActivePlan` / `findOtherDays` / `findMealSlot` / `replaceWeek` / `replaceDay` を実装する。`replaceWeek`/`replaceDay`は`MealSlot.nutrition`（`VerifiedNutritionValues`13項目）を`meal_slots`の対応する列にそのまま書き込む。`getActivePlan`は各日の`DayMenu.dayNutrition`を、`day_menus`の列からではなく当該日の4件の`meal_slots`行を読み取り時に合算して構築する
  - `replaceWeek`が対象週の既存データを単一トランザクションで全置換すること、`replaceDay`が対象日以外の`meal_slots`に影響しないこと、`findOtherDays`が対象日を除く6日分の料理名・食品IDのみを返すこと、`getActivePlan`が返す`DayMenu.dayNutrition`が当該日の4食枠（微量栄養素9項目を含む）の合算値と一致することをテストで確認する
  - _Requirements: 1.3, 1.6, 6.1, 6.3, 7.1, 7.2, 7.4, 4.7_
  - _Boundary: MenuPlanRepository_

- [ ] 6. Core: Claude API連携とプロンプト構築
- [x] 6.1 ClaudeMenuClientのtool定義構築ロジックの実装
  - `server/src/menu-generation/claude-menu.client.ts` に、週間生成・日単位生成・レシピ生成それぞれの`strict: true`・`additionalProperties: false`のtool定義を構築するロジックを実装する。食材選択プロパティの`food_id`は`FoodCompositionRepository.listAllIds()`から取得した一覧を`enum`として埋め込み、`quantity`（正の数値）と`unit`（既知の単位コードの`enum`）を必須プロパティとする
  - 構築されたtool定義のJSON Schemaが`additionalProperties: false`と`required`を含み、`food_id`の`enum`が食品成分DBの全件と一致することをテストで確認する
  - _Requirements: 3.1, 3.2, 3.4_
  - _Boundary: ClaudeMenuClient_
- [x] 6.2 ClaudeMenuClientの生成メソッドの実装
  - `generateWeek` / `generateDay` / `generateRecipe` を実装し、`tool_choice`で対象toolを固定して呼び出す。既定モデル・thinking/effort設定は`constants.ts`から取得する。tool定義にはプロンプトキャッシュ（`cache_control`）を適用する
  - レスポンスの`stop_reason`が`refusal`の場合に`ClaudeGenerationError(type: "refusal")`を、ネットワーク/サーバーエラー時に`type: "request_failed"`を、`tool_use.input`が期待するフィールド構成（例: `days`が7件、各`meals`が4件）を満たさない場合に`type: "schema_validation_failed"`を返すことを、Anthropic SDKをモック化したテストで確認する
  - _Requirements: 1.2, 1.4, 3.3, 9.2, 12.3, 12.4_
  - _Depends: 6.1_
  - _Boundary: ClaudeMenuClient_
- [x] 6.3 (P) MenuPromptBuilderの実装
  - `server/src/menu-generation/menu-prompt.builder.ts` に `buildWeeklyPrompt` / `buildDailyPrompt` / `buildRecipeDetailPrompt` を実装する
  - NG食材が除外制約として、好み食材が優先候補として、`restrictionType`が`none`の場合は制限文言を含めずに、`restrictionNotes`が原文のまま一般的なガイダンスとして、調理スキル/時間/予算感がコンテキストとして、それぞれプロンプトテキストに含まれることをテストで確認する
  - `buildDailyPrompt`では残り6日分の料理名・食品IDと重複回避の明示的な指示が、`buildRecipeDetailPrompt`ではNG食材・食事制限の制約が補助副菜提案にも適用される旨の指示が含まれることをテストで確認する
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 7.2, 7.3, 9.4, 10.3_
  - _Boundary: MenuPromptBuilder_

- [ ] 7. Core: 満足度フィードバック
- [x] 7.1 FeedbackRepositoryの実装
  - `server/src/menu-generation/feedback.repository.ts` に `upsert` / `findRecentDisliked` を実装する
  - `(week_start_date, day_index, meal_type)`が一致する既存行がある場合は`liked`・スナップショット・`updated_at`を更新し、ない場合は新規作成することをテストで確認する
  - _Requirements: 10.2, 10.5_
  - _Boundary: FeedbackRepository_
- [x] 7.2 FeedbackServiceの実装
  - `server/src/menu-generation/feedback.service.ts` に `recordFeedback` / `getDislikedSummary` を実装する。`recordFeedback`は対象食事枠が有効な献立プラン内に存在することを確認したうえでスナップショットを保存する
  - `getDislikedSummary`が`liked: false`のフィードバックのみを対象とし、上限件数で新しいものを優先することをテストで確認する
  - _Requirements: 10.1, 10.3, 10.4_
  - _Depends: 7.1_
  - _Boundary: FeedbackService_

- [ ] 8. Core: レシピ詳細の永続化
- [x] 8.1 RecipeDetailRepositoryの実装
  - `server/src/menu-generation/recipe-detail.repository.ts` に `findByMealSlotId` / `upsert` を実装する
  - 同一`meal_slot_id`への`upsert`が既存の`recipe_details`/`supplementary_suggestions`/`supplementary_ingredients`を置き換えること、対応する`meal_slots`行が削除された場合にCASCADE DELETEで従属データも削除されることをテストで確認する
  - _Requirements: 8.4_
  - _Boundary: RecipeDetailRepository_

- [ ] 9. Integration: 週間・日単位献立生成のオーケストレーション
- [x] 9.1 MenuPlanServiceの実装（週間生成・週単位再生成）
  - `server/src/menu-generation/menu-plan.service.ts` に `generateWeek` / `regenerateWeek` / `getActivePlan` を実装する。`ProfileGateway`でプロフィール未登録を検知した場合は`GenerationError(profile_missing)`を、`NutritionGateway`で栄養目標値算出不可を検知した場合は`GenerationError(nutrition_unavailable)`を返す
  - `FeedbackService.getDislikedSummary()`の結果を`MenuPromptBuilder`に渡し、`ClaudeMenuClient.generateWeek`の結果を`NutritionVerificationService`で検証したうえで`MenuPlanRepository.replaceWeek`に永続化し、成功した各日について`PlannedCalorieGateway.submitPlannedCalories`を呼び出す（失敗しても処理全体は継続する）
  - 同一週への同時生成要求を検知するインメモリロック機構を実装し、処理中の対象への重複要求に`GenerationError(generation_in_progress)`を返すことをテストで確認する。プロフィール未登録・栄養目標値算出不可・生成失敗のいずれの場合も部分的な献立プランが永続化されないことをテストで確認する
  - _Requirements: 1.1, 1.3, 1.4, 1.5, 1.6, 6.1, 6.2, 6.3, 11.1, 11.2, 11.4, 12.1, 12.2, 12.3, 12.4, 12.5_
  - _Depends: 3.3, 4.1, 4.2, 4.3, 5.1, 6.2, 6.3, 7.2_
  - _Boundary: MenuPlanService_
- [x] 9.2 MenuPlanServiceの実装（日単位再生成、他6日考慮）
  - `regenerateDay(weekStartDate, dayIndex)`を実装する。`MenuPlanRepository.findOtherDays`で残り6日分の料理名・食品IDを取得し、`MenuPromptBuilder.buildDailyPrompt`のコンテキストとして渡す
  - 対象日のみが`MenuPlanRepository.replaceDay`で置き換えられ、他6日の`meal_slots`が変更されないこと、対象週の有効なプランが存在しない場合に`NotFoundError`を返すことをテストで確認する
  - _Requirements: 7.1, 7.2, 7.3, 7.4, 7.5, 11.1, 11.2_
  - _Depends: 9.1_
  - _Boundary: MenuPlanService_
- [x] 9.3 MenuPlanControllerとルーティング登録
  - `server/src/menu-generation/menu-plan.routes.ts` に `POST /api/menu-plans/:weekStartDate/generate`、`POST /api/menu-plans/:weekStartDate/regenerate`、`POST /api/menu-plans/:weekStartDate/days/:dayIndex/regenerate`、`GET /api/menu-plans/:weekStartDate` を実装し、Zodスキーマでリクエストを検証する
  - `GenerationError`の`reason`に応じて409（`profile_missing`/`nutrition_unavailable`/`generation_in_progress`）または502（それ以外）を、`NotFoundError`を404で返すよう`app.ts`にルート登録し、実際にHTTPリクエストを送って各ステータスコードが返ることを確認する
  - _Requirements: 1.1, 6.1, 7.1, 11.3, 12.1, 12.2, 12.3, 12.4, 12.5, 13.1, 13.2_
  - _Depends: 9.2_
  - _Boundary: MenuPlanController_

- [ ] 10. Integration: レシピ詳細生成のオーケストレーション
- [x] 10.1 RecipeDetailServiceの実装
  - `server/src/menu-generation/recipe-detail.service.ts` に `generateForMealSlot(weekStartDate, dayIndex, mealType)` を実装する。対象食事枠が存在しない場合は`NotFoundError`を、存在する場合は`MenuPromptBuilder.buildRecipeDetailPrompt`でプロンプトを構築し`ClaudeMenuClient.generateRecipe`を呼び出す
  - 生成された1〜2件の補助副菜提案それぞれについて`NutritionVerificationService`で栄養増分を算出し、既存の`meal_slots`/`meal_ingredients`の値を変更せずに`RecipeDetailRepository.upsert`で永続化することをテストで確認する
  - _Requirements: 8.1, 8.2, 8.3, 9.1, 9.2, 9.3, 9.4_
  - _Depends: 5.1, 6.2, 6.3, 8.1, 4.1_
  - _Boundary: RecipeDetailService_
- [x] 10.2 MealSlotController（レシピ詳細・フィードバック）とルーティング登録
  - `server/src/menu-generation/meal-slot.routes.ts` に `POST /api/menu-plans/:weekStartDate/days/:dayIndex/meals/:mealType/recipe-detail` と `POST .../feedback` を実装し、Zodスキーマでリクエスト（`{ liked: boolean }`を含む）を検証する
  - `app.ts`にルート登録し、正常系（レシピ詳細200・フィードバック204）と異常系（存在しない食事枠で404、生成失敗で502）が実際のHTTPリクエストで返ることを確認する
  - _Requirements: 8.1, 9.1, 10.1, 10.4, 13.1, 13.2_
  - _Depends: 10.1, 7.2_
  - _Boundary: MealSlotController_

- [ ] 11. Validation: 単体テストの拡充
- [x] 11.1 (P) MenuPromptBuilderの単体テストの拡充
  - 食事制限タイプ×強度の組み合わせ、`restrictionNotes`が食事制限以外の一般的要望（例:「朝食は毎日同じでよい」）を含む場合でも原文のまま渡されること、日単位再生成時に他6日の食材重複回避指示が含まれることを網羅するテストケースを追加する
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 7.2, 7.3_
- [x] 11.2 (P) NutritionVerificationService/UnitConversionServiceの境界値テストの拡充
  - 複数食材・複数単位（g/個/大さじ混在）での合算、食材固有エントリと汎用エントリが両方存在する場合の優先順位、いずれも存在しない単位でのエラー返却を網羅するテストケースを追加する
  - _Requirements: 4.3, 4.4, 4.6, 5.2, 5.3, 5.4_
- [x] 11.3 (P) FeedbackService/ClaudeMenuClientの単体テストの拡充
  - 同一食事枠インスタンスへの複数回フィードバックで最新値のみが残ること、`liked: true`のフィードバックが苦手サマリに含まれないこと、Claudeのtool出力が期待フィールド数（週7日/日4食）を満たさない場合に`schema_validation_failed`となることを網羅するテストケースを追加する
  - _Requirements: 10.3, 10.5, 1.4_

- [ ] 12. Validation: 結合テストとエラーパスの検証
- [x] 12.1 週間献立生成・週単位再生成の結合テスト
  - Claude APIをモック化し、`POST /api/menu-plans/:week/generate`で28食枠すべてが永続化され7日分すべてで`PlannedCalorieGateway.submitPlannedCalories`が呼ばれること、`POST /api/menu-plans/:week/regenerate`で既存プランが置き換わり苦手サマリがプロンプトに反映されることを確認する
  - _Requirements: 1.1, 1.3, 6.1, 6.2, 6.3, 11.1, 11.2_
- [x] 12.2 日単位再生成・レシピ詳細生成の結合テスト
  - `POST /api/menu-plans/:week/days/:dayIndex/regenerate`で対象日のみが置き換わり残り6日が変更されないこと、`POST .../recipe-detail`でレシピ詳細と1〜2件の補助副菜提案が同一レスポンスで返り対象食枠の栄養価が変更されないことを確認する
  - _Requirements: 7.1, 7.4, 8.1, 8.3, 9.1_
  - _Depends: 9.3, 10.2_
- [x] 12.3 エラーパス・フィードバック反映の結合テスト
  - プロフィール未登録時の409（`profile_missing`）、栄養目標値算出不可時の409（`nutrition_unavailable`）、Claude APIモックがスキーマ不一致を返した場合の502（`schema_validation_failed`）で献立が永続化されないこと、フィードバックで「苦手」と記録した料理・食材が次回の週単位再生成のプロンプトコンテキストに含まれることを確認する
  - _Requirements: 12.1, 12.2, 12.3, 12.5, 10.3_
  - _Depends: 9.3, 10.2_

- [ ] 13. Integration: 買い物リスト・外食代替提案（results-dashboardの表示専念方針に伴う追加）
- [x] 13.1 (P) ShoppingListServiceの実装
  - `server/src/menu-generation/shopping-list.service.ts` に `buildForWeek(weekStartDate)` を実装する。`MenuPlanRepository`から対象週の有効なプランを取得し、28食枠の確定済み食材を`UnitConversionService`で正規化したグラム量で同一食品IDごとに合算し、`FoodCompositionRepository`から品目名を解決し、`category-display-groups.data.ts`のマッピングで4カテゴリに分類する
  - `display_unit_code`が設定済みの食品は対応する`unit_conversions`エントリの`grams_per_unit`で除算し0.5刻みで丸めた`displayQuantity`/`displayUnit`を算出し、未設定の食品はグラム量をそのまま`displayQuantity`とし`displayUnit`を`"g"`とする
  - 対象週の有効なプランが存在しない場合に`null`を返すこと、同一食品IDが複数料理で使われる場合に合算されること、未定義カテゴリの食品が「調味料・その他」に分類されること、`display_unit_code`の有無で表示用数量の算出方法が分岐することをテストで確認する
  - _Requirements: 14.1, 14.2, 14.3, 14.4, 14.5, 14.6, 14.7_
  - _Depends: 5.1, 3.1, 3.2_
  - _Boundary: ShoppingListService_
- [x] 13.2 (P) 食品カテゴリ→表示グループの静的マッピングデータ作成
  - `server/src/menu-generation/category-display-groups.data.ts` を作成し、食品成分DBのMEXT分類カテゴリを野菜・きのこ／肉・魚／乳製品・卵・豆／調味料・その他の4グループにマッピングする定義を投入する
  - 食品カタログ（2.1で投入した数百件）の全カテゴリが4グループのいずれかに解決されることを確認する
  - _Requirements: 14.4, 14.5_
  - _Boundary: category-display-groups.data_
- [x] 13.3 MenuPlanControllerへの買い物リストエンドポイント追加
  - `menu-plan.routes.ts`に `GET /api/menu-plans/:weekStartDate/shopping-list` を追加し、`ShoppingListService`に委譲する。対象週の有効なプランが存在しない場合は200 + `null`を返す
  - 実際にHTTPリクエストを送り、買い物リストが正しいレスポンス形式（カテゴリ別グルーピング済み）で返ることを確認する
  - _Requirements: 14.1, 14.6_
  - _Depends: 13.1, 9.3_
  - _Boundary: MenuPlanController_
- [x] 13.4 (P) 外食メニュー参照データの作成
  - `server/src/menu-generation/eating-out-reference.data.ts` を作成し、一般的な外食メニューとその代替案のペア（メニュー名・想定カロリー・代替メニュー名・想定カロリー・たんぱく質増分・主要食材）を、丼もの・定食・麺類・ファストフード等の代表的な外食シーンを横断して数十件規模で投入する
  - 各エントリについて代替案のカロリーが基準メニューのカロリー以下であることを確認する
  - _Requirements: 15.1, 15.2, 15.7_
  - _Boundary: eating-out-reference.data_
- [x] 13.5 EatingOutSuggestionServiceの実装
  - `server/src/menu-generation/eating-out-suggestion.service.ts` に `suggestForMealSlot(weekStartDate, dayIndex, mealType)` を実装する。対象食事枠の検証済みエネルギー量を基準に、`ProfileGateway`から取得したNG食材一覧で参照データを絞り込み、カロリーが基準値以下でそれに最も近い候補を選定する
  - 候補がNG食材除外により0件になった場合に`suggestion: null`を返すこと、対象食事枠が存在しない場合に`NotFoundError`を返すことをテストで確認する
  - _Requirements: 15.1, 15.2, 15.3, 15.4, 15.5, 15.6_
  - _Depends: 13.4, 4.1, 5.1_
  - _Boundary: EatingOutSuggestionService_
- [x] 13.6 MealSlotControllerへの外食代替提案エンドポイント追加
  - `meal-slot.routes.ts` に `GET /api/menu-plans/:weekStartDate/days/:dayIndex/meals/:mealType/eating-out-suggestion` を追加し、`EatingOutSuggestionService`に委譲する
  - 実際にHTTPリクエストを送り、正常系（200 + suggestion）・候補なし（200 + null）・存在しない食事枠（404）の各ケースを確認する
  - _Requirements: 15.6_
  - _Depends: 13.5, 10.2_
  - _Boundary: MealSlotController_

- [ ] 14. Validation: 買い物リスト・外食代替提案のテスト拡充
- [x] 14.1 (P) ShoppingListServiceの単体テストの拡充
  - 複数の単位（g/個/大さじ混在）で同一食材が使われるケースでの合算精度、4カテゴリいずれにも該当しない食品の「調味料・その他」への分類、有効プランが存在しない週での`null`返却、`display_unit_code`設定済み食品での自然単位換算（0.5刻み丸め、0になる場合に0.5を下限とする）と未設定食品でのグラム表示フォールバックを網羅するテストケースを追加する
  - _Requirements: 14.2, 14.3, 14.4, 14.5, 14.6, 14.7_
- [x] 14.2 (P) EatingOutSuggestionServiceの単体テストの拡充
  - 複数候補から最もカロリーが近い代替案が選定されること、NG食材を主要食材とする候補が除外されること、除外の結果候補が0件になるケースで`null`が返ることを網羅するテストケースを追加する
  - _Requirements: 15.2, 15.4, 15.5_
- [x] 14.3 買い物リスト・外食代替提案の結合テスト
  - `GET /api/menu-plans/:weekStartDate/shopping-list` で28食枠分の食材が正しく集約・分類されたレスポンスが返ること、`GET .../eating-out-suggestion` で選定結果が正しいフィールド構成（外食メニュー名・カロリー・代替メニュー名・カロリー・たんぱく質増分）で返ることを実際のHTTPリクエストで確認する
  - _Requirements: 14.1, 14.6, 15.3, 15.6_
  - _Depends: 13.3, 13.6_

- [ ] 15. NO-GO是正: `/kiro-validate-impl menu-generation`の最終統合検証で判明した実行時到達不能・実行時例外の解消
- [x] 15.1 `index.ts`へのmenu-generation本番配線
  - `server/src/index.ts`の`startServer()`に、`MenuPlanService`/`ShoppingListService`/`RecipeDetailService`/`FeedbackService`/`EatingOutSuggestionService`とその依存グラフ（各Repository/Gateway/`ClaudeMenuClient`/`NutritionVerificationService`等、各Serviceのファクトリ関数のシグネチャに従う）を実際に構築し、`registerRoutes(app, {...})`へ渡す配線を追加する
  - 実際にビルドした`dist/index.js`を起動し、`GET /api/menu-plans/:weekStartDate`等のmenu-generationエンドポイントが（Fastify自身の汎用404ではなく）アプリの`NotFoundError`ハンドラ経由で応答することを確認する（起動疎通による実機確認。既存の`/api/profile`等が引き続き200を返すことも合わせて確認する）
  - _Requirements: 1.1〜15.7（menu-generation機能全体のHTTP到達性）_
  - _Depends: 9.3, 10.2, 13.3, 13.6_
- [x] 15.2 `ShoppingListService`のunit_conversions欠落時のグレースフルデグレード
  - `display_unit_code`が設定済みだが対応する食材固有`unit_conversions`エントリが存在しない食品について、`ShoppingListService.buildItem`が例外を投げる現行挙動を、要件14.7が既に`display_unit_code`未設定食品に対して定義しているグラム表示フォールバック（`displayQuantity = totalGrams`, `displayUnit = "g"`）と同一の扱いへ拡張する（`findUnitConversion`が`null`を返した場合も同じフォールバック分岐に合流させる）
  - `food_items`/`unit_conversions`のシードデータ自体は変更しない（task 2.1/2.2の「実データのみ・出典なき換算は捏造しない」方針を維持し、あくまでサービス層の欠落時ハンドリングのみを変更する）
  - 対応する食材固有`unit_conversions`エントリが存在しない実データ（例: `'01034'`）を用いた結合テストで、例外の代わりにグラム表示の`ShoppingListItem`が返ることを確認する
  - _Requirements: 14.7_
  - _Depends: 13.1_

- [ ] 16. Amendment: レシピ詳細・追加副菜提案への食材名解決の追加（`results-dashboard` task 4.2の実装中に判明した要件4.8への対応）
- [x] 16.1 RecipeDetail/SupplementarySuggestionへの食材名解決の追加
  - `shared/src/menu.schema.ts`の`RecipeDetailSchema`に`ingredients: ResolvedIngredient[]`（対象食事枠に既に確定している食材を食材名解決したもの。新規生成ではない）を追加し、`SupplementarySuggestionSchema.ingredients`の型を`IngredientSelection[]`から`ResolvedIngredient`（`IngredientSelection`に`name: string`を加えたもの）の配列へ変更する。`IngredientSelectionSchema`自体（Claudeのtool呼び出し入力スキーマ、`MealSlot.ingredients`）は変更しない
  - `server/src/menu-generation/recipe-detail.service.ts`に`FoodCompositionRepository`への依存を追加し、`generateForMealSlot`が返す`RecipeDetail`について、対象`MealSlot.ingredients`と各補助副菜提案の`ingredients`の両方をfoodIdごとに`FoodCompositionRepository.findById(...).name`で名前解決してから返すようにする。`RecipeDetailRepository`への永続化形式（`recipe_details`/`supplementary_suggestions`/`supplementary_ingredients`テーブル）は変更せず、名前解決は返却の都度算出する（新規マイグレーションなし）
  - 既存の`recipe-detail.service.ts`関連テストを、新しい`ingredients`フィールドおよび食材名付き`supplementarySuggestions`を反映するよう更新する
  - 実際にレシピ詳細生成のテストで、返却される`RecipeDetail.ingredients`と`supplementarySuggestions[].ingredients`の各要素が正しい`name`フィールドを持つこと、`recipe_details`/`supplementary_suggestions`/`supplementary_ingredients`テーブルのスキーマに変更が発生していないことを確認する
  - _Requirements: 4.8_
  - _Depends: 10.1_
  - _Boundary: RecipeDetailService_

- [ ] 17. Amendment: 非AI献立生成ロジックの追加（`NOT_AI`バリアント、ユーザー指示によるAPI課金回避対応。このリポジトリは`AI_API`（Claude API版、無変更）と`NOT_AI`（本amendment適用先）の2独立コピーとして分岐しており、本タスク群は`NOT_AI`側にのみ適用する）
- [x] 17.1 `MenuGenerator`インターフェースの定義とClaudeMenuGeneratorへのリファクタリング
  - `server/src/menu-generation/menu-generator.ts`（新規）に、構造化入力（`profile: MenuProfileSnapshot`, `targets`, `dislikedSummary`等）を受け取り`WeeklyGenerationToolResult`/`DailyGenerationToolResult`/`RecipeGenerationToolResult`を返す`MenuGenerator`インターフェース（`generateWeek`/`generateDay`/`generateRecipe`）を新規定義する
  - 既存の`menu-prompt.builder.ts`によるプロンプト構築+`claudeMenuClient`呼び出しを`ClaudeMenuGenerator`（同ファイルまたは新規`claude-menu-generator.ts`）としてこの新インターフェースの背後にそのまま移動する（挙動は一切変更しない、純粋なリファクタリング）
  - `menu-plan.service.ts`/`recipe-detail.service.ts`の`deps.claudeMenuClient`+個別プロンプト構築呼び出しを`deps.menuGenerator: MenuGenerator`への呼び出しに置き換える
  - 既存の`menu-plan.service.test.ts`/`recipe-detail.service.test.ts`が注入していた`claudeMenuClient`モックを`menuGenerator`モックへ移行し、既存の全アサーション（配線・エラーパス）が無傷で通ることを確認する
  - _Requirements: なし（個別要件IDに紐づかない、非AI代替実装のためのリファクタリング）_
  - _Boundary: menu-generator interface_
- [x] 17.2 (P) 非AI用キュレーション済みレシピDBの作成
  - `server/src/menu-generation/rule-based-recipe.data.ts`（新規）に、実在する食品ID（`010_seed_food_items.sql`の361件）のみを使った完成済みの料理エントリを手作業でキュレーションする。各エントリは`{dishName, mealType, ingredients: {foodId, quantity, unit:"g"}[], tags: string[]（NG食材・食事制限判定用の平易な日本語タグ）, restrictionSuitability: RestrictionType[]（この料理が適合する食事制限タイプ、"none"は常に適合）, servings, cookingTimeMinutes, steps: string[]（簡単な調理手順）}`という形状とする（`eating-out-reference.data.ts`と同じ「配列リテラル+個別ソースコメント」の慣行に倣うが、栄養価はハードコードせず`FoodCompositionRepository`から実行時に算出する前提とする）
  - 各食事タイプ（朝食/昼食/夕食/間食）につき最低10品、計40品以上を目安にキュレーションする
  - 補助副菜提案用に、主菜より軽量な小規模データセット（`server/src/menu-generation/rule-based-side-dish.data.ts`、各食事タイプ5品程度）も同様の形状で作成する
  - _Requirements: なし_
  - _Boundary: rule-based recipe data_
- [x] 17.3 `RuleBasedMenuGenerator`の実装（generateWeek/generateDay）
  - `server/src/menu-generation/rule-based-menu.generator.ts`（新規）に、task 17.1の`MenuGenerator`インターフェースを満たす非AI実装を作成する。選定ロジック: 対象`mealType`が一致し、`profile.ngIngredients`のいずれのタグも含まず、`dislikedSummary`の`dishName`と一致しない候補から、`profile.restrictionType`に適合する（またはtagsが"none"許容の）ものを優先しつつ、週内で既に選んだ料理をできるだけ避けて（`generateWeek`は週内、`generateDay`は`otherDays`引数を考慮）1件選ぶ
  - 選んだ料理の全食材の`quantity`を、対象日の`target.calorieTarget`に近づくよう一律スケーリングする（`FoodCompositionRepository`から算出した素の合計kcalとの比率でスケール。極端な値にならないよう妥当な範囲にクランプする）
  - 決定論的なテスト容易性のため、候補選択の乱数要素はシード可能な擬似乱数関数を注入可能にする（テストで固定シードを使い再現可能にする）
  - `WeeklyGenerationToolResult`/`DailyGenerationToolResult`の型契約（dayIndex 0-6網羅・重複なし、各日4mealType網羅・重複なし）を満たすことをテストで確認する
  - _Requirements: なし_
  - _Depends: 17.1, 17.2_
  - _Boundary: RuleBasedMenuGenerator_
- [x] 17.4 (P) `RuleBasedMenuGenerator`のレシピ詳細生成（generateRecipe）実装
  - task 17.3と同じ`rule-based-menu.generator.ts`（または分離した新ファイル）に`generateRecipe`を実装する。対象の`dishName`/`ingredients`（既に確定済み）に対応するキュレーション済みエントリ（task 17.2）から`servings`/`cookingTimeMinutes`/`steps`をそのまま返し、`supplementarySuggestions`はtask 17.2の副菜データセットから、対象食事タイプ・NG食材を考慮して1〜2件選ぶ
  - 対象`dishName`がキュレーション済みデータに見つからない場合（他日再生成等で発生しうる整合性エッジケース）のフォールバック方針を決定し実装する
  - _Requirements: なし_
  - _Depends: 17.2, 17.3_
  - _Boundary: RuleBasedMenuGenerator_
- [x] 17.5 配線切り替え: composition rootでの`RuleBasedMenuGenerator`採用とAPIキー要件の解消
  - `server/src/index.ts`（または実際に`createMenuPlanService`/`createRecipeDetailService`を構築している箇所）で、`NOT_AI`側では`ClaudeMenuGenerator`ではなく`RuleBasedMenuGenerator`をデフォルトで注入するよう変更する
  - `checkAnthropicApiKeyConfigured`（`ANTHROPIC_API_KEY`未設定時の起動時警告）を、`NOT_AI`側では非表示または「非AIモードのため不要」という趣旨のメッセージへ変更する（`@anthropic-ai/sdk`自体の依存関係は残してよいが、実行時に呼び出されないことを確認する）
  - 観測可能な完了条件: `ANTHROPIC_API_KEY`を一切設定しない状態でサーバーを起動し、献立生成ボタンを押しても502エラーにならず実際に献立が生成されることを確認する
  - _Requirements: なし_
  - _Depends: 17.3, 17.4_
  - _Boundary: app bootstrap_
- [x] 17.6 検証: 非AI献立生成の結合テストとE2E確認
  - 実際のfood_items/unit_conversions DBに対して`RuleBasedMenuGenerator`を通しで実行し、生成された`WeekMenuPlan`が実際に検証を通り永続化できることを確認する結合テストを追加する
  - プロフィールのNG食材・食事制限タイプを変えたケースで、生成結果がそれぞれ正しく反映されることを確認する
  - `npm run dev`でサーバーを起動し、実際に献立生成ボタンから週間献立が生成され、レシピ詳細・買い物リストが正常に表示されることを目視確認する
  - _Requirements: なし_
  - _Depends: 17.5_
  - _Boundary: Integration_

- [ ] 17.7 `RuleBasedMenuGenerator`への好み食材(preferredIngredients)の反映（ユーザー指示による事後追加）
  - `generateWeek`において、`profile.preferredIngredients`が空でない場合、週内28食枠のうち**最低2食枠**で、`tags`が`preferredIngredients`のいずれかと一致する候補が（NG食材・食事制限の既存フィルタを満たした上で）選ばれるようにする
  - 実現方法: 全28食枠それぞれについて、NG食材・食事制限フィルタ通過後の候補群の中に`preferredIngredients`タグ一致候補が存在するかを事前判定し、該当する食枠（存在する場合）の中から最大2枠を選び、その2枠のみ候補群を好み食材一致のもの限定に絞ってから通常の（バラエティ→乱数）選定を行う。他の26食枠は既存ロジックのまま変更しない
  - 好み食材一致候補が存在する食枠が2枠未満の場合（キュレーション済みデータ上限）はベストエフォートとし、無理に2枠を満たそうとしない（エラーにしない）
  - `generateDay`（単一日再生成）では週単位の「最低2回」保証はできないため、同じタグ一致ロジックを**ソフトな優先条件**として適用する（該当候補があれば優先、なければ通常通り）に留める
  - 決定論的テストのため、既存の乱数注入の仕組みをそのまま再利用する
  - _Requirements: なし（ユーザー指示による機能追加）_
  - _Depends: 17.3_
  - _Boundary: RuleBasedMenuGenerator_

## Implementation Notes
- (1.1) Unlike `user-profile`/`nutrition-engine`, this spec's tasks.md has NO `_Boundary:_`/`_Depends:_` annotations on tasks 1.1-1.5 and 2.1-2.2 (the "Foundation" groups — shared infrastructure with no single named design.md component to map to). Annotations DO exist starting from task 3.1 onward (component-mapped tasks). For the annotation-less Foundation tasks, the parent controller determines scope itself from the task's literal file-creation instructions and existing repo conventions — this is expected and not a spec defect; future Foundation-group tasks (1.2-1.5, 2.1-2.2) will need the same treatment. Created `005_create_food_items.sql`/`006_create_unit_conversions.sql` matching design.md's Physical Data Model exactly (independently byte-for-byte verified by review, including the `source_citation` DEFAULT citation string). Extended the EXISTING shared `server/src/db/migrate.test.ts` (its `EXPECTED_TABLES` array plus a few focused new constraint tests) rather than creating a new per-migration test file — this repo's established convention (from `user-profile`) is ONE shared migration test file covering all tables, not one file per migration; `migrate.ts` itself needs zero code changes since it's a generic, auto-discovering runner. **Genuine non-obvious SQL semantic surfaced and caught by review**: `unit_conversions`' `UNIQUE(food_id, unit_code)` constraint provides ZERO deduplication protection for generic (`food_id IS NULL`) entries — standard SQL never treats two NULLs as equal, even within a composite UNIQUE constraint, so two NULL-food_id rows can share the same `unit_code` without violating the constraint. This is correct, intended behavior per design.md's literal schema (not a bug), but the round-1 test claiming to prove this only tested a trivial contrasting case (different `unit_code`s, which trivially wouldn't collide regardless of NULL semantics) — round-1 review independently reproduced the actual behavior via a standalone script and required a genuine same-`unit_code` test; round-2 review independently reproduced it AGAIN before approving. **Lesson**: when a test's name/claim involves a NULL-related uniqueness or equality semantic, verify the test actually constructs the case where NULL-vs-NULL comparison would matter (same values on every OTHER composite-key column) — a "different values elsewhere" variant proves nothing about NULL handling specifically, no matter how the test is named. This exact behavior is directly relevant to task 2.2 (seeding generic/NULL-food_id unit entries) and task 3.2 (`UnitConversionService` querying generic entries by `unit_code`) — future seed data for generic units must avoid accidentally inserting duplicate `unit_code` rows itself, since the DB will not catch it.
- (1.2) `007_create_menu_plan_tables.sql` bundles all 4 tables (`week_menu_plans`/`day_menus`/`meal_slots`/`meal_ingredients`) into ONE file, per the task text's own file-naming instruction (unlike task 1.1, which split into 2 files) — matches design.md's Physical Data Model exactly, independently re-verified column-by-column by review. `day_menus` deliberately has TWO separate UNIQUE constraints (`(week_start_date, day_index)` composite AND a standalone `day_date UNIQUE`) — both genuinely independently enforced and tested (a row colliding on one but not the other still fails), since `day_date` is the natural calendar-date identity while `(week_start_date, day_index)` is the aggregate-position identity; both must hold for the `WeekMenuPlan`→`DayMenu[7]` aggregate invariant to be sound. **Deliberate, scrutinized design choice**: `meal_ingredients.food_id REFERENCES food_items(food_id)` has NO `ON DELETE CASCADE` (every other FK in this migration does) — confirmed by review as intentional, not a spec gap, based on 3 independent signals: (a) design.md applies the identical no-cascade pattern to the structurally analogous `supplementary_ingredients.food_id` (a later table, task 1.3), consistently across two separate tables — inconsistent if it were a copy-paste mistake; (b) contrast with `unit_conversions.food_id` (task 1.1), which DOES cascade — `unit_conversions` is pure secondary lookup data meaningless without its parent food, whereas `meal_ingredients` rows are historical generated-menu records with independent lifecycle meaning; (c) design.md's own Revalidation Triggers section anticipates `food_items` being revised when the MEXT data source version updates — silently cascade-deleting historical meal records on a reference-data refresh would be a genuine data-loss bug. **Lesson**: when one FK in a migration deviates from an otherwise-consistent CASCADE pattern, don't assume it's an oversight — check whether design.md applies the SAME deviation elsewhere for a structurally similar relationship (a strong signal of intent) before deciding whether to escalate or just implement as literally specified; here, cross-referencing a table 2 tasks away (`supplementary_ingredients`, not yet built) was the key evidence. The 3-level CASCADE chain (`week_menu_plans`→`day_menus`→`meal_slots`→`meal_ingredients`) and the non-cascading `food_items` FK were both tested with a fully realistic, FK-satisfying fixture chain (a real `food_items` row inserted first) and verified via row-count assertions at each level, plus a mutation test (temporarily adding the CASCADE clause) confirming the non-cascade test is genuinely load-bearing.
- (1.3) `008_create_recipe_detail_tables.sql` (recipe_details/supplementary_suggestions/supplementary_ingredients) and `009_create_satisfaction_feedback.sql` (satisfaction_feedback) match design.md's Physical Data Model exactly (independently re-verified column-by-column by review). `supplementary_suggestions`' 4 delta columns (`energy_kcal_delta`/`protein_g_delta`/`fat_g_delta`/`carb_g_delta`) deliberately have NO `>=0` CHECK, unlike almost every other numeric column in this schema — they represent signed deltas (a supplementary dish's nutrition impact relative to nothing, can legitimately be negative or positive), confirmed correct against design.md's own unconstrained spec for these columns. `supplementary_ingredients.food_id` reapplies task 1.2's already-confirmed non-cascading FK pattern (historical record → reference data, restrict not cascade) — now established as consistent across BOTH ingredient-junction tables in the schema. **`satisfaction_feedback` deliberately has ZERO foreign keys anywhere** (not even to `week_menu_plans`/`day_menus`/`meal_slots` via its `week_start_date`/`day_index`/`meal_type` columns, which superficially resemble FK'd columns elsewhere) — proven not just by schema inspection but BEHAVIORALLY: a test inserts a feedback row for a `(week_start_date, day_index, meal_type)` triple with NO corresponding `day_menus`/`meal_slots` row anywhere, and confirms the insert succeeds. This is intentional per Requirement 10.2 (feedback is an independent historical snapshot, storing its own `dish_name`/`primary_food_ids` copy) — an FK with CASCADE here would destroy feedback history every time a day/week gets regenerated, defeating Requirement 10.3's entire purpose (using recent negative feedback to influence FUTURE generation prompts). **Two judgment calls, both independently verified by review, not just accepted on the implementer's word**: (1) added `CHECK (liked IN (0,1))` even though design.md's wording for this one column is a softer parenthetical "(0 or 1)" rather than the explicit "CHECK IN (...)" phrasing used elsewhere in the same table — review cross-checked every mention of `liked`/満足度フィードバック across design.md and requirements.md and confirmed the feedback concept is consistently binary (好き/苦手) throughout with no hint of a third state anywhere, concluding the added CHECK is a safe strengthening, not a premature/risky assumption. (2) Did NOT add an explicit `CREATE INDEX` on `recipe_details.meal_slot_id` (breaking the established "always index FK columns" precedent from tasks 1.1/1.2) since it's UNIQUE on that single column alone — review independently ran a real SQLite experiment (`PRAGMA index_list`/`EXPLAIN QUERY PLAN`) and confirmed SQLite auto-creates an equivalent index for any single-column UNIQUE constraint, making an explicit index a byte-for-byte redundant duplicate; this is the FIRST single-column-UNIQUE FK in this schema, so the divergence from the blanket indexing precedent is justified by a genuinely new case, not an inconsistency. **Lesson**: when a design.md constraint's wording is less explicit/formal than its siblings in the same table (e.g. a parenthetical note vs. an explicit CHECK-IN clause), don't treat that as license to skip the constraint OR to silently add a stricter one — cross-reference the ENTIRE spec (design.md + requirements.md) for any hint the softer wording was deliberate (e.g. a hidden third state), and only add the stricter constraint once that search comes up empty.
- (1.4) `shared/src/menu.schema.ts` (new file, registered via a single added line in `shared/src/index.ts`) defines all 10 types design.md's Service Interface specifies (`MealType`, `IngredientSelection`, `NutritionValues`, `VerifiedNutritionValues`, `MealSlot`, `DayMenu`, `WeekMenuPlan`, `SupplementarySuggestion`, `RecipeDetail`, `FeedbackInput`), independently verified field-for-field against design.md with zero discrepancies. **Two consequential judgment calls, both independently traced through design.md's actual generation flow by review before approval** (not accepted on the implementer's word alone, since this is a foundational type ~35 future tasks will build on): (1) `NutritionValuesSchema` is deliberately LEFT UNCONSTRAINED (no `.nonnegative()`) since it's reused for BOTH `RecipeDetail.nutrition` (an absolute value) AND `SupplementarySuggestion.nutritionDelta` (a genuine signed delta) — directly reapplying `nutrition-engine`'s `PfcTargetsSchema` precedent (task 1.1 of that spec) for the identical "shared shape spans an absolute-value context and a delta context" reasoning. `VerifiedNutritionValuesSchema` (used for `MealSlot.nutrition`/`DayMenu.dayNutrition`, always DB-verified absolute values, never deltas) is instead built as a FULLY INDEPENDENT `z.object()` (not `.extend()`-ing the unconstrained base, which would have silently inherited its permissiveness) with all 13 fields `.nonnegative()`. (2) `DayMenu.meals`/`WeekMenuPlan.days`/`RecipeDetail.supplementarySuggestions` all enforce EXACT cardinality (`.length(4)`/`.length(7)`/`.min(1).max(2)`) at the Zod-schema level, matching design.md's "4件"/"7件"/"1〜2件" comments — review traced design.md's actual generation flow (`MenuPlanRepository.replaceWeek`/`replaceDay`) and confirmed `WeekMenuPlan`/`DayMenu` are ONLY EVER constructed as fully-assembled, post-verification, post-save objects; Claude's raw pre-verification tool output uses entirely SEPARATE types (`WeeklyGenerationToolResult`/etc., a later ClaudeMenuClient task's concern) with their own independently-enforced cardinality — so no future task can ever need to `.parse()` a partial/in-progress `WeekMenuPlan` against this fixed-length schema; the enforcement is a safe, valuable assembly-bug catcher, not a landmine. **A third, smaller judgment call**: `RecipeDetail.mealSlotId` deliberately uses PLAIN unconstrained `z.number()` (no `.int()`/`.positive()`) even though it references an AUTOINCREMENT PK — matches the actual established precedent (`daily-log.schema.ts`'s `ExerciseLogEntry.id`, also plain `z.number()`) and design.md's own `recipe_details` table, which (unlike `servings`/`cooking_time_minutes`) has no ">0" annotation on this column; review additionally confirmed `mealSlotId` never crosses the HTTP request boundary (always resolved server-side from a trusted DB row, never client-supplied), so the laxer choice carries no realistic risk. `IsoDate` (the plain type alias from `daily-log.schema.ts`) is genuinely reused, not redefined — via a local `IsoDateSchema: z.ZodType<IsoDate> = z.string()` helper, deliberately matching `daily-log.schema.ts`'s own plain (non-regex) date-string validation rather than `nutrition.schema.ts`'s stricter private (and structurally inaccessible) `IsoDateStringSchema`. The task's literal "import from server, confirm build" completion condition was satisfied via the full monorepo build succeeding (root `npm run build`, which depends on `shared` via TS project references) rather than a literal new import inside `server/src/`, since the task's own boundary explicitly forbids touching `server/src/` — this is the correct reading, not a shortcut. **Lesson**: for a foundational shared-schema task whose types will constrain many future tasks, tracing how the DOWNSTREAM consumers (later tasks' sequence diagrams / repository interfaces) will actually construct and pass around the type is the right way to validate a strictness judgment call (fixed-length arrays, numeric constraints) — don't just check whether design.md's wording technically permits the stricter reading; check whether any future task could plausibly need the LAXER reading and be blocked by it.
- (1.5) FIRST task creating `server/src/menu-generation/` (mirroring `server/src/nutrition/`/`server/src/profile/`). Added `@anthropic-ai/sdk@^0.122.0` as a real dependency (via genuine `npm install`, independently verified by review against the npm registry's own integrity hash — not hand-edited into `package.json`). `constants.ts` defines `DEFAULT_MODEL_ID="claude-sonnet-5"`, `WEEKLY_DAILY_GENERATION_THINKING={type:"adaptive"}`/`WEEKLY_DAILY_GENERATION_EFFORT="medium"`, `RECIPE_GENERATION_THINKING={type:"disabled"}`/`RECIPE_GENERATION_EFFORT="low"` — all independently verified against BOTH design.md's literal text AND the actual current Anthropic API (via the `claude-api` skill, consulted specifically because this task involves Claude API configuration): `claude-sonnet-5` is a real current model ID, `thinking` is correctly modeled as an OBJECT (`{type:...}`, not a bare string — the task text's own shorthand `（"adaptive"/"medium"）` could be misread as two parallel strings, but only `thinking` needs the object shape while `effort` is a plain string), Sonnet 5 uniquely accepts `thinking:{type:"disabled"}` at any effort level (unlike some other current models), and `budget_tokens` (removed/400-rejected on Sonnet 5) correctly does not appear anywhere. **Deliberately did NOT construct an `Anthropic` SDK client or import any of its runtime code** — this task only adds the dependency and constants; actual API calls are `ClaudeMenuClient`'s job (task 6.1/6.2). Added a startup-time, non-fatal warning (`checkAnthropicApiKeyConfigured`, mirroring the file's existing `resolvePort`/`resolveDbPath` pattern exactly — same `env` parameter injection for testability) that `console.warn`s if `ANTHROPIC_API_KEY` is missing but NEVER throws — the server must keep serving `/api/profile`/`/api/daily-logs`/`/api/nutrition/*` normally even with no Claude key configured, since menu-generation's own HTTP routes aren't wired up until much later (tasks 9.3/10.2); mutation-tested by review (temporarily making it throw broke 5 tests, proving the never-throws behavior is genuinely exercised). **Flagged, unresolved cross-task judgment call for task 2.2's future implementer**: `KNOWN_UNIT_CODES` (`["g","個","大さじ","小さじ","ml","cc","枚","本","缶","袋","束","パック"]`) is an explicitly PROVISIONAL list — neither design.md nor research.md enumerates an exhaustive/canonical unit-code set (only Requirement 5.1's minimum: g/個/大さじ/小さじ). **Task 2.2 ("分量単位正規化テーブルへのデータ投入") MUST seed `unit_conversions` DB rows (food-specific and/or generic entries) for EXACTLY this same set of unit codes** — a mismatch would let the strict tool-use enum (task 6.1, built from this same constant) accept a unit that `UnitConversionService` can't actually resolve at verification time, causing a latent Requirement 5.4 failure invisible until real generation is attempted. When task 2.2 is picked up, its implementer MUST read this constant first and either seed exactly this set or, if a different/refined set is judged better, update BOTH `constants.ts` and its own seed data together in the same task, not silently diverge. **Lesson**: when a task's completion depends on data a LATER, not-yet-started task will provide (a list, a schema, a reference value), don't just pick something reasonable and move on — write an explicit, impossible-to-miss comment in the source itself (not just the task's own report) naming the future task by number and the exact failure mode if they diverge, since the two tasks may be implemented in different context windows with no shared memory otherwise.
- (2.2) `011_seed_unit_conversions.sql` seeds 221 rows (5 generic volume-unit conversions + 216 food-specific, 132 distinct foods) into `unit_conversions`, sourced from real government/industry references (a 松本市 municipal PDF and a 香川栄養学園 PDF both citing 女子栄養大学出版部『調理のためのベーシックデータ第5版』) — no value fabricated. **The single most important technical insight**: `grams_per_unit` MUST be seeded on a 可食部（正味, edible-portion）basis, NOT gross/as-purchased weight, because `food_items`' nutrition values (task 2.1) are per-100g-可食部 and `toGrams()`'s output feeds directly into that multiplication — seeding gross weights would silently introduce ~15-40% nutrition errors (independently confirmed by review against MEXT's own 廃棄率 column for banana/cabbage/egg/broccoli/taro). Deliberately seeded NO generic entry for count-based units (個/本/枚/玉/丁/束/パック/缶) since no defensible universal "1個" exists (Requirement 5.2's own example is 卵=50g vs 玉ねぎ=200g) — unresolved count-unit pairs correctly fail loudly via `unit_not_found` (5.4), never silently guessed. Closed a real gap task 2.1's review discovered: added `"玉"`/`"丁"` to task 1.5's `KNOWN_UNIT_CODES` (a narrow, explicitly-flagged edit — nothing else in that file changed).
  - **Round-1 rejection, 3 "Important" findings, all legitimate and all fixed in round 2**: (1) the migration's OWN documentation falsely claimed every unseeded pair fails loudly via `unit_not_found` — actually only true for count-based units; the 5 VOLUME-based units (大さじ/小さじ/カップ/ml/cc) DO have generic entries, so an unseeded food-specific override for those SILENTLY resolves to the generic water-basis value instead — a real, undisclosed, untested failure mode affecting 17 of 56 seasoning/oil/sugar foods (errors from -17% to +150%), invisible to every existing test since they all keyed on `display_unit_code` (which no seasoning has). Fixed by correcting the documentation to state the asymmetry explicitly AND adding a test that pins the exact 17-food_id set falling back silently, freezing it against silent growth. (2) グラニュー糖 (03005) was missing despite its real value (小さじ4g/大さじ12g) sitting on the very next line of an ALREADY-cited source, adjacent to 上白糖's row — a pure oversight, not a fabrication, fixed by adding the real row. (3) the 缶 (can) gap was justified with a "固形量/drained-weight generally not published" rationale that is genuinely true ONLY for fish water-pack cans (a real, confirmed Japanese labeling-law exemption) but FALSE for 農産物缶詰 (canned agricultural products — soybeans/corn/tomato), where 食品表示基準 別表第四 legally MANDATES 固形量+内容総量 on the label — fixed by seeding real label-sourced 固形量 values for 大豆水煮缶/スイートコーン缶/トマト缶 (the last one a well-justified, independently-verified implementer-initiated extension beyond what was literally asked, since task 2.1 had already assigned that food a `display_unit_code='缶'`, making its conversion an existing obligation, not scope creep). さば水煮缶/うずら卵水煮缶 remain genuinely unresolvable (confirmed) and correctly resolve to null.
  - **Non-blocking findings carried forward for later tasks (3.2 UnitConversionService, 11.2 its test expansion)**: (a) the silent-fallback pinning test is scoped to `category='調味料類'` foods only, but review's DB audit found 6 MORE foods (09044 カットわかめ, 09050 ほしひじき, 13003 普通牛乳, 13013 加糖練乳, 13014 生クリーム, 13025 ヨーグルト) that have a 大さじ-only override with no 小さじ override, silently falling back to generic 5g for 小さじ — mostly negligible for near-water-density dairy, but a genuine ~21% error for 加糖練乳 (sweetened condensed milk, density ≈1.28); (b) seeding cans on a 固形量 (drained-solid) basis is CORRECT and consistent with MEXT's own analysis basis, but a home cook using a whole can INCLUDING its liquid (e.g. tomato juice in a stew) will have that liquid's nutrition contribution silently uncounted — an inherent, disclosed modeling limitation inherited from the composition table itself, not a data error, worth a documentation note but not a fix.
  - **Lesson**: real reference-data seeding tasks at this scale benefit enormously from the implementer proactively dispatching ITS OWN parallel research subagents for independent categories of data (spoon/cup conversions, count-unit weights by food category, can drained-weights) — but the implementer must not treat a dispatched subagent's absence-of-response (e.g. due to a rate limit) as license to silently drop that research thread; it must either resume/redo that specific piece of work or explicitly document the resulting gap, never leave it as an invisible hole. Both the implementer and the reviewer in this task demonstrated the same core discipline nutrition-engine and earlier menu-generation tasks established: NEVER fabricate a number, ALWAYS cite a verifiable primary source, and explicitly document (not silently drop) anything that cannot be verified.
- (3.1) FIRST task creating a source file directly under `server/src/menu-generation/` (besides task 1.5's `constants.ts`). `food-composition.repository.ts` implements `createFoodCompositionRepository(db)` matching the established repository/gateway DI convention exactly, with `FoodItemNutrition`/`UnitConversionEntry` as LOCAL TypeScript interfaces (not shared Zod schemas, per design.md's Service-only Contracts marker — mirrors `nutrition-engine`'s `BmrInput`/`ProfileSnapshot` precedent), reusing `NutritionValues` from `shared/src/menu.schema.ts` (task 1.4) for the 4 base per100g fields. **Critical, easy-to-miss SQL correctness point, explicitly verified by review**: `findUnitConversion(foodId, unitCode)` uses plain equality (`WHERE food_id = ? AND unit_code = ?`), while `findGenericUnitConversion(unitCode)` uses genuine `IS NULL` syntax (`WHERE food_id IS NULL AND unit_code = ?`) — NOT a parameterized `food_id = ?` call with a JS `null` argument, which would silently match NOTHING due to SQL's three-valued NULL logic (`NULL = NULL` is never true in SQL). Review mutation-tested this exact bug class by deliberately introducing it and confirming a test caught it. The two lookup methods are deliberately narrow with zero fallback/OR logic between them — resolving generic-vs-food-specific PRIORITY is explicitly deferred to task 3.2's `UnitConversionService`, not this Repository. All `null`-vs-`undefined` handling (better-sqlite3's `.get()` returns `undefined` on no match, explicitly converted to `null` here) and micronutrient-NULL passthrough (no accidental `?? 0` coercion) were independently verified against real seeded rows (卵/12004 fully populated exact-match, ベーグル/01148 for its genuine 3-field NULL pattern). Zero findings from review — the cleanest single-round approval in this spec so far.
- (3.2) `unit-conversion.service.ts` implements `createUnitConversionService(repository: FoodCompositionRepository)` with `toGrams(foodId, quantity, unitCode)`, matching design.md's 4-branch algorithm exactly: `"g"` short-circuits and returns immediately (verified by review to never call the repository, via a fake whose lookup methods `throw` if invoked — a genuinely strong test design, not a silently-passing fake), food-specific `findUnitConversion` is checked BEFORE generic `findGenericUnitConversion` with real early-return short-circuiting (proven via a test using CONFLICTING food-specific/generic values, not matching ones, since only a genuine conflict can prove which one actually won), generic fallback when no food-specific entry exists, and `VerificationError(type:"unit_not_found", foodId, unit)` when neither exists. `VerificationError`/`VerificationErrorType` (shared with the not-yet-built `NutritionVerificationService`, task 3.3) are defined and exported HERE, in this file — task 3.3 will import them from here rather than a separate shared-types file, since design.md's Contracts marker treats this as a Service-only (non-HTTP) type. `Result<T,E>` is genuinely reused from `server/src/shared/result.ts` (established since `user-profile`), not redefined. Deliberately does NOT defensively re-validate `quantity > 0` inside `toGrams` — design.md documents this as a caller-side PRECONDITION, not a postcondition this Service must enforce, matching the established "don't re-validate documented preconditions" convention from `nutrition-engine`'s `BmrCalculator`; review independently re-read design.md's Preconditions/Postconditions/Invariants text and agreed, noting (FYI) that unlike `BmrCalculator`, design.md doesn't yet name a specific upstream enforcer for this precondition — likely task 6.1's tool-schema `quantity>0` JSON Schema constraint will end up being it. Zero findings, approved in a single round — review independently mutation-tested both the priority order and the "g" short-circuit and confirmed the test suite catches breaks in each.
- (3.3) `nutrition-verification.service.ts` implements `createNutritionVerificationService(repository, unitConversionService)` orchestrating tasks 3.1+3.2. `verifyDish` checks `FoodCompositionRepository.findById(foodId)` BEFORE calling `UnitConversionService.toGrams` for each ingredient — a deliberate, reviewer-confirmed-correct ordering choice: `toGrams`'s `"g"`-unit fast-path (task 3.2) never consults the repository at all, so checking unit-resolution first would let a nonexistent food_id combined with a `"g"` unit silently "succeed," masking Requirement 4.6's food-id-existence check behind an unrelated short-circuit. Checking existence first makes 4.6 independent of `UnitConversionService`'s internals — verified by a dedicated test AND a mutation test (swapping the order broke exactly this test). `VerificationError`/`VerificationErrorType` are re-exported from `unit-conversion.service.ts` (task 3.2, the type's original home) rather than redefined, establishing the pattern that this shared error type flows from wherever it was FIRST introduced. Null micronutrients (`per100g.<field> ?? 0`) are treated as zero contribution per design.md's explicit "underestimate rather than fail" design (already documented in design.md's own Revalidation Triggers section from the design phase — no design.md edit was needed for this task). ANY ingredient failure aborts the WHOLE `verifyDish` call with no partial-sum leakage (verified via a `hasOwnProperty(result, "value")` check, not just an `ok===false` check). `verifyDay` deliberately has NO hardcoded length-4 assumption despite design.md's "4食枠" prose comment — the actual function signature just takes an array, and a future caller (`MenuPlanService`, not yet built) owns the 4-meal invariant if one exists; tested with 4/2/1-element arrays to prove this. `computeVarianceKcal` uses `actual - target` (positive=surplus) — design.md doesn't specify the sign, but review cross-referenced `results-dashboard`'s own spec language ("超過が最大の曜日", diverging bar-chart framing) and confirmed this matches the SAME sign convention used elsewhere in the spec family, not an arbitrary guess. Review independently re-derived all 13 nutrition fields of the multi-ingredient sum test from scratch and found zero discrepancies. Zero findings, approved in a single round.
- (4.1) FIRST task in the "Cross-Spec Gateways" group. `profile.gateway.ts` implements `createProfileGateway(profileService: ProfileService)` — a thin, pure field-by-field projection of `user-profile`'s real `Profile` into `MenuProfileSnapshot` (8 fields: ngIngredients/preferredIngredients/restrictionType/restrictionIntensity/restrictionNotes/cookingSkill/cookingTimePreference/budgetPreference), with ZERO renaming/transformation and no caching (fresh `profileService.getProfile()` call every invocation) — this is the EXACT same pattern `nutrition-engine`'s own `profile.gateway.ts` already established for the identical kind of upstream projection, reused here nearly verbatim. `RestrictionType`/`RestrictionIntensity` are reused from `@nutrition/shared` (already defined by `user-profile`), not redefined. `MenuProfileSnapshot` is defined and exported HERE (the first task needing it) — a later task (`MenuPromptBuilder`, 6.3) will import it from this file rather than redefining it, following the same "type lives where it's first produced" pattern established by `VerificationError` in tasks 3.2/3.3. Tests used a fixture with 8 MUTUALLY DISTINGUISHABLE field values (not generic/interchangeable placeholders) specifically so a field-swap bug would be genuinely catchable — confirmed by review's mutation test (swapping `ngIngredients`/`preferredIngredients` broke exactly 2 tests). Zero findings, approved in a single round.
- (4.2) `nutrition.gateway.ts` implements `createNutritionGateway(nutritionService)`, projecting `nutrition-engine`'s `NutritionSummary` (`dietMode ?? normalMode` priority) into a local `NutritionTargetSnapshot`, propagating `CalculationUnavailableError` by REFERENCE (`return summaryResult` unchanged, never reconstructed) on failure. **Critical type-reuse correction, caught before implementation began**: design.md's OWN local Service Interface excerpt for this component shows an outdated, narrower 3-value `CalculationUnavailableReason` redefinition (missing `diet_mode_disabled`) — this was documentation drift from before `nutrition-engine`'s real 4-value shared type was finalized (task 1.1 of that spec), NOT an instruction to duplicate the type; the implementer correctly imported and reused the REAL type from `@nutrition/shared` instead, verified by review with 2 distinct reason values including `diet_mode_disabled` (which a narrower local type would have rejected). By contrast, `PfcTargets` genuinely IS different: `@nutrition/shared`'s real `PfcTargets` has 6 fields (proteinG/fatG/carbG/proteinKcal/fatKcal/carbKcal) but this spec's `NutritionTargetSnapshot.pfc` deliberately needs only 3 — a real, narrower, menu-generation-OWNED projection type, correctly kept LOCAL (not imported from shared) and renamed `MenuPfcTargets` (design.md literally reuses the name `PfcTargets` for this local type too, which review judged a reasonable, well-documented collision-avoidance choice, not a defect, since a later task's test file already needed to import both the real and local shapes simultaneously — exactly the collision scenario the rename was meant to avoid). `activityLevelLabel` is correctly sourced from `NutritionSummary`'s TOP LEVEL (not nested inside `normalMode`/`dietMode`) and `guardrailWarningTypes` correctly reads specifically from `dietMode?.guardrails` (never from `mode`, which would lack `.guardrails` entirely when `normalMode` is selected) — review mutation-tested both by reading from the wrong location and confirmed TypeScript's own compiler catches the `mode.activityLevelLabel` mistake (TS2339, property doesn't exist on the union) in addition to a runtime test failure, a double-layer safety net. **Lesson reinforced from tasks 3.2/3.3**: when a design.md excerpt for a cross-spec Gateway locally redefines a type that a PRIOR, already-completed spec already defines as a real shared type, always check whether the design.md excerpt is just stale documentation (reuse the real type) versus a deliberate, narrower, spec-owned projection (keep it local) — the two situations look superficially similar (both are "design.md shows a local interface") but require opposite handling, and getting this wrong either duplicates a type unnecessarily or silently narrows a type's real value space.
- (4.3) FINAL task in the "Cross-Spec Gateways" group. `planned-calorie.gateway.ts` implements `createPlannedCalorieGateway(dailyLogService)`, wrapping `user-profile`'s already-Result-returning `DailyLogService.setPlannedCalories(date, plannedKcal): Result<DailyLogEntry, ValidationError>` in a `submitPlannedCalories(date, plannedKcal): Result<void, PlannedCalorieSubmissionError>`. Two distinct failure modes are both translated into the SAME `PlannedCalorieSubmissionError` shape: (1) the DOCUMENTED `ValidationError` Result-failure path (translated via a field-error-summarizing message), and (2) a DEFENSIVE `try`/`catch` around the entire call, catching even genuinely UNEXPECTED thrown exceptions (handling non-`Error` thrown values gracefully via `instanceof Error ? .message : String(caught)`, without itself throwing while processing the caught value) — satisfying the task's explicit "例外を投げず" (never throws) requirement beyond just the already-Result-wrapped case. The success path correctly DISCARDS the returned `DailyLogEntry` (this Gateway's success type is `void`, not the entry) — review mutation-tested this specifically (leaking the entry was caught by a clean assertion failure) and mutation-tested the try/catch itself (removing it caused the thrown-exception test to fail via an actual UNCAUGHT error surfacing in the test runner, not a clean assertion failure — the strongest possible proof that specific test genuinely exercises the catch path). Message-construction for both failure paths includes the operation name, exact input values, and either the field-level validation messages or the caught exception's own message — judged by review as genuinely useful for Requirement 11.4's stated "logging only" purpose (a future caller, `MenuPlanService`, not yet built, needs enough context in the message alone to write a useful log line without reconstructing context elsewhere). Zero findings, approved in a single round — this completes task group 4 (all 3 Gateways: Profile/Nutrition/PlannedCalorie) with zero rejections across the entire group.
- (5.1) FIRST task in "Menu Plan Persistence". `menu-plan.repository.ts` implements all 5 methods (`getActivePlan`/`findOtherDays`/`findMealSlot`/`replaceWeek`/`replaceDay`) over `week_menu_plans`/`day_menus`/`meal_slots`/`meal_ingredients`. **`createMenuPlanRepository(db, unitConversionService)` takes an ADDITIONAL injected `UnitConversionService` (task 3.2) beyond the DB connection — this is a DELIBERATE, DOCUMENTED DEVIATION from design.md line 934's stated "Dependencies: only better-sqlite3"**: `meal_ingredients.quantity_g` (`NOT NULL, >0`) has no other source anywhere in the data flow, since `IngredientSelection` (`@nutrition/shared`) only carries `{foodId, quantity, unit}`, never a pre-computed grams value — reimplementing task 3.2's food-specific→generic fallback resolution locally via raw SQL would be strictly worse (duplicated logic, real drift risk) than injecting the already-built, already-tested Service. Confirmed cycle-free (`MenuPlanRepository → UnitConversionService → FoodCompositionRepository → DB`, no reverse edge) and confirmed the import is `import type` only (erased at compile time, zero runtime coupling). A `toGrams` failure inside `replaceWeek`/`replaceDay` is treated as an unreachable invariant violation (`throw`, not a `Result` failure) — consistent with design.md's own interface signatures, where NEITHER method returns a `Result` (both return plain `WeekMenuPlan`/`DayMenu`), and design.md's own Preconditions place input validation on the caller (`MenuPlanService`, not yet built, which would have already run `NutritionVerificationService.verifyDish` — internally calling the SAME `toGrams` — successfully before ever reaching this Repository). **`generation_source` (`'initial'|'week_regenerate'`, NOT NULL, no param in design.md's `replaceWeek` signature) is inferred by `replaceWeek` itself**: probes for an existing `week_menu_plans` row BEFORE any write, INSIDE the same transaction — existing row present → `'week_regenerate'`, absent → `'initial'`. `replaceDay` never touches this column at all (day-level replace ≠ week regeneration). **A subtle, reviewer-scrutinized asymmetry**: `replaceWeek` persists each `DayMenu.varianceKcal` exactly AS GIVEN (callers already assembled complete `DayMenu` objects with variance pre-computed); `replaceDay` instead DERIVES `varianceKcal` itself from `dayNutrition.energyKcal - targetKcal` — using the 4 given `meals`' OWN SUMMED ENERGY, deliberately NOT the separately-passed `plannedKcal` argument (design.md's signature gives `replaceDay` only raw `meals[]` + bare `plannedKcal`/`targetKcal` numbers, forcing it to derive `dayNutrition`/`varianceKcal` itself, unlike `replaceWeek`) — proven by a test that deliberately passes a plannedKcal (9999) inconsistent with the meals' real energy sum (3224), confirming variance (1124 = 3224−2100) tracks the meals, not the argument; independently re-derived by review from scratch, exact match. `getActivePlan`'s `dayNutrition` (13 fields) is computed via a LOCAL `sumNutrition` helper reading that day's 4 `meal_slots` rows FRESH on every call (never cached, never stored on `day_menus`, which retains zero micronutrient columns by design) — deliberately NOT importing `NutritionVerificationService.verifyDay` for this, since — unlike `quantity_g` — this is trivial arithmetic with no external-data dependency, keeping the Repository below Service in the dependency order wherever the data is actually available locally. Review independently ran 5 mutation tests (3 reproducing the implementer's own claims, 2 of the reviewer's own devising — including dropping `ironMg` from the summation) and all 5 were caught. **Required follow-up action (not a code change)**: this note itself is that action — a future task composing this Repository into the real DI graph (likely `MenuPlanService`, task 7.1+) must remember `createMenuPlanRepository` takes 2 arguments, not 1, and design.md line 934's Dependencies line should be understood as incomplete (missing `UnitConversionService`) rather than authoritative. **Lesson**: when a design.md interface signature structurally REQUIRES data (like `quantity_g`) that literally doesn't exist anywhere else in the data flow, a documented, cycle-checked Repository→Service dependency exception is the right call — but ALWAYS distinguish this from cases (like `dayNutrition`'s trivial summation) where the data IS available locally and importing a sibling Service would be unjustified architectural inversion; the deciding question is "does any other component already hold this exact data," not "would importing be convenient."
- (6.1) FIRST task in "Claude API連携とプロンプト構築". `claude-menu.client.ts` implements `buildWeeklyGenerationTool`/`buildDailyGenerationTool`/`buildRecipeGenerationTool` as PURE, dependency-free functions (no `@anthropic-ai/sdk` import, no network call — API-calling is task 6.2's job) taking `foodIds: readonly string[]` and `unitCodes: readonly string[] = KNOWN_UNIT_CODES` as parameters, rather than this module calling `FoodCompositionRepository.listAllIds()` itself — a deliberate, reviewer-confirmed interpretation of design.md's Dependencies line ("tool schema用の食品ID一覧取得"), since the actual repository call naturally belongs to task 6.2's `generateWeek`/`generateDay`/`generateRecipe` (which will fetch the list once and pass it in), not to pure schema-construction. **Consequential correction to the PARENT's OWN task briefing, independently verified by both implementer and reviewer against Anthropic's live documentation (not training-data recall)**: the original briefing instructed using `exclusiveMinimum`/`minimum`/`maximum`/`multipleOf` for numeric bounds and `minItems`/`maxItems` for exact array lengths (e.g. 7 days, 4 meals, 1–2 suggestions) — but Claude's `strict: true` tool-use JSON Schema does NOT support any of these; only `enum` (closed domains), `minItems` limited to exactly `0` or `1`, and prose in `description` (open-ended bounds, upper array bounds) are legal, everything else 400s at request time. Confirmed independently by the reviewer via a live fetch of `platform.claude.com/docs/en/build-with-claude/structured-outputs` + `.../strict-tool-use` during review (quoted verbatim in the review), not by trusting the implementer's citation. Consequently: `dayIndex` (weekly tool) is constrained via `enum: [0,1,2,3,4,5,6]`, NOT array `minItems`/`maxItems` on `days`; the exactly-7-days/exactly-4-meals/1–2-suggestions invariants are documented in `description` text only, with authoritative enforcement deferred to task 6.2's runtime Zod re-validation (`schema_validation_failed`) — this task cannot and does not enforce them structurally; `quantity`'s "must be positive" constraint is `description`-text only (no `exclusiveMinimum`); `supplementarySuggestions` uses the one legal `minItems: 1` (its 1–2 upper bound is `description`-only). `ClaudeToolArraySchema.minItems` is typed in TS as the literal union `0 | 1` (not `number`), a genuinely effective compile-time guardrail — reviewer mutation-tested this by reintroducing `maxItems` and confirmed `tsc` itself rejects it (`TS2353`), independent of the runtime tests. `mealType`'s enum is read at runtime from `@nutrition/shared`'s `MealTypeSchema.options` (not hand-copied literals, preventing drift); `unit`'s enum defaults to `KNOWN_UNIT_CODES` from `constants.ts` (task 1.5/2.2). The recipe tool has NO top-level `ingredients` (per design.md's `RecipeGenerationToolResult`, ingredients only exist per-suggestion under `supplementarySuggestions[].ingredients`). `strict: true` sits at the correct top level (sibling of `name`/`description`/`input_schema`), and `additionalProperties: false` + `required` are present at every nested object level recursively (verified by reviewer mutation test: removing it from the innermost ingredient node broke 3 tests across all 3 tools). Zero findings, approved in a single round after the reviewer's own independent live-doc fetch and 3 self-devised mutation tests (nested `additionalProperties` removal, enum pollution, `maxItems` reintroduction) all caught. **Forward note for task 6.2**: the tool schema's wire-level property is snake_case `food_id` (matching typical Claude tool-use JSON convention and the task text's own literal naming); 6.2 will need to convert this back to camelCase `foodId` when parsing `tool_use.input` into `IngredientSelection`/`WeeklyGenerationToolResult`/etc. **Lesson, reinforcing the project's existing "verify, don't trust recalled API shapes" discipline**: this is a case of an implementer catching a genuine error in the PARENT's OWN briefing (not just a prior task's design.md) by consulting authoritative current docs instead of the parent's instructions — the correct response was independent re-verification (both the implementer's citation and, separately, the reviewer's own live fetch) before accepting the deviation, not just trusting either party's say-so, exactly as the project already does for spec-vs-reality conflicts.
- (6.2) `generateWeek`/`generateDay`/`generateRecipe` extend task 6.1's `claude-menu.client.ts` with the real `client.messages.create` calls, via `createClaudeMenuClient(repository: FoodCompositionRepository, anthropicClient: AnthropicMessagesClient = new Anthropic())` — a MINIMAL, locally-declared `AnthropicMessagesClient` interface (just `messages.create(params)`) rather than the full SDK client type, so tests inject a plain fake object and `new Anthropic()` is only ever an unused default parameter (never constructed with a real key, never a real network call, in any test). Model/thinking/effort are read from `constants.ts` (task 1.5) — `WEEKLY_DAILY_GENERATION_THINKING`/`_EFFORT` for `generateWeek`/`generateDay`, the DISTINCT `RECIPE_GENERATION_THINKING`/`_EFFORT` pair for `generateRecipe` — never hardcoded, confirmed not swapped by a reviewer mutation test (swapping the pairs broke a test asserting the exact `thinking`/`output_config.effort` object passed to `messages.create`). A shared `invokeGenerationTool` helper implements the 3-outcome pipeline in the REQUIRED order: (1) `try/catch` around the API call → any thrown error becomes `ClaudeGenerationError(type:"request_failed")`; (2) on a normal return, `stop_reason === "refusal"` is checked BEFORE attempting to locate a `tool_use` block → `type:"refusal"`, proven genuinely order-dependent (not incidental) by a reviewer mutation test removing the refusal check, which flipped a refusal-test's outcome to `schema_validation_failed`; (3) only then is the matching `tool_use` block located and its `.input` re-validated with LOCAL Zod schemas (`WeeklyToolInputSchema`/`DailyToolInputSchema`/`RecipeToolInputSchema`) enforcing the exact cardinality invariants `strict: true` cannot express structurally (task 6.1's note) — `days.length===7`, each day's `meals.length===4`, `supplementarySuggestions.length` between 1 and 2 inclusive (confirmed via reviewer mutation test: removing just the upper `.max(2)` bound let a 3-suggestion response wrongly pass) — any violation, or a missing/absent `tool_use` block entirely, becomes `type:"schema_validation_failed"`. The wire-level snake_case `food_id`→camelCase `foodId` conversion (plus the authoritative `quantity>0` check) is done by REUSING `@nutrition/shared`'s real `IngredientSelectionSchema` via `.transform().pipe(IngredientSelectionSchema)`, not a hand-rolled mapper — confirmed a genuinely valid Zod v4 composition (both packages pin zod `^4.4.3`) and confirmed zero leftover snake_case keys in every success-path result. `cache_control:{type:"ephemeral"}` is applied to the single tool object passed in each call's `tools` array (there is exactly one, since `tool_choice:{type:"tool",name:...}` forces it) per research.md's explicit placement guidance. Requirement 3.3 (rejecting an out-of-enum food_id/unit) is satisfied BY CONSTRUCTION here — this task does not and should not re-check food_id/unit existence at runtime (design.md is explicit that this is deferred to `NutritionVerificationService`); `strict:true` + the enum (task 6.1) + this task's actual `tool_choice`-forced invocation together make an out-of-enum value structurally impossible in a well-formed response, not something requiring extra code. **One narrow, reviewer-verified-safe type cast**: `ClaudeToolDefinition` (task 6.1's hand-written local type, SDK-independent by design) is not structurally assignable to `Anthropic.Tool` under TypeScript's index-signature rule (`Tool.input_schema` carries `[k:string]:unknown`, the local `interface` does not) even though the two shapes are otherwise identical — resolved with a single `as unknown as Anthropic.Tool` cast at the one call site, verified safe (not masking a real mismatch) by a reviewer mutation test that removed the cast and got exactly the predicted `tsc` index-signature error, nothing else. Task 6.1's stale file-header comment claiming direct assignability (no cast needed) was corrected in this task to describe the actual cast and why it's needed. Added `zod` as an explicit direct dependency of `server/package.json` (previously only transitively available via `@nutrition/shared`) since this file now imports it directly — a reviewer-flagged, low-risk-but-real dependency-hygiene gap, fixed immediately rather than deferred. Zero blocking findings; approved in a single round after 646/646 full-suite pass, clean typecheck/build, and 4 independent reviewer-run mutation tests (cast removal, thinking/effort pair swap, upper-bound removal on supplementary suggestions, refusal-check removal) all caught. **Forward note for task 6.3** (`MenuPromptBuilder`, not yet started): `ClaudePromptPayload` (`{system, userMessage}`) is exported from `claude-menu.client.ts`, ready to be imported from there rather than redefined, per the established "type lives where it's first produced" convention.
- (6.3) FINAL task in "Claude API連携とプロンプト構築". `menu-prompt.builder.ts` implements `buildWeeklyPrompt`/`buildDailyPrompt`/`buildRecipeDetailPrompt` as three PLAIN top-level pure functions (matching design.md's "なし（外部依存を持たない純粋関数）" Dependencies line literally) — NOT a `createXBuilder(...)` factory, correctly mirroring the same "pure logic → plain function, stateful logic → factory" split task 6.1 already established (`buildWeeklyGenerationTool`/etc. as plain functions vs. `createClaudeMenuClient` as a factory). Also defines and exports `DislikedItemSummary` (`{dishName, foodIds}`) here for the FIRST time — confirmed by review via a full-repo grep to be genuinely undefined elsewhere — since its real producer, `FeedbackService` (task 7.2), doesn't exist yet; task 7.2 will import it from here, continuing the established "type lives where it's first produced" convention (`ClaudePromptPayload`/6.2, `OtherDayContext`/5.1, `MenuProfileSnapshot`/4.1, `VerificationError`/3.2). NG ingredients (2.1) and preferred ingredients (2.3) are rendered via genuinely DISTINCT template wording (exclusion/使用禁止 vs. priority/優先候補), not just two lists under different headers — reviewer mutation-tested this specifically (swapping only the imperative body sentence between the two sections, leaving headers untouched, was NOT caught by the original test's header-substring-plus-loose-regex approach; the round's follow-up hardening added explicit `not.toMatch()` negative assertions on both sides to close this gap). `restrictionType === "none"` correctly suppresses ALL restriction-type/intensity wording while still including NG-exclusion and preferred-priority (2.5). **A deliberate, twice-independently-verified judgment call**: `restrictionNotes` (2.4) is treated as fully INDEPENDENT of `restrictionType` — appearing verbatim even when `restrictionType==="none"` — since requirement 2.4's own text describes it as a free-text field not limited to dietary-restriction interpretation, and 2.5 only suppresses the restriction-*type*-based constraint, not this separate field; review traced this against both requirements.md 2.4/2.5's exact wording and confirmed it's the correct reading, not a stretch. **`buildRecipeDetailPrompt`'s scope-narrowing** (omits `preferredIngredients`, cooking-context, and `dislikedSummary` — only NG-exclusion, restriction constraints, and steps-generation instructions) was independently re-derived by review from THREE angles, not just accepted: (1) design.md's own Service Interface signature `buildRecipeDetailPrompt(meal, profile)` has no `dislikedSummary` parameter at all, unlike the other two methods — a hard interface constraint, not a judgment call; (2) Requirement 9.4's text narrowly enumerates "NG食材と食事制限設定" only, contrasting with Requirement 7.5 elsewhere in the SAME spec, which explicitly uses "同様に" to extend a requirement fully — the spec author's demonstrated willingness to write full-extension language elsewhere, combined with its absence here, reads as deliberate narrowing; (3) Requirement 8 (recipe detail itself) never mentions cooking-preference context at all. `buildDailyPrompt` (7.2/7.3) includes the other 6 days' dish names/food IDs (sorted by `dayIndex`) PLUS an explicit duplicate-avoidance imperative (not just a data dump) — confirmed load-bearing by a reviewer mutation test removing just the imperative sentence, which broke the test. `buildRecipeDetailPrompt`'s "NG/restriction constraints also apply to supplementary suggestions" instruction (9.4) was likewise confirmed load-bearing by mutation. `buildWeeklyPrompt`'s `Record<IsoDate, NutritionTargetSnapshot>` enumeration is explicitly `Object.keys(...).sort()`-ed rather than relying on insertion order — the original test's fixture happened to insert dates already in sorted order, so review independently ran the built module against a REVERSED-insertion-order object literal to confirm real sorting (not insertion-order luck); this adversarial case was then added as a permanent regression test in this same round (rather than left as a reviewer-only, one-off check) alongside the NG/preferred negative-assertion hardening described above. Approved in a single round with two non-blocking test-hardening suggestions, both applied immediately in-round (not deferred) since they were small, well-scoped, and left zero ambiguity about the fix.
- (7.1) FIRST task in "Core: 満足度フィードバック". `feedback.repository.ts` implements `createFeedbackRepository(db)` matching the established `createXRepository(db)` DI convention. `upsert` uses a single native SQLite `INSERT ... ON CONFLICT (week_start_date, day_index, meal_type) DO UPDATE SET dish_name=..., primary_food_ids=..., liked=..., updated_at=...` statement — a genuinely new pattern for this codebase (all prior upserts/replaces used a `db.transaction()`-wrapped check-then-write instead), chosen because the native upsert is naturally atomic AND naturally excludes `created_at` from the update set (preserving it on conflict, setting it fresh on insert) without needing an explicit branch. Review independently verified the `ON CONFLICT` column list is a genuine, exact match to the live `UNIQUE(week_start_date, day_index, meal_type)` constraint — not a coincidental syntactic match — via a mutation test that changed it to a non-matching column list and got an immediate `SqliteError: ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint`, failing all 12 tests; this proves the clause is genuinely load-bearing, not silently degrading to a plain insert. `primary_food_ids` (`string[]`) round-trips via `JSON.stringify`/`JSON.parse` — the FIRST precedent in this codebase for an array-in-TEXT-column pattern (future tasks needing similar array persistence should follow this same approach rather than inventing another). `liked` follows the already-established `row.liked === 1` / `entry.liked ? 1 : 0` boolean-as-INTEGER convention (`ProfileRepository.dietModeEnabled`, confirmed by review to be a real precedent, not asserted). Both `primary_food_ids` and `liked` round-trips are tested against the RAW underlying column value via a direct SQL query bypassing the repository's own read path (not just the repo's own mapped return value), specifically to prevent a symmetric write/read bug from canceling out and hiding. **Requirement 10.5's core "same instance, latest value only" behavior** (created_at fixed, updated_at advances, snapshot fields — dish_name/primary_food_ids, not just liked — genuinely update on conflict) is proven via a real `await wait(20)`ms gap between two upsert calls to the same triple (confirmed by review to reliably produce distinct millisecond-precision ISO timestamps in this Node runtime, not flaky-by-construction) AND via a mutation test that removed `dish_name`/`primary_food_ids` from the `DO UPDATE SET` clause, catching the resulting stale-snapshot bug in 3 tests. DB-level `COUNT(*)===1` after two upserts to the same triple, and per-triple isolation (varying only `dayIndex` or only `mealType` never affects the sibling row), are both directly tested. `findRecentDisliked` filters `WHERE liked=0 ORDER BY updated_at DESC LIMIT ?`, confirmed by review (via a mutation removing the `WHERE` clause, failing 2 tests) to never leak `liked=true` rows, correctly handles `limit` both below and above the actual disliked-row count (no padding, no error). No FK/join to `meal_slots`/`day_menus`/`week_menu_plans` anywhere in the file, consistent with this table's deliberately FK-less design (established and reviewed back in task 1.3). Zero findings, approved in a single round. **Forward note for task 7.2** (`FeedbackService`, not yet started): this Repository has no dependents wired up yet; `FeedbackService.getDislikedSummary` will call `findRecentDisliked` and map to the `DislikedItemSummary` type already defined in `menu-prompt.builder.ts` (task 6.3) rather than redefining it.
- (7.2) FINAL task in "Core: 満足度フィードバック" (completes task group 7). `feedback.service.ts` implements `createFeedbackService(feedbackRepository, menuPlanRepository)` — a Service depending on TWO sibling components (a Repository from its own spec plus the Repository providing meal-slot state), resolving a genuine THREE-WAY design.md internal inconsistency: the Service Interface's `recordFeedback(weekStartDate, dayIndex, mealType, {liked})` signature has no snapshot (dishName/foodIds) parameter, while the Responsibilities text requires this same method to both confirm meal-slot existence (`NotFoundError` on miss) AND persist a dishName/food-id snapshot, and the Dependencies line lists ONLY `FeedbackRepository` (omitting `MenuPlanRepository`, the only place meal-slot state actually lives). Resolved by calling `MenuPlanRepository.findMealSlot(weekStartDate, dayIndex, mealType)` EXACTLY ONCE per `recordFeedback` call — its result serves BOTH the existence check (`null`→`NotFoundError`, `FeedbackRepository.upsert` never reached, mutation-tested by review moving the upsert call before the null-check and confirming it broke exactly the "never called on not-found" test) AND the snapshot derivation (`dishName`/`ingredients.map(i=>i.foodId)`) from the SAME lookup — avoiding a wasteful/racy double-lookup. Review's independent verdict went further than the implementer's own justification and found THREE convergent, independent lines of evidence the resolution is not just plausible but the ONLY structurally possible one: (1) design.md's own HTTP contract for the feedback endpoint (line ~1231, task 10.2's future scope) specifies the request body as LITERALLY `{liked: boolean}` — no HTTP client could ever supply snapshot data as a parameter, since it never has it, closing off "a future caller passes it in" as a possibility entirely; (2) design.md's OWN architecture diagram already contains a directly analogous, already-accepted edge for the identical class of need (`RecipeDetailService → MenuPlanRepository`, reading an already-committed meal slot from a sibling controller-invoked service) that the Dependencies-line omission for `FeedbackService` simply failed to mirror; (3) task 5.1's own precedent (`MenuPlanRepository` adding an undeclared `UnitConversionService` constructor dependency to derive data with no other source, while its design.md-specified method signatures stayed unchanged) is mechanically identical in shape, not just a generic analogy. **Judgment call, explicitly flagged for scrutiny and independently confirmed correct**: `primaryFoodIds`（主要な食材の食品ID）is built from ALL of `MealSlot.ingredients`, not a filtered subset — confirmed by review reading `IngredientSelectionSchema` (`{foodId, quantity, unit}`) to have genuinely NO primary/non-primary flag anywhere in the data model, making "all ingredients" the only defensible reading given the current schema (would need revisiting only if a future task ever introduces such a flag). `FeedbackInput` was assumed (in the initial task briefing) to need local definition but turned out to ALREADY exist as a real shared type (`FeedbackInputSchema`/`FeedbackInput`, `shared/src/menu.schema.ts`, re-exported via `shared/src/index.ts`) — imported instead of redefined, a factual correction to the briefing rather than a judgment call. `NotFoundError`/`Result<T,E>` are genuinely reused from `server/src/shared/result.ts` (the SAME shared `NotFoundError` already used by `daily-log.service.ts`/`nutrition.routes.ts`/`app.ts`'s common error handler), not redefined. `ValidationError` is kept in the return type per design.md's literal signature but is never actually constructed anywhere, since `{liked: boolean}` has no constraint beyond what TypeScript's type system already enforces — no spurious validation rule was fabricated just to exercise it. `getDislikedSummary` correctly delegates ALL `liked=false`/recency/limit filtering to `FeedbackRepository.findRecentDisliked` (already fully tested against a real DB in task 7.1) and is proven to NEVER touch `MenuPlanRepository` at all (a throws-if-called fake for it still passes cleanly) — default limit of 10 chosen (unspecified exactly by design.md, "上限件数を設ける" only) and reasoned from a week's 28 meal slots providing a meaningful-but-bounded trend signal. Zero findings, approved in a single round. **This completes task group 7** — both `FeedbackRepository` (7.1) and `FeedbackService` (7.2) are now done; `DislikedItemSummary` (defined in 6.3's `menu-prompt.builder.ts`) now has both a real producer (`FeedbackService.getDislikedSummary`, this task) and a real consumer-shape contract (`MenuPromptBuilder.buildWeeklyPrompt`/`buildDailyPrompt`, already built in 6.3) — the two will be wired together by `MenuPlanService` (task 9.1/9.2, not yet started).
- (8.1) SOLE task in "Core: レシピ詳細の永続化" (completes task group 8). `recipe-detail.repository.ts` implements `createRecipeDetailRepository(db, unitConversionService)` — the DI signature deliberately mirrors `createMenuPlanRepository(db, unitConversionService)` (task 5.1) exactly, since `supplementary_ingredients.quantity_g` needs the identical unit→grams derivation with no other source, and design.md's Dependencies line (listing only `better-sqlite3`) is equally incomplete here as it was there — resolved as the same documented, precedented deviation, independently reconfirmed by review as a faithful (not just superficially similar) match to task 5.1's exact reasoning. **A second, equally load-bearing gap resolution**: `RecipeDetail.nutrition` has NO corresponding column anywhere in `recipe_details` — deliberately, since a recipe's dish nutrition is ALREADY authoritative in `meal_slots.energy_kcal`/`protein_g`/`fat_g`/`carb_g` (written earlier by `MenuPlanRepository`), and design.md's own Postcondition for the not-yet-built `RecipeDetailService` (task 10.1) explicitly states recipe-detail generation must not change the meal slot's nutrition — meaning the two values are DEFINITIONALLY identical, not just usually equal, so storing nutrition a second time would be pure duplication; review independently traced this through design.md's Requirement 8.3 cross-reference and confirmed no scenario exists where they could legitimately diverge. Resolution: `upsert`'s incoming `detail.nutrition` argument is NEVER persisted to any column; both `findByMealSlotId` and `upsert`'s return value derive `nutrition` fresh by reading the referenced `meal_slots` row's own 4 columns — continuing this spec's established "derive, don't duplicate" convention (`MenuPlanRepository.getActivePlan.dayNutrition`, task 5.1). `upsert` implements design.md's explicit, literal "delete existing row then reinsert, in one transaction" instruction verbatim (one `db.transaction(...)` wrapping `DELETE FROM recipe_details WHERE meal_slot_id=?` — which alone cascades away `supplementary_suggestions`/`supplementary_ingredients` via the schema's own `ON DELETE CASCADE`, no redundant manual deletes — followed by fresh inserts), reads back its own write via `findByMealSlotId` before returning (matching `MenuPlanRepository`/`FeedbackRepository` convention), and uses the by-now twice-precedented JSON-in-TEXT-column pattern (`steps_json`, following `feedback.repository.ts`'s `primary_food_ids`) for the `steps: string[]` field. **Round-1 REJECTED on exactly one finding, fixed in round 2**: the original order-preservation test (`supplementarySuggestions` array order surviving an upsert+read round trip) did NOT actually prove `sort_order`-based ordering — reviewer mutation-tested this by removing the `ORDER BY sort_order ASC` clause entirely and found ALL 15 tests still passed, because `upsert`'s own write path always assigns `sort_order` from the same array index used for physical row insertion, so through the public API alone `sort_order` and SQLite's insertion order can never diverge, making the test's own "not accidental insertion order" claim unproven by construction — an identical class of gap to task 1.1's already-documented "trivial contrasting case doesn't prove the NULL-specific claim" lesson. Fixed by adding a test that directly `UPDATE`s `sort_order` via raw SQL (keyed by row id, independent of insertion order) to construct a genuine divergence, then confirms `findByMealSlotId` follows the NEW `sort_order`, not insertion order — round-2 review independently re-reproduced the exact same mutation-kill (removing `ORDER BY` breaks only this one new test) before approving, rather than trusting the implementer's report of having already verified it. CASCADE DELETE (8.4) is proven not just at the immediate `recipe_details` level but end-to-end through all 3 dependent tables, using the same real deletion pattern `MenuPlanRepository.replaceDay`/`replaceWeek` actually performs during regeneration (confirmed by review cross-referencing those methods' real `DELETE FROM meal_slots` calls) — so the test's simulated scenario is a faithful mirror of production behavior, not a contrived one. `quantity_g` conversion is proven against REAL seeded data (egg food_id `12004`, unit "個"=50g/unit) rather than a fake, specifically chosen for a non-1:1 gram ratio so a broken conversion couldn't silently pass. Approved after 2 rounds — the only rejection in the `menu-generation` spec so far to require an actual remediation round (all prior tasks approved on round 1). **This completes task group 8.**
- (9.1) FIRST task in "Integration: 週間・日単位献立生成のオーケストレーション" — the largest, most consequential task in this spec so far (8 upstream dependencies wired together, plus safety-critical concurrency control). `menu-plan.service.ts` implements `generateWeek`/`regenerateWeek`/`getActivePlan` (deliberately NOT `regenerateDay` — that's task 9.2's scope, not started). `generateWeek` and `regenerateWeek` are thin wrappers sharing ONE internal orchestration function (`MenuPlanRepository.replaceWeek`, already built in 5.1, determines `generation_source` itself via its own pre-write probe, so this Service genuinely needs zero branching between the two — confirmed by design.md's own prose that both follow "同一フロー"). A generic, reusable `KeyedLock`/`withGenerationLock<T>` helper implements the in-memory concurrency guard (Requirement 12.5) — acquired as the LITERAL FIRST action (before any dependency call), released in a SINGLE shared `finally` wrapping the entire orchestration (so success/Result-failure/thrown-exception all release identically, by construction, not via 3 separately-maintained branches), keyed by `weekStartDate` (shared namespace between `generateWeek`/`regenerateWeek` since they mutate the same resource) — explicitly documented as forward-compatible for task 9.2's day-level key format (`` `${weekStartDate}:day:${dayIndex}` ``). Reviewer mutation-tested lock release by deleting it entirely: 4 tests failed (success-reuse, profile_missing-reuse, claude_refusal-reuse, thrown-exception-reuse), proving release is genuinely exercised across early-stage, late-stage, and exceptional exit paths, not just the happy path. **A genuine, previously-unidentified defensive gap this task closes**: task 6.2's `WeeklyToolInputSchema` (Zod re-validation of Claude's tool output) checks `days.length===7` and each individual `dayIndex∈[0,6]`, but never checked the 7 values collectively form the exact set `{0..6}` with no duplicates — a response with two entries at `dayIndex:2` and none at `dayIndex:5` would have passed task 6.2's validation undetected. This task adds `hasCompleteDayIndexSet` (a `new Set(dayIndices).size===7` check, confirmed correct and sufficient given the per-item enum already constrains each value to 0-6) as its own orchestration-layer guard, mapping any violation to `GenerationError(reason:"schema_validation_failed")` before any persistence — reviewer confirmed this guard and a separate PRE-EXISTING "day not found" fallback lookup happen to both catch the same malformation (redundant defense-in-depth, not a gap) via the SAME reason string, and mutation-tested that the dedicated test for the NEW guard didn't originally discriminate between the two code paths (removing the guard entirely still passed all tests, since the fallback silently covered for it) — fixed within the same round by asserting on the guard's own distinct message text. `GenerationError`/`GenerationFailureReason` (8 values: profile_missing/nutrition_unavailable/schema_validation_failed/food_id_not_found/unit_not_found/claude_refusal/claude_request_failed/generation_in_progress) are defined and exported HERE for the first time (confirmed by full-repo grep to not already exist in `@nutrition/shared` or elsewhere), continuing the "type lives where it's first produced" convention. `buildWeeklyPrompt` (a plain pure function, task 6.3) is called directly rather than dependency-injected — confirmed by review to genuinely match the established "pure functions aren't wrapped in DI factories" precedent (task 6.1's `buildWeeklyGenerationTool`/etc. are likewise plain functions never wrapped by `createClaudeMenuClient`). **Round-1 REJECTED on exactly one finding**: the task's own explicitly-named `FeedbackService.getDislikedSummary()`→`MenuPromptBuilder` wiring (Requirement 6.2) had ZERO test coverage — reviewer mutation-tested this by discarding the real disliked summary (passing `[]` instead) and found all 22 original tests still passed, since every test's fake `ClaudeMenuClient.generateWeek` ignored its `payload` argument entirely. Fixed in round 2 by adding a test that captures the actual payload delivered to `ClaudeMenuClient.generateWeek` under a distinguishable non-empty disliked-summary override and asserts the disliked content reached it — round-2 review independently re-reproduced the exact same mutation (not trusting the implementer's report) and confirmed exactly 1 new failure, matching precisely. Two optional hardening suggestions from round 1 (strengthening the dayIndex-guard test's discriminating power; adding a later-stage — `claude_refusal` — lock-release-then-reuse test beyond the original early-failure/thrown-exception cases) were BOTH also applied proactively in the same round rather than deferred, and both independently re-verified as genuine via reviewer-reproduced mutations. All 8 `GenerationFailureReason` mappings, full 28-slot verification/assembly correctness (including UTC-safe date arithmetic at month/year boundaries, correct per-day target matching for `varianceKcal`), partial-failure atomicity across every failure path (`replaceWeek` proven never called via throws-if-called fakes), and `PlannedCalorieGateway`'s Requirement-11.4 non-fatal-failure behavior (all 7 days still attempted, overall success preserved, failures logged) were all independently verified with reviewer-run mutation tests, not just read-and-trust. Approved after 2 rounds — this is now the SECOND task in the spec (after 8.1) requiring an actual remediation round, and by far the most complex to pass review. **Forward note for task 9.2**: `regenerateDay`'s day-level lock key format and the `KeyedLock`/`withGenerationLock` reuse are both anticipated and ready; `regenerateDay` itself and its `NotFoundError` handling remain entirely unimplemented, as scoped.
- (9.2) Extends task 9.1's `menu-plan.service.ts` with `regenerateDay(weekStartDate, dayIndex)`, sharing its established patterns: day-level `KeyedLock` key (`` `${weekStartDate}:day:${dayIndex}` ``, using the generalized `withGenerationLock<T, TError=never>` helper — confirmed by review to leave `generateWeek`/`regenerateWeek`'s existing non-`NotFoundError` behavior genuinely unchanged), `getActivePlan`→`NotFoundError` on a missing week, then the SAME profile/nutrition-target/disliked-summary sequence as weekly generation (per design.md's own explicit "同一の手順" note) scoped to one day, `findOtherDays`/`getDislikedSummary` genuinely wired into `buildDailyPrompt`'s payload (proven via distinguishable-fixture payload-capture tests, correctly avoiding task 9.1's original round-1 mistake of untested wiring — not repeated here). **Critical, easy-to-miss correctness point, independently re-derived by review from `menu-plan.repository.ts`'s actual `runReplaceDay` code**: `replaceDay`'s `plannedKcal` parameter is stored VERBATIM into `day_menus.planned_kcal`, while `variance_kcal` is independently RE-DERIVED by `replaceDay` itself from the passed `meals`' own summed nutrition — meaning `MenuPlanService` MUST compute `plannedKcal` as the genuine sum of the 4 verified meals' `energyKcal` (via `verifyDay`, bit-identical arithmetic to the Repository's own internal `sumNutrition`) and never substitute a placeholder (e.g. the fetched `targetKcal`) — confirmed correct and mutation-tested by review (swapping in `targetKcal` broke a test asserting the real computed sum). **A second gap this task ALSO closed, symmetric to task 9.1's `hasCompleteDayIndexSet`**: task 6.2's `MealWireSchema`/`DailyToolInputSchema`/`WeeklyToolInputSchema` constrain each meal's `mealType` individually (enum) and the meals array to length 4, but never check the 4 meals collectively cover all 4 distinct `MealType` values with no duplicates — a duplicate-mealType response would previously have reached `MenuPlanRepository.replaceWeek`/`replaceDay` and thrown an UNCAUGHT `SqliteError: UNIQUE constraint failed: meal_slots...` instead of a clean `GenerationError` Result. Added a reusable `hasCompleteMealTypeSet` guard (same `new Set(...).size===N` pattern as `hasCompleteDayIndexSet`, sourced from the real `MealTypeSchema.options`, not hand-copied) and RETROFITTED it into task 9.1's already-approved, already-committed `runWeeklyGeneration` (per-day, for all 7 days) in addition to this task's own `regenerateDay` — review mutation-tested BOTH call sites independently (removing the guard from only the daily path broke only the daily test, confirming the weekly test doesn't accidentally mask a daily-only regression, and vice versa) and confirmed zero regression to any of task 9.1's original 24 tests. **An important, explicitly-escalated (not rejected) finding from review, left as a known, documented gap rather than silently accepted or force-fixed mid-task**: `regenerateDay`'s day-level lock key and `generateWeek`/`regenerateWeek`'s week-level lock key are DELIBERATELY INDEPENDENT (a documented judgment call: Requirement 12.5's literal text only mandates blocking duplicate requests to the SAME target — week/day/meal-slot are enumerated as distinct granularities — and design.md is silent on the overlap case). Review traced a concrete scenario: a `regenerateWeek` call in-flight (mid-Claude-round-trip) concurrently with a `regenerateDay` call for a day WITHIN that same week — both proceed independently (different lock keys), and whichever's `replaceWeek`/`replaceDay` DB write lands SECOND silently wins, discarding the other's just-persisted result with NO error surfaced to either caller (better-sqlite3's synchronous, single-transaction writes rule out literal row-level SQL corruption, but a genuine last-writer-wins data loss remains possible across the two independent async operations). This is a REAL, user-triggerable risk (e.g., a user requests a full week regenerate, then impatiently regenerates one day before the first completes) that is spec-permitted (doesn't violate 12.5's literal wording) but user-experience-significant — reviewer explicitly recommended escalating to the spec owner (e.g., a future task making `regenerateDay` ALSO hold the week-level lock, which would need reader/writer-style semantics rather than a simple mutual-exclusion Set, since two DIFFERENT days' concurrent regeneration must remain allowed) rather than silently living with it or over-fixing it mid-task with an untested, unreviewed locking redesign. **Recorded here explicitly as a known limitation requiring a deliberate future decision — not forgotten, not silently patched.** Approved in a single round (no remediation needed) — all mechanical checks, wiring tests, and 3 independent reviewer-run mutation tests (plannedKcal placeholder, mealType-guard removal at each site, lock-release removal) passed.
- (9.3) FINAL task in "Integration: 週間・日単位献立生成のオーケストレーション" (completes task group 9). `menu-plan.routes.ts` implements the 4 in-scope routes (`generate`/`regenerate`/day-`regenerate`/`GET`) — the `/shopping-list` row in design.md's own API Contract table is correctly NOT implemented here (that's `ShoppingListService`, task 13.x, not yet started). `weekStartDate` validation is 3-stage (regex format → real-calendar-date round-trip → `getUTCDay()===1` Monday-only, matching `MenuPlanService`'s documented precondition that the Controller — not the Service — enforces this) — genuinely the most distinctive rule in this task, reviewer mutation-tested by disabling the Monday check and confirming 4 tests failed. `GenerationError.reason`→409/502 mapping (409: profile_missing/nutrition_unavailable/generation_in_progress; 502: the other 5 reasons) is handled DIRECTLY inside `menu-plan.routes.ts` via `reply.status(...).send(...)`, reusing `nutrition.routes.ts`'s already-established precedent for exactly this situation (a novel error shape `app.ts`'s shared `setErrorHandler` doesn't recognize) — confirmed by review via byte-for-byte `git diff` that `setErrorHandler` itself was NOT touched. `regenerateDay`'s `NotFoundError` is `throw`n, relying on the EXISTING (also unmodified) handler's 404 branch. `GET /api/menu-plans/:weekStartDate` with no active plan returns 200+`null` (NOT 404) — an explicit, easy-to-invert design.md requirement, deliberately DIFFERENT from `regenerateDay`'s 404-on-missing-plan; reviewer mutation-tested this distinction (flipping GET's no-plan case to 404) and confirmed a dedicated Requirement-11.3 test catches it. Zod schemas for `weekStartDate`/`dayIndex` are defined LOCALLY in this file (not added to `@nutrition/shared`) since, unlike `nutrition-engine`'s cross-route-shared `NutritionDateQuerySchema`, this validation is specific to a single spec's Controller — and use `zod`'s REAL types directly (not `nutrition.routes.ts`'s `ZodIssueLike` structural-typing workaround), since that workaround's original motivation (server having no direct `zod` dependency) no longer applies post-task-6.2. **A deliberate, explicitly-flagged, and reviewer-scrutinized departure from precedent**: `AppRouteDependencies.menuPlanService` was added as OPTIONAL (unlike `profileService`/`dailyLogService`/`nutritionService`, all required) with a guarded `if (deps.menuPlanService)` registration call — necessary because making it required would break `index.ts`'s existing `registerRoutes` call (no task in `tasks.md` yet wires `MenuPlanService`'s own 8 dependencies into `index.ts`). Review independently git-log-verified (`git show` on the 3 historical commits) that this is GENUINELY DIFFERENT from how the 3 existing fields were introduced — each was added as REQUIRED with its real `index.ts` wiring landing in the SAME commit, no prior precedent exists for an optional/deferred field — the implementer's original in-code comment inaccurately claimed similarity to that precedent; corrected in this same round to describe it as a deliberate departure, not a repetition. **This is the most significant finding of this task, escalated rather than silently accepted**: `tasks.md` (grepped by review, confirmed empty) has NO future task, from 10.1 through 14.3, that wires `MenuPlanService`, `RecipeDetailService`, or `ShoppingListService` into `server/src/index.ts` — meaning after EVERY currently-planned menu-generation task is complete, the entire feature remains UNREACHABLE from a real running server (only exercised via `.inject()`-based route tests against a manually-constructed `buildApp()`). This is a genuine gap in the TASK PLAN itself, not a defect of task 9.3 (whose own literal text only required `app.ts` registration + `.inject()`-verified status codes, not server-startup reachability — unlike `nutrition-engine`'s analogous task 4.2, which explicitly required observable reachability after real server startup, a stricter decomposition this spec's author did not repeat here). **Flagging prominently for `/kiro-validate-impl menu-generation`'s Design/Boundary Audit section to surface as NO-GO or MANUAL_VERIFY_REQUIRED**: a new task (likely alongside or after task group 13/14, once `ShoppingListService`/`RecipeDetailService` also exist) will be needed to construct the full `MenuPlanService`/`RecipeDetailService` dependency graphs (DB connections, `Anthropic` client, all Gateways/Repositories/Services) inside `index.ts`'s `startServer()` and pass them to `registerRoutes`, before this spec can be considered functionally complete, not just unit/integration-tested in isolation. Approved in a single round with one trivial, immediately-applied fix (correcting the app.ts comment's inaccurate historical claim).
- (10.1) FIRST task in "Integration: レシピ詳細生成のオーケストレーション". `recipe-detail.service.ts` implements `createRecipeDetailService(deps)` orchestrating `MenuPlanRepository.findMealSlot` (→`NotFoundError` if missing) → `ProfileGateway.getCurrentProfile` (→`GenerationError(profile_missing)` if null) → `buildRecipeDetailPrompt` (real, uninjected pure function, matching 9.1's precedent) → `ClaudeMenuClient.generateRecipe` → sequential `NutritionVerificationService.verifyDish` per supplementary suggestion (short-circuiting on the FIRST failure, even if a later suggestion would have verified fine — confirmed load-bearing by reviewer mutation test) → `RecipeDetailRepository.upsert`. **Deliberate judgment call, independently scrutinized against Requirement 12.1's actual text (not just superficial consistency) and endorsed as correct**: `ProfileGateway` is labeled P1 (not P0) in design.md's Dependencies table, but a missing profile is still treated as a hard `GenerationError(profile_missing)` failure (matching `MenuPlanService`'s task-9.1 precedent) rather than proceeding with a degraded/empty-constraint prompt — justified because `buildRecipeDetailPrompt(meal, profile)` requires a real profile as a non-optional argument to apply Requirement 9.4's NG-ingredient/restriction constraints to supplementary suggestions, and Requirement 12.1's text isn't scoped to weekly/daily generation only; the P1 label is better read as a DI-sequencing note, not a "may be skipped" signal. **A genuine, easy-to-miss field-projection step, explicitly targeted by review's mutation testing and confirmed correctly and completely applied at BOTH call sites**: `SupplementarySuggestion.nutritionDelta` and `RecipeDetail.nutrition` are both the 4-field `NutritionValues` (per Requirement 9.3's literal "エネルギー量・PFC量" — energy+PFC only), while `NutritionVerificationService.verifyDish` returns the 13-field `VerifiedNutritionValues` — a `projectToNutritionValues` helper strips the 9 micronutrient fields before either assignment; reviewer mutation-tested by skipping the projection at both sites (passing the raw 13-field object, which TypeScript's structural typing would silently ALLOW since it's a superset) and confirmed 2 tests genuinely catch it via explicit `Object.keys().sort()` key-set assertions, not just value-presence checks. The Claude-error→`GenerationFailureReason` mapping table is REPLICATED locally (not imported) since `menu-plan.service.ts`'s equivalent mapping is a private, unexported helper and this task's boundary disallows modifying that file — reviewer confirmed the replica is character-for-character identical to the original via direct side-by-side comparison (a real, flagged future-maintenance risk if the two copies are ever edited independently, but correct today). Task 8.1's already-established "Repository discards incoming `detail.nutrition`, always re-derives from `meal_slots` on return" behavior is respected: this Service still passes the semantically-correct (meal slot's own, projected) value rather than a placeholder, purely for code clarity, and returns the REPOSITORY's own returned `RecipeDetail` verbatim rather than reconstructing one locally. **Round-1 REJECTED on exactly one finding**: both success-path tests' fake `upsert` was wired as `(_, detail) => buildStoredRecipeDetail(detail)` — since that fixture builder spreads `...overrides` LAST, passing the real `detail` as its own override argument caused every "distinguishable" sentinel field to be silently overwritten by the real input, making the fake indistinguishable from a locally-reconstructed object; reviewer proved this by mutating the Service to return `{mealSlotId, ...detail}` instead of the Repository's real return value and finding all 10 tests still passed. Fixed in round 2 by calling `buildStoredRecipeDetail()` with NO arguments (a genuinely fixed, input-independent sentinel object) — round-2 review independently re-reproduced the exact same substitution mutation (not trusting the implementer's report) and confirmed it now correctly fails exactly the 2 success-path tests. Requirement 8.4 (レシピ詳細が再生成で破棄される) required ZERO new code in this task — already fully guaranteed by the existing `recipe_details...ON DELETE CASCADE` (task 8.1) + `MenuPlanRepository.replaceDay`/`replaceWeek`'s delete-and-reinsert-with-new-id `meal_slots` semantics (task 5.1), correctly recognized and left untouched rather than redundantly reimplemented.
- (10.2) FINAL task in "Integration: レシピ詳細生成のオーケストレーション" (completes task group 10). `meal-slot.routes.ts` implements the 2 in-scope routes (`recipe-detail`/`feedback`) — the `eating-out-suggestion` GET row in design.md's own API Contract table is correctly NOT implemented here (task 15.x, not started). Closely mirrors task 9.3's already-approved `menu-plan.routes.ts` conventions verbatim: the identical 3-stage `weekStartDate` Zod validation (format→real-calendar-date→Monday-only), identical `dayIndex` coercion, the SAME complete 8-reason `GenerationError`→409/502 bucketing (even though only ~6 reasons are structurally reachable from `RecipeDetailService`, matching 9.3's own "implement the complete table regardless of local reachability" choice for consistency), `GenerationError` handled directly in-route (not modifying `app.ts`'s shared `setErrorHandler`, confirmed byte-for-byte unchanged by review), and `NotFoundError`/`ValidationError` thrown to the existing common handler. `mealType` sourced from the real `MealTypeSchema` (`@nutrition/shared`), `{liked:boolean}` body validated via the real `FeedbackInputSchema` — neither hand-copied. The 204-No-Content feedback-success path was explicitly verified (by review) to have a TRULY empty body (`rawPayload.length===0`), not `"null"`/`"{}"`. `AppRouteDependencies` gained 2 more optional fields (`recipeDetailService`/`feedbackService`), following task 9.3's `menuPlanService?` precedent for the identical reason (no task yet wires these Services' own dependency graphs into `index.ts`) — the two new fields are JOINTLY gated (`if (deps.recipeDetailService && deps.feedbackService)`) since one route file/registration call exposes both endpoints together, reviewer-confirmed correct given that structure. Zero findings; approved in a single round — the FIRST task in this spec's task-9/10 HTTP-layer group to pass with zero reviewer findings on the first attempt (9.3 needed one trivial comment fix, 10.1 needed a real remediation round). Reviewer ran 4 independent mutation tests (Monday-check removal, reason-bucket swap, 204→200+body swap, argument-order swap into `recordFeedback` — the last one caught by BOTH a test failure AND a TypeScript compile error), all caught. **This completes task group 10 and, combined with task group 9, means the full HTTP surface for weekly/daily generation AND on-demand recipe detail/feedback is now implemented — though, per task 9.3's already-recorded finding, none of it is yet reachable from a real running server pending a future `index.ts`-wiring task.**
- (11.1) FIRST task in "Validation: 単体テストの拡充" — the first pure test-expansion task in this spec (no new production code, `_Boundary:_`-less like the Foundation-group tasks but for a different reason: cutting across an already-complete component's own test file). Added 16 net-new tests to `menu-prompt.builder.test.ts` (21→37), closing 3 gaps identified by an explicit pre-write audit of existing coverage: (1) a full restriction-type×intensity combination matrix for `buildWeeklyPrompt` — **caught and corrected a genuine arithmetic error in the parent's own task briefing**: `RestrictionTypeSchema` has 5 total values but only 4 are non-`"none"` (`low_carb`/`low_fat`/`high_protein`/`calorie_only`), so the real combination count is 4×3=12, not the briefed "5×3=15" — implementer verified against the real schema and used the correct 12, reviewer independently re-verified the same arithmetic before accepting; each combination's assertions include NEGATIVE checks (does NOT contain the other 3 types' or 2 other intensities' text), confirmed genuinely load-bearing by reviewer mutation test (hardcoding intensity display to always "strict" broke exactly the 8 non-strict matrix cases); (2) `restrictionNotes` non-dietary-verbatim-passthrough coverage extended to `buildDailyPrompt`/`buildRecipeDetailPrompt` (previously only independently tested under `buildWeeklyPrompt`, even though the OTHER two functions include the same content via a shared internal helper) — reviewer mutation-tested that stripping the notes section from ONLY `buildDailyPrompt` (leaving `buildWeeklyPrompt` untouched) is caught SPECIFICALLY by the new dedicated test, proving the old `buildWeeklyPrompt`-only coverage genuinely would NOT have caught a per-function regression; (3) a new daily-duplicate-avoidance depth test with an identical food ID deliberately shared across 2 of the 6 "other days" — proving the shared ID appears exactly twice (not silently deduped) and each occurrence is correctly anchored to its own day's line, reviewer mutation-tested by introducing a cross-day dedup bug and confirming only this new test (not the pre-existing presence-only checks) catches it. Zero production bugs found — `menu-prompt.builder.ts` remains completely untouched, confirmed byte-identical to HEAD by review before and after all mutation testing. Approved in a single round with zero findings.
- (11.2) Second test-expansion task in "Validation". A pre-write audit (both by the implementer and independently by review) found `unit-conversion.service.test.ts`'s existing priority/fallback/error coverage (from task 3.2) ALREADY comprehensive for `toGrams` in isolation — correctly NOT padded with redundant tests; two of the parent's own hypothesized gaps ("more than 2 candidate entries", "exact-vs-near-match food ID") were correctly ruled out as structurally inapplicable to `UnitConversionService`'s boundary (its repository interface exposes only single-result lookups; ID-matching lives in `FoodCompositionRepository`'s SQL layer, a different, already-tested component — confirmed by review cross-referencing `food-composition.repository.test.ts`'s real seeded-DB coverage of exactly this). The one genuine gap: no existing test combined multiple DIFFERENT unit-resolution paths WITHIN A SINGLE `verifyDish` call. Added to `nutrition-verification.service.test.ts`: a 3-ingredient dish (白米150g via the "g" fast-path/zero lookups, 卵2個 via a food-specific entry, しょうゆ2大さじ via generic-only fallback) that — unlike the PRE-EXISTING multi-ingredient test, which fakes `UnitConversionService.toGrams` directly — constructs the REAL `createUnitConversionService(fakeRepository)` and composes it into the real `NutritionVerificationService`, so priority/fallback resolution genuinely executes end-to-end rather than being mocked away; a deliberately CONFLICTING generic 個-entry (999g, vs. the real food-specific 50g) is seeded simultaneously to prove true priority while a DIFFERENT ingredient's generic-fallback path is also live in the same call, and call-count/argument assertions on `findUnitConversion`/`findGenericUnitConversion` independently confirm each of the 3 ingredients took its expected resolution path (not just a coincidentally-correct final sum). All 13 `VerifiedNutritionValues` fields were hand-derived and INDEPENDENTLY RE-DERIVED FROM SCRATCH by review (energyKcal=415, proteinG=18.35, fatG=10.65, carbG=58.45, fiberG=2.25, calciumMg=59.5, ironMg=2.25, vitaminAUg=140, vitaminDUg=3.8, vitaminB1Mg=0.105, vitaminB2Mg=0.445, vitaminCMg=0, saltEquivalentG=4.8) — zero discrepancy, matching the rigor task 3.3's original review already established as the standard for this file. Reviewer ran 3 mutation tests (priority-order swap in `toGrams` — breaks the test via a ~7.5x energyKcal jump; removing the "g" fast-path short-circuit — breaks the call-count assertions; an off-by-one summation divisor — caught by both the new test and a pre-existing one), all caught. A small `verifyDay([])` empty-array boundary test (all 13 fields individually asserted zero, not just "doesn't throw") was also added for Req 4.4. Zero production bugs found in either service; both `.ts` files confirmed byte-identical to HEAD. Approved in a single round with zero findings — the SECOND task in a row (after 11.1) to demonstrate that "audit first, only add genuine gaps" correctly identifies test-expansion tasks where MOST of the requested coverage already exists from thorough original-task review, rather than mechanically padding every named scenario.
- (11.3) FINAL task in "Validation: 単体テストの拡充" (completes task group 11). Audit confirmed `claude-menu.client.test.ts` (task 6.2) ALREADY comprehensively covers the "週7日/日4食" `schema_validation_failed` field-count scenarios (7 existing cases: weekly 6-days, weekly one-day-3-meals, daily 3/5-meals, recipe 0/3-suggestions, missing tool_use block) — correctly left untouched; review additionally confirmed Requirement 1.4's OTHER half ("does not persist a partial plan") is a DIFFERENT component's boundary (`MenuPlanService`, task 9.1, already tested there), so no gap remains anywhere for 1.4. The genuine gap, matching the SAME "compose the real dependency instead of a fake" pattern already used in task 11.2: `feedback.service.test.ts`'s existing tests exclusively faked `FeedbackRepository`, meaning Req 10.5 ("latest value only" on repeated feedback) and Req 10.3 (`liked:true` excluded from disliked summary) were only ever proven at the isolated Repository-DB layer (task 7.1) OR via a Service-layer test asserting "the right fake method was called with the right arguments" — never through the REAL Service→Repository→DB round trip together. Added 3 integration-style tests composing a REAL `createFeedbackRepository` against a real, migrated, temp-file SQLite DB (reusing `feedback.repository.test.ts`'s established `mkdtempSync`+`createConnection`+`runMigrations` pattern verbatim, not reinvented) wired into the REAL `createFeedbackService` — `MenuPlanRepository` correctly stays faked (reviewer independently confirmed `recordFeedback`/`getDislikedSummary` only ever touch it via `findMealSlot`, never for persistence, making this a legitimate scope reduction, not a hidden weakness). Test 1: same meal-slot instance, 2nd `recordFeedback` call with a DIFFERENT snapshot — proves the OLD snapshot is genuinely GONE (not just that the new one is present) via both a raw DB query and `getDislikedSummary()`. Test 2: same instance, 2nd call flips `liked:false→true` — proves via raw DB (row count stays 1, `liked` flips) that the entry becomes fully excluded from the disliked summary, not left with stale data. Test 3: 5 distinct meal-slot instances seeded with a real mix of liked/disliked via genuine `recordFeedback` calls — proves `getDislikedSummary()` contains EXACTLY the 3 disliked entries AND actively excludes the 2 liked ones (not just presence-only checking, which could pass even with a broken never-filters bug). Reviewer ran 3 mutation tests directly against `feedback.repository.ts` (removing snapshot fields from the upsert's `DO UPDATE SET` clause; removing the `WHERE liked=0` filter; replacing the upsert with a silent-no-op `INSERT OR IGNORE`), each caught by the new tests' CONTENT assertions specifically (not just row-count, which the third mutation would have slipped past) — confirming the tests are genuinely load-bearing, not vacuous. Zero production bugs found across all 4 files touched by this task group's audits (`feedback.service.ts`/`feedback.repository.ts`/`claude-menu.client.ts` all confirmed byte-identical to HEAD). Approved in a single round with zero findings — the THIRD task in a row (11.1, 11.2, 11.3) to demonstrate the "audit first, add only genuine end-to-end gaps, don't pad already-thorough coverage" discipline paying off: each of these 3 tasks found that MOST of their nominally-requested scenarios were already covered by the original tasks' own thorough reviews, with the real remaining value concentrated in composing REAL dependencies where prior tests had only used fakes. **This completes task group 11 and, with it, all "Validation: 単体テストの拡充" work — task groups 12 (結合テスト), 13 (買い物リスト・外食代替), and 14 (their test expansion) remain.**
- (12.1) FIRST task in "Validation: 結合テストとエラーパスの検証" — the most architecturally significant test in the spec so far: the FIRST place anywhere in the codebase that constructs `MenuPlanService`'s full real dependency graph end-to-end (a graph no task has yet wired into `index.ts` itself — see task 9.3's recorded gap). `week-generation.integration.test.ts` follows `nutrition-engine`'s already-established `nutrition.summary.integration.test.ts` precedent exactly (temp SQLite DB per test, real Repository→Service→Gateway chain matching `index.ts`'s intended build order, `buildApp()`+route registration, `app.inject()`-based real HTTP). Every dependency is REAL except the Anthropic Messages API layer (faked via the minimal `AnthropicMessagesClient` seam from task 6.2), including correctly distinguishing and wiring BOTH of the two same-named-pattern gateway layers that exist in this codebase: nutrition-engine's OWN `ProfileGateway`/`DailyLogGateway` (in `server/src/nutrition/`, feeding nutrition-engine's `NutritionService`) versus menu-generation's OWN `ProfileGateway`/`NutritionGateway`/`PlannedCalorieGateway` (in `server/src/menu-generation/`, wrapping the SAME underlying service instances) — reviewer independently traced every single factory call against its real production signature and confirmed neither layer was conflated or skipped. Uses genuinely real, seeded food IDs (`01088`/`12004`/`01006`/`01015`/`01020`/`01026`/`01031`/`01034`, independently re-verified by review via direct grep of `010_seed_food_items.sql` — the exact same set already used in `menu-plan.repository.test.ts`'s `FOOD_IDS` fixture) all paired with unit `"g"`, which review confirmed via source inspection genuinely bypasses `unit_conversions` table dependency entirely (`toGrams`'s "g" fast-path). The 28-meal-slots assertion (Req 1.1/1.3) queries real persisted state directly (raw SQL AND a fresh `getActivePlan` re-read), not just the HTTP response body. `PlannedCalorieGateway.submitPlannedCalories`'s "called for all 7 days" requirement (Req 11.1/11.2) is proven via its REAL OBSERVABLE EFFECT — querying `dailyLogService.getLog(date)` for each of the 7 real dates — since this Gateway was deliberately NOT faked (no spy exists on it anywhere in the file). `regenerate`'s replace-not-duplicate behavior (Req 6.1) is proven by re-confirming exactly 28 rows (not 56) after a second generation. The disliked-summary-reaches-Claude-prompt proof (Req 6.2/6.3) captures the ACTUAL request payload delivered to the fake Anthropic client for the regenerate call specifically and asserts the real recorded disliked dish name is present — genuine end-to-end proof through `FeedbackService.getDislikedSummary()` → `MenuPromptBuilder.buildWeeklyPrompt` → the real HTTP-triggered `MenuPlanService.regenerateWeek` → `ClaudeMenuClient.generateWeek`. Feedback for the disliked meal was recorded via a direct `feedbackService.recordFeedback(...)` call rather than a real `POST .../feedback` HTTP request — a deliberate, documented scope-narrowing since wiring `RecipeDetailService`/`meal-slot.routes.ts` (needed for that HTTP endpoint) is out of THIS task's explicit 2-flow scope and belongs to tasks 12.2/12.3. Reviewer ran 3 independent mutation tests (disabling `replaceWeek`'s child-row deletion — breaks the regenerate test with a 500; injecting a fabricated non-seeded food_id — breaks BOTH tests with 502/food_id_not_found, proving no silent pass-through; hardcoding `getDislikedSummary` to return `[]` — breaks the prompt-content assertion with a clear diff), all caught. Zero findings, approved in a single round. **Forward note for tasks 12.2/12.3**: the fake `AnthropicMessagesClient` here only handles the weekly-generation tool name (throws on any other) — extending this pattern for daily/recipe generation will need `DAILY_GENERATION_TOOL_NAME`/`RECIPE_GENERATION_TOOL_NAME` branches, and HTTP-level recipe-detail coverage will additionally need `RecipeDetailRepository`/`RecipeDetailService` construction and `meal-slot.routes.ts` registration, neither built in this task.
- (12.2) Extends task 12.1's real-chain integration pattern with `RecipeDetailRepository`/`RecipeDetailService` (tasks 8.1/10.1) and `registerMealSlotRoutes` (task 10.2) — reviewer independently cross-checked every new factory call's field names/parameter order against actual production source and confirmed exact matches. `day-regeneration-recipe-detail.integration.test.ts` deliberately seeds the initial 7-day week via a DIRECT `menuPlanRepository.replaceWeek(...)` call (bypassing HTTP/Claude) rather than a real `/generate` request — a documented efficiency choice since weekly generation's HTTP path is already thoroughly proven by 12.1, keeping this file focused on its own 2 named flows; the fake `AnthropicMessagesClient` correspondingly only handles `DAILY_GENERATION_TOOL_NAME`/`RECIPE_GENERATION_TOOL_NAME` (throws on weekly, intentionally unused). **Day-regenerate's "residual 6 days unchanged" proof (Req 7.1/7.4)** is a genuine byte-for-byte deep-equal across each OTHER day's full `meals` array (dishName, food IDs, all 13 nutrition fields) PLUS `plannedKcal`/`targetKcal` against the ORIGINAL seeded values — not a superficial "still 4 meals" check; reviewer mutation-tested by corrupting a NON-target day's `dish_name` inside `MenuPlanRepository.replaceDay` and confirmed the assertion catches it immediately. **Recipe-detail's "same response" proof (Req 8.1/9.1)** asserts `supplementarySuggestions.length` is genuinely 1-2 from the SAME response object; reviewer mutation-tested by having the fake Claude client return 3 suggestions and confirmed the REAL (unfaked) `ClaudeMenuClient`'s own Zod schema (`.min(1).max(2)`, task 6.2) rejects it with a 502 before ever reaching `RecipeDetailService` — proving this integration test genuinely relies on and cannot silently bypass that existing enforcement layer. **Recipe-detail's "nutrition unchanged" proof (Req 8.3)** queries the target meal slot's real DB state both BEFORE and AFTER the recipe-detail call and asserts byte-for-byte identity; reviewer mutation-tested by injecting a fault into `RecipeDetailRepository.upsert`'s transaction that also mutates the meal slot's own `energy_kcal` and confirmed the assertion catches the corruption immediately (expected 652 vs. a corrupted 10651). Approved with one non-blocking documentation suggestion (Requirement 7.4's OTHER clause — `PlannedCalorieGateway.submitPlannedCalories` being called for the regenerated day — is correctly NOT re-proven here via the real `DailyLogService`, since that exact call-count/args/failure-swallowing/lock-interaction behavior is already thoroughly unit-tested with a fake `PlannedCalorieGateway` in `menu-plan.service.test.ts`'s own `regenerateDay` tests; unlike task 12.1's own explicit scope-narrowing documentation for an analogous omission, this one wasn't originally commented — fixed by the parent adding a one-line scope note directly in the test file before commit, no behavior change).
- (12.3) FINAL task in "Validation: 結合テストとエラーパスの検証" (completes task group 12). `error-paths-feedback.integration.test.ts` extends tasks 12.1/12.2's real-chain pattern to cover the 3 `generate` FAILURE paths 12.1 never tested (profile_missing/nutrition_unavailable/schema_validation_failed → 409/409/502, each independently confirmed to leave NO active plan persisted, closing the task's explicit "献立が永続化されない" requirement) plus CLOSING A GAP task 12.1 explicitly left open: the disliked-feedback-reaches-next-regenerate-prompt proof now goes through the REAL HTTP `POST .../feedback` endpoint (not a direct `feedbackService.recordFeedback` call, task 12.1's documented scope-narrowing) followed by a REAL HTTP `POST .../regenerate`, with the captured Claude request payload for that specific regenerate call asserted to contain the recorded dish name/food IDs. **The most technically involved piece, independently re-verified line-by-line by review against current source (not just the implementer's cited line numbers)**: the `nutrition_unavailable` trigger (`dietModeEnabled:true` + a null goal field → `incomplete_diet_mode_data`, confirmed in `nutrition.service.ts`'s actual `getSummary`) is UNREACHABLE via the normal `ProfileService.saveProfile` path (`ProfileInputSchema.superRefine` forces both goal fields non-null whenever `dietModeEnabled` is true) — so the test constructs this inconsistent state by calling `profileRepository.upsert(...)` DIRECTLY, bypassing ONLY the Zod validation layer (no production code touched), following an already-real precedent from `nutrition-engine`'s OWN `nutrition.summary.integration.test.ts` (task 5.4 of that spec) that review independently confirmed is genuine, not fabricated or misremembered. Review went further than reading: it wrote and ran (then deleted) a throwaway scratch test calling `profileService.saveProfile` with the SAME inconsistent input and empirically confirmed Zod genuinely rejects it — proving the bypass is truly necessary, not just a habitually-copied shortcut. `registerMealSlotRoutes`/`RecipeDetailService` are wired with fully real (not faked) dependencies purely as necessary plumbing to register the feedback route, with recipe-detail generation itself never exercised in this file — confirmed not a hidden shortcut. Requirement 12.5 (`generation_in_progress`) was correctly judged already sufficiently covered by existing Service-level lock tests (`menu-plan.service.test.ts`, tasks 9.1/9.2) and NOT redundantly re-tested here, per the parent's explicit guidance to avoid padding. Reviewer ran 3 independent mutation tests (an unconditional pre-check `replaceWeek` call simulating a partial-persistence bug on a failure path; a no-op feedback route; the reverted-bypass `saveProfile` empirical check described above), each behaving exactly as needed to prove the assertions have real teeth. Zero findings, approved in a single round. **This completes task group 12 — all of `menu-plan.service`'s HTTP-triggered generation/regeneration flows (weekly success, daily-scoped isolation, recipe-detail non-mutation, and now all 3 failure reasons plus the full real feedback→regenerate loop) are now proven end-to-end through the real full-stack chain established across tasks 12.1-12.3, with only the Anthropic Messages API ever faked.**
- (13.2) FIRST task in "Integration: 買い物リスト・外食代替提案". `category-display-groups.data.ts` exports `resolveDisplayGroup(category): DisplayGroup` (4-value literal union, independently confirmed by review to be character-for-character identical to design.md's `ShoppingListItem.category` type) mapping all 10 REAL seeded `food_items.category` values — independently re-derived by review via a from-scratch regex parse of `010_seed_food_items.sql` (not trusting either the implementer's or task 2.1's own summary comments): 主食36/肉類28/魚介類45/卵類8/乳類20/豆類24/野菜類86/果実類28/調味料類58/菓子類28=361, exact match. 6 of the 10 map "obviously" (肉類/魚介類→肉・魚; 卵類/乳類/豆類→乳製品・卵・豆; 野菜類→野菜・きのこ; 調味料類→調味料・その他 via literal text match); 4 required real judgment calls, each documented in-code with WHY: 果実類 (fruit) → 野菜・きのこ, reasoning that real Japanese supermarkets group vegetables+fruit into one 青果 (produce) section — the SAME real-shelf-placement logic `010_seed_food_items.sql` itself already used to fold いも類/きのこ類/藻類 into 野菜類, making this a consistent extension of an already-established precedent, not an arbitrary new one; 主食 (staples: rice/bread/noodles) and 菓子類 (snacks/confectionery) → 調味料・その他, since neither fits ANY of the 3 substantive groups and the catch-all is the only defensible landing spot — deliberately invoking Requirement 14.5's fallback rationale rather than treating it as an accidental gap. Review independently assessed all 4 judgment calls and could not construct a more natural alternative for any of them. **Test-quality point specifically targeted by review's mutation testing**: the "every real category resolves to a valid group" check alone would be too weak to prove CORRECT (as opposed to accidentally-fallback) mapping for any individual category — review confirmed the test suite avoids this trap by ALSO having individual/per-category exact-value assertions (not just membership-in-4-values checks), demonstrated by deleting the `魚介類→肉・魚` map entry and confirming exactly the dedicated `魚介類` test failed while the generic "resolves to one of 4" test still passed — proving the specific assertions are genuinely doing discriminating work, not redundant with the general one. Real DB used for the "all categories resolve" test (not a hardcoded list that could silently drift from future seed-data changes), matching this codebase's established `createConnection`+`runMigrations` real-DB test convention. Zero findings, approved in a single round. **Forward note for task 13.1** (not yet started, will consume this file): `resolveDisplayGroup` is the single needed export — call it with each food's `category` (from `FoodCompositionRepository`) to get `ShoppingListItem.category` directly.
- (13.1) `shopping-list.service.ts` implements `createShoppingListService(menuPlanRepository, foodCompositionRepository, unitConversionService).buildForWeek(weekStartDate): ShoppingList | null`. Since `MealSlot.ingredients` only carries raw `{foodId, quantity, unit}` (never pre-computed grams, despite design.md's own sequence-diagram prose loosely implying otherwise), the Service calls `UnitConversionService.toGrams` itself for all 28 meal slots' ingredients, accumulating a running total per unique `foodId` across the whole week — `toGrams`/`findById`/`findUnitConversion` failures are all treated as unreachable invariant violations (thrown, not wrapped in `Result`), matching the established precedent that this data was already successfully verified once at generation time. **A genuine, real gap closed via a narrow, documented extension**: `food_items.display_unit_code` existed in the DB schema and was seeded (task 2.1) but was never exposed by `FoodCompositionRepository` (task 3.1) — added `displayUnitCode: string | null` to `FoodItemNutrition` and one column to `findById`'s SELECT, touching NOTHING else in that file, matching this spec's own established "narrowly extend an already-built component only when the data exists nowhere else" pattern (tasks 5.1, 8.1). 0.5-rounding for foods with a set `displayUnitCode` correctly floors to 0.5 (never displays a bare 0) per Requirement 14.7; foods without one use raw grams as-is with `displayUnit:"g"`. **Round-1 REJECTED on exactly one finding, and the most consequential type-placement finding in this spec since task 4.2**: `ShoppingListItem`/`ShoppingList` were initially defined as LOCAL interfaces in `shopping-list.service.ts`, reasoning (plausibly but incorrectly) that design.md's lack of a dedicated `#### ShoppingListService` "Contracts: API[x]" subsection meant these types should stay Service-local like genuinely-local types (`FoodItemNutrition`, `VerificationError`). Reviewer disproved this with concrete, spec-wide evidence: design.md's own File Structure Plan explicitly lists `ShoppingList` in the SAME shared-type list as `WeekMenuPlan`/`DayMenu`/`MealSlot`/`RecipeDetail`/`FeedbackInput` (independently confirmed 5-for-5 that ALL of those other listed types are indeed already real shared Zod schemas), AND `RecipeDetailService` — carrying the IDENTICAL "no API[x] on its own Contracts marker" pattern the implementer used to justify local placement — had its own `RecipeDetail`/`SupplementarySuggestion` correctly promoted to shared types in task 10.1, directly disproving the local-placement heuristic. Fixed in round 2: promoted `ShoppingListItemSchema`/`ShoppingListSchema` (plus a new `ShoppingListCategorySchema` 4-value enum, since `shared` cannot import `DisplayGroup` from `server`) into `shared/src/menu.schema.ts`, with `shopping-list.service.ts` now importing rather than declaring these types — round-2 review independently re-verified the promoted schemas match round-1's already-approved field shapes exactly, confirmed the new enum is codepoint-identical to `DisplayGroup`'s values, and LIVE mutation-tested the cross-package safety net itself (deliberately desyncing one enum value in `shared`, confirming `tsc` immediately fails at the exact two `ShoppingListItem`-construction call sites, then restoring). **A second small round-2-review-flagged (non-blocking) duplication was ALSO closed by the parent before commit**: `category-display-groups.data.ts`'s `DisplayGroup` type is now a direct re-export alias of the shared `ShoppingListCategory` (`export type DisplayGroup = ShoppingListCategory`) rather than an independently-hand-maintained duplicate 4-value union — eliminating the last remaining sync-by-hand risk the reviewer's own live mutation test had already proven was merely compile-time-safe, not drift-free. **Escalation flagged by review, recorded here for `/kiro-validate-impl menu-generation`**: design.md never expanded `ShoppingListService`/`EatingOutSuggestionService` into full `####` Domain subsections (only Components-table summary rows exist) — the root cause of this round's type-placement mistake, and likely to resurface identically for `EatingOutSuggestionResult` at task 13.5 unless proactively watched for. Also: no task in `tasks.md` (13.1 through 14.3, read in full by review) currently owns adding `ShoppingList`/`EatingOutSuggestionResult` to `shared/src/menu.schema.ts` explicitly — this task closed its own half of that gap already, but task 13.5's implementer should be briefed on this exact precedent before starting, not left to discover it independently.
- (13.3) Extends task 9.3's `menu-plan.routes.ts` with `GET /api/menu-plans/:weekStartDate/shopping-list`, correctly reading the task text's "カテゴリ別グルーピング済み" as "each item carries its own correct `category` label" (per `ShoppingList.items`' genuinely FLAT array shape, task 13.1) rather than inventing a category-nested response shape — reviewer independently confirmed via the actual shared schema and via a mutation test (reshaping the response into a nested object, caught by the flat-shape assertion). Reuses the file's existing Monday-only `weekStartDate` Zod validation (independently re-proven to apply to THIS specific new route via a dedicated mutation test disabling it only here). `shoppingListService` was made a REQUIRED (not optional) positional parameter of `registerMenuPlanRoutes`, with `app.ts`'s registration gated on `menuPlanService && shoppingListService` BOTH being present (an all-or-nothing, whole-file registration gate) — reviewer confirmed this is genuinely consistent with the already-established `recipeDetailService && feedbackService` dual-gate precedent for `registerMealSlotRoutes` (not an invented pattern) and correctly grounded in design.md's own explicit P0 marking of `ShoppingListService` as a `MenuPlanController` dependency; flagged as a non-blocking FYI that this is slightly less granular than per-route gating (4 of the file's 5 routes don't touch `ShoppingListService`) but a defensible, precedented convention choice, not a defect. `AppRouteDependencies` keeps both fields optional at the TOP level (confirmed `index.ts` still calls `registerRoutes` with neither), preserving the already-documented "no task yet wires these Services' own dependency graphs into `index.ts`" gap. Reviewer ran 3 live mutation tests (null-response reshaping, category-nesting reshaping, per-route Monday-check bypass), all caught — with one notable process moment: a reviewer mutation-testing step accidentally ran `git checkout --` which briefly reverted the implementer's own uncommitted diff before being caught and fully restored from a pre-mutation backup; the parent independently re-verified the restored working tree's diff stats/content matched the implementer's original report before proceeding, out of an abundance of caution around any git-history-touching mishap during review. Zero blocking findings, approved in a single round.
- (13.4) `eating-out-reference.data.ts` exports `EATING_OUT_REFERENCE_DATA` (37 curated entries), each an `EatingOutReferenceEntry` with fields BEYOND the `EatingOutSuggestion` result-type shape — `mealType` (real 4-value enum, for Requirement 15.5's per-mealType filtering) and `ingredientTags: string[]` (for Requirement 15.4's NG-ingredient exclusion, a documented judgment call: combines BOTH the typical AND alternative menu's main ingredients into ONE array, reasoning a user's NG ingredient should hide the whole suggestion regardless of which side of the pair it appears on) — neither of which exists on the design.md `EatingOutSuggestion` RESULT type, since that's produced only AFTER filtering/selection (task 13.5's job, not this one). Real-sourced data, independently spot-checked by review against 12 named-chain figures across 8 chains (吉野家/松屋/やよい軒/マクドナルド/KFC/CoCo壱番屋/丸亀製麺/ミスタードーナツ/セブンイレブン/セブンプレミアム) via the reviewer's OWN separate WebSearch queries (not just re-reading the implementer's cited sources) — all matched exactly or within trivial (<1%) normal cross-aggregator rounding variance, zero fabrication found; entries using general dish-type estimates (ラーメン系統/パスタ/カレーライス-adjacent items/sweets) are honestly labeled as such in-file, not misrepresented as chain-verified. All 37 entries independently re-parsed and re-verified by review (via a script, not eyeballing) to satisfy `alternativeMenuKcal <= typicalMenuKcal` with zero violations; reviewer mutation-tested by flipping one real entry's value and confirming the guarding test genuinely fails. Coverage independently confirmed: mealType lunch=11/dinner=10/breakfast=8/snack=8 (all 4 types represented, not lunch/dinner-only), 30 distinct dish names, 0 duplicate (typical, alternative, mealType) rows — genuine scene diversity, not cosmetic repetition. **A deliberate deviation from the task text's literal field list, judged reasonable by review**: stores `typicalMenuProteinG`/`alternativeMenuProteinG` as two positive absolutes rather than a single signed "たんぱく質増分" (protein-increment) delta field — justified since a stored delta alone would be strictly less informative, and this task's OWN testing requirements (positive-value integrity checks) couldn't be satisfied by a delta that can legitimately be negative; task 13.5 derives `proteinDeltaG = alternativeMenuProteinG - typicalMenuProteinG` in one line when producing the actual `EatingOutSuggestion` result. **An important, explicitly-escalated (not blocking) finding requiring awareness before task 13.5/13.6 are briefed or accepted**: 17 of 37 entries (46%) have `alternativeMenuProteinG < typicalMenuProteinG` — i.e., the "healthier" alternative actually contains LESS protein than what it replaces (e.g. 親子丼→かけうどん: -18.5g; ビッグマック→ハンバーガー: -13.1g) — contradicting the literal "たんぱく質増分" (protein INCREMENT) wording used to describe this field in both this task's text and its own informal naming. Reviewer traced this against the actual requirements and design.md and confirmed it does NOT block acceptance: task 13.4's own cited requirements are 15.1/15.2/15.7 only (NOT 15.3, which governs what gets reported to users — that's task 13.5's scope); design.md's `EatingOutSuggestion.proteinDeltaG` is explicitly defined as an UNCONSTRAINED signed difference (no positivity requirement anywhere in the spec); and Requirement 15's own Objective text says "低カロリー**または**栄養バランスの良い" (low-calorie OR nutritionally-balanced — an OR, not an AND) — meaning a pairing selected purely for being lower-calorie, even with a protein decrease, is a legitimate, in-scope outcome per the spec's own stated value proposition, not a data defect requiring re-curation. **Recorded here explicitly so task 13.5's implementer is briefed on this upfront** (proteinDeltaG can and will legitimately be negative for ~46% of real selections — this is expected, by-design behavior, not something to filter out or treat as an error) rather than being surprised by it mid-implementation or silently "fixing" it by re-curating already-reviewed, source-verified data. Zero blocking findings, approved in a single round.
- (13.5) `eating-out-suggestion.service.ts` implements `createEatingOutSuggestionService(menuPlanRepository, profileGateway).suggestForMealSlot(weekStartDate, dayIndex, mealType)` — genuinely SYNCHRONOUS (no `Promise`/`async`, confirmed by review, matching Requirement 15.7's "no Claude API call" constraint), the interface INFERRED from the sequence diagram + task text since design.md (as already flagged during task 13.1's review) has no full `#### EatingOutSuggestionService` Domain subsection for this component, only a Components-table row. **A deliberate, well-justified DIVERGENCE from `RecipeDetailService`'s (task 10.1) resolution for its own nominally-similar P1 `ProfileGateway` dependency**: a `null` profile here is NOT an error — `ngIngredients` defaults to `[]` (no NG-restriction applied) and the function proceeds with the full candidate set, since (unlike `RecipeDetailService`, where the profile was ESSENTIAL to prompt construction) here the profile is used ONLY for an OPTIONAL filtering refinement; review independently confirmed both that requirements.md's Requirement 15 (this task's own cited scope) never mentions 12.1/profile_missing AND that the sequence diagram has no profile-null branch (unlike the weekly-generation flow's explicit one), then empirically confirmed via mutation test (reverting to throw-on-null) that 6 of 8 tests catch a regression here. NG-ingredient exclusion (Requirement 15.4) uses EXACT string-equality matching between `entry.ingredientTags` (task 13.4) and `profile.ngIngredients` — a reasoned, documented choice given no fuzzy-Japanese-ingredient-matching precedent exists anywhere else in this codebase (NG-ingredient handling elsewhere is Claude-prompt-based, not string-matching) — reviewer mutation-tested this too (1 of 8 tests catches its removal). Selection correctly picks the candidate with the HIGHEST `alternativeMenuKcal` among those `<=` the meal slot's `energyKcal` (closest-without-exceeding, Requirement 15.2) — reviewer mutation-tested BOTH the highest-vs-lowest direction (5/8 tests catch a swap) AND the `<=`-vs-`<` boundary-equality condition specifically (4/8 tests catch it, including one exercising an EXACT equality case: `alternativeMenuKcal === energyKcal`), confirming the boundary itself is genuinely covered, not just the interior cases. A real, already-known-negative `proteinDeltaG` entry (親子丼→かけうどん, task 13.4's own review-flagged example: 9.5−28.0=−18.5) is exercised end-to-end and asserted exactly, confirming negative protein deltas pass through uncorrected/unfiltered as intended. **Proactively closed the exact gap task 13.1 was REJECTED for in its own round 1**: `EatingOutSuggestionSchema`/`EatingOutSuggestionResultSchema` were added to `shared/src/menu.schema.ts` FROM THE START (not as a later remediation), with the service importing rather than declaring these types locally — reviewer independently confirmed the field shapes match design.md's interface block exactly and that `shared/src/index.ts`'s existing `export * from "./menu.schema.js"` needed no further registration change. Zero blocking findings, approved in a single round. **Worth recording as a small process note**: mid-review, the parent controller (resuming the reviewer after an org spend-limit interruption) asserted the working tree was clean based on checking only ONE of the reviewer's several in-flight mutations — the reviewer correctly did NOT take this at face value, independently re-verified the actual file content itself, found a DIFFERENT mutation (the NG-ingredient exclusion filter) still applied from before the interruption, reverted it properly, and only then completed its remaining checks and issued a verdict — the exact "verify, don't trust, even when the claim comes from the coordinator" discipline this project has consistently applied to implementer/reviewer claims, now also correctly applied to a parent-controller claim.
- (13.6) FINAL task in "Integration: 買い物リスト・外食代替提案" (completes task group 13). Extends `meal-slot.routes.ts` with a THIRD route (`eating-out-suggestion`, joining `recipe-detail`/`feedback`), correctly reading the task text's "候補なし（200 + null）" shorthand as `200 + {"suggestion": null}` (a JSON OBJECT, per the real shared `EatingOutSuggestionResultSchema`), NOT a bare `null` body — contrasting deliberately with `ShoppingList`'s route (task 13.3), which DOES return literal `null`; reviewer mutation-tested this distinction (temporarily returning the bare `.suggestion` value) and confirmed 3 assertions across 2 test files catch it. Since `EatingOutSuggestionService.suggestForMealSlot` is genuinely synchronous with only `NotFoundError` as a possible failure (no `GenerationError` at all, task 13.5), this route correctly has NO 409/502 branch — reviewer confirmed the handler never references the file's own `generationErrorStatus`/`CONFLICT_REASONS` helpers (which stay scoped to the `recipe-detail` route only), genuinely simpler rather than copy-pasted-with-a-dead-branch. Reuses the file's existing shared Zod validation (no new schema). A real, legitimately negative `proteinDeltaG` value (task 13.4's own 親子丼→かけうどん figure, -18.5) passes through the success response unmangled and asserted exactly — confirming this route correctly does NOT special-case or reject negative protein deltas as errors. `app.ts`'s registration gate was widened from 2-way to a genuine 3-way AND (`recipeDetailService && feedbackService && eatingOutSuggestionService`) — reviewer independently mutation-tested by loosening it back to 2-way and confirmed the dedicated "third service alone missing → 404" test catches it, proving that test is real and load-bearing, not vacuous. **One acknowledged, independently-scrutinized process deviation**: `app.test.ts`'s wiring tests were written AFTER (not before) the `app.ts` gate change, breaking strict RED-phase discipline for that one file (unlike `meal-slot.routes.ts`'s own correctly-sequenced TDD cycle) — reviewer judged this an acceptable, narrow deviation given the change's small/mechanical/already-precedented nature, and independently substantiated the SUBSTANCE of the concern (are the resulting tests actually meaningful, not just present) via its own mutation test rather than accepting the implementer's justification at face value. **A minor, non-blocking self-report accuracy issue caught by review**: the implementer's claimed test-count breakdown ("10 new route tests + net +3 app.test.ts tests") didn't match reviewer's own re-measurement (actual: +6 route tests, net +2 app.test.ts tests, from a rewritten describe block removing 3 old tests and adding 5) — though the AGGREGATE total (+8, matching 914→922) was accurate, only the internal attribution was off; flagged purely for self-report-tracking purposes, no coverage or code defect resulted. Zero blocking findings, approved in a single round. **This completes task group 13** — the full shopping-list and eating-out-substitution feature surface (data → service → HTTP) is now built and unit-tested end-to-end; only task group 14's test-expansion/integration-test tasks remain before the whole `menu-generation` spec reaches its final `/kiro-validate-impl` gate.
- (14.1) FIRST task in the FINAL "Validation: 買い物リスト・外食代替提案のテスト拡充" group. A rigorous, requirement-by-requirement audit found 5 of 6 cited requirements (14.2/14.3/14.5/14.6/14.7) ALREADY genuinely, thoroughly covered by task 13.1's original suite — including Req 14.3's coverage, which lives only INCIDENTALLY inside an unrelated-sounding `describe` block (a discoverability nit noted but correctly left as-is, not padded with a redundant standalone test). Initial round added ZERO new tests, a legitimate but UNUSUAL-enough outcome that review gave it deliberately heavy scrutiny (mutation-testing the two most uncertain claims rather than accepting the "already adequate" reasoning at face value) — and this scrutiny paid off: **Req 14.4's POSITIVE/known-category wiring path through `buildForWeek` (as opposed to `resolveDisplayGroup` in isolation, already covered by task 13.2's own file) turned out to be a genuine, previously-undetected gap.** Reviewer proved this by hardcoding `shopping-list.service.ts`'s `category = resolveDisplayGroup(food.category)` line to a constant fallback value and finding ALL 922 existing tests (including every test that happens to use a real mapped category like `"野菜類"`) still passed — because the ONLY test asserting on `item.category` (the Req 14.5 undefined-category fallback test) produces an output value INDISTINGUISHABLE from the mutant's hardcoded constant, so it couldn't catch this specific class of bug. Fixed in round 2 with one new dedicated test (`category: "肉類"` → asserts `"肉・魚"`, a genuinely different, distinguishable mapped value) — round-2 review independently re-reproduced the EXACT same mutation against the fixed suite (not trusting the implementer's report) and confirmed it's now caught by precisely the new test while the Req 14.5 test remains unaffected, proving the two tests exercise genuinely distinct code paths, not overlapping/redundant coverage. **This is a valuable, generalizable lesson for this spec's remaining test-expansion work and any future one**: a test asserting "category X falls back to the catch-all" can accidentally provide ZERO regression protection for "category Y correctly resolves to its OWN distinct group" if the catch-all's output value happens to coincide with what a broken/hardcoded implementation would also produce — the two properties (fallback-works, positive-mapping-works) are NOT interchangeable and must each be independently, distinguishably tested. Approved after 2 rounds.
- (14.2) Second test-expansion task in the final group — **directly confirmed the exact same class of gap task 14.1 had just found**, this time flagged UPFRONT in the task briefing (rather than discovered only via review) precisely because the pattern was now recognized: Requirement 15.2/15.4 were already genuinely, adequately covered by task 13.5's original suite (verified independently by review reading the actual assertions, not just trusting test names) — but the existing "候補が0件になる場合（Req 15.5）" test used a CALORIE-filtering-causes-zero scenario (breakfast, energyKcal=5, below the cheapest alternative), a DIFFERENT code path than what Requirement 15.5's own literal text specifically names: "NG食材の除外によって...候補が残らない場合." Closed with one new test: `snack` mealType (all 8 real entries), `energyKcal:999` (deliberately generous — independently confirmed by review to exceed every snack entry's `alternativeMenuKcal`, max 150, so calorie-filtering alone excludes NOTHING), `ngIngredients` set to the verified-complete union of all 8 snack entries' `ingredientTags`, exhaustively excluding every candidate via NG-exclusion ALONE — isolating the exact mechanism 15.5 names. **Both the implementer AND review independently reproduced the same mutation test** (neutralizing the NG-exclusion filter into a no-op) and got IDENTICAL results: the OLD calorie-based zero-candidates test still PASSED (confirming zero regression protection against a broken NG-exclusion path), while the NEW test correctly FAILED — review additionally verified byte-for-byte production-file restoration via md5 hash comparison (not just `git diff`), the first use of this stronger verification technique in this spec's review history. Zero findings, approved in a single round on the first attempt (unlike 14.1's 2 rounds) — directly crediting the prior task's lesson being proactively applied at BRIEFING time rather than discovered reactively during review. **This is the SECOND and final confirmed instance of the "same final result (null/fallback), different causal mechanism" test-coverage trap in this spec** (after task 14.1's category-fallback case) — a now well-established, generalizable pattern: whenever a function can reach the SAME output (a null result, a fallback value, an empty list) via MULTIPLE independent code paths, each path needs its OWN distinguishing test, since a test for one path can accidentally provide zero protection for another.
- (14.3) **最終タスク完了**: `shopping-list-eating-out.integration.test.ts`を新規作成し、`menu-generation`スペック全38タスクが完了した。4テスト構成: (a) 買い物リスト集約・分類の実チェーン証明（同一実食品ID`'12004'`を3つの食事枠にまたがって使用し、手計算した合計値と一致することを確認。さらに3つの異なる`category`が正しく異なる表示グループへ分類されることも同一テストで確認）。(b) 要件14.6のnull寛容ケース（有効プランが存在しない週で`shopping-list`エンドポイントが例外を投げず`null`相当のレスポンスを返すことを実チェーンで確認）。(c) 要件15.3のフィールド構成証明（day3/dinner/energyKcal=700 → カルボナーラから和風きのこパスタへの代替提案、proteinDeltaG=-8.0という実データに基づく具体的な期待値で、外食メニュー名・カロリー・代替メニュー名・カロリー・たんぱく質増分の5フィールドすべてを検証）。(d) 要件15.6の実チェーンnot-foundケース（round 1で追加漏れが指摘され、round 2で追加: 既存の`NO_PLAN_WEEK_START`フィクスチャを再利用し、`GET .../eating-out-suggestion`が実DB+実`MenuPlanRepository`+実`EatingOutSuggestionService`+実HTTPを通じて404と`{type:"not_found", message}`を返すことを確認。メッセージ文字列は`eating-out-suggestion.service.ts`の実テンプレートから独立に再現。レビューはこのテストが本当に機能しているかをmutation testで確認済み——`if (!mealSlot)`分岐を無効化すると期待通り`500`になり失敗した）。**round 1→round 2の経緯**: round 1は他の3テスト（集約・分類、14.6のnullケース、15.3のフィールド構成）は全て承認したが、要件15.3の「外食代替提案」については実チェーンでフィールド構成のみ確認しており、要件15.6（プラン不在時の404）が実チェーンで一切カバーされていない非対称性を指摘してREJECTED。round 2でこの1テストのみを追加し、レビューが独自にコードを再読み・mutation testを再実施した上でAPPROVEDとなった。

  **実装作業中に新規発見した、タスク境界外の本番データ不整合（未修正、将来の専用データ修正タスクが必要）**: `food_items`の`'01034'`（ロールパン）は`display_unit_code='個'`が設定されているが、`011_seed_unit_conversions.sql`冒頭コメント（122-137行目付近）はこの組み合わせを「信頼できる出典が見つからなかったため意図的に未シード」と明記している。`ShoppingListService.buildForWeek`内の`findUnitConversion(foodId, food.displayUnitCode)`は、その食材が実際にどの単位で使われたかとは無関係に、食品の静的な`displayUnitCode`プロパティをキーに毎回この変換をルックアップするため、`'01034'`を含む週の買い物リストをリクエストすると`null`が返り例外がスローされ、`app.ts`の共通エラーハンドラがこれを不透明な500に変換してしまう（週全体の買い物リストAPIが丸ごと失敗する）。これは献立生成時にグラム単位で`'01034'`が指定されていても再現する（`toGrams`は`"g"`の場合はルックアップ自体をバイパスするため生成検証時点では問題が顕在化せず、買い物リストリクエスト時点で初めて失敗する）。本タスクの境界外のため、このテストファイル自体では`'01034'`の代わりに別の実食品ID`'11220'`を使う形で回避し、根本原因はここに記録するのみに留めた。既に記録済みの`index.ts`配線ギャップ（task 9.3/13.1）、週/日ロック独立性の競合可能性（task 9.2）と合わせて、`/kiro-validate-impl menu-generation`のGO/NO-GO判定で表面化させるべき3つ目の既知アーキテクチャ/データギャップとして扱う。

  **テスト戦略上の教訓（このタスク自体では未発生だが、task 14.1/14.2からの継続的教訓として記録）**: 本タスクの4テストはいずれも実チェーン経由の初出しの検証であり、既存のfakeベーステスト（`shopping-list.service.test.ts`, `eating-out-suggestion.service.test.ts`, `meal-slot.routes.test.ts`, `menu-plan.routes.test.ts`）と重複しないことをレビューが個別に確認済み。

- (`/kiro-validate-impl menu-generation` 1回目) **NO-GO**。38タスク全完了・全テスト成功（server 928/928, web 175/175）、TODO/secrets grepクリーン、要件73/73が何らかのタスクにマップ済み、タスク間契約・依存方向・shared型重複いずれも健全だったが、実機起動疎通テスト（`dist/index.js`を実際に起動し`curl`）により2件のCriticalな実行時欠陥を発見: (1) `server/src/index.ts`の`startServer()`がmenu-generationの5 Serviceを一切構築・配線しておらず、`GET /api/menu-plans/...`等が実サーバーではFastifyの汎用404（未登録ルート）を返す——テストは`buildApp()`を直接呼びこの配線をバイパスするため検出されなかった。tasks.mdのどのタスクもこの配線を担当範囲に含めておらず、実装ミスではなくタスク分割時の計画漏れ（9.3/10.2/12.1/13.1/13.3の各Implementation Notesが実装当時から繰り返し自己記録・エスカレート済み）。(2) `ShoppingListService.buildItem`が、`display_unit_code`設定済みだが対応する`unit_conversions`食材固有エントリを持たない食品に対し未処理例外を投げる——task 14.3は`'01034'`の1例のみ記録していたが、独立監査により`display_unit_code`設定済み152食品中73件（48%）が該当する広範な問題と判明（ロールパン・クロワッサン・缶詰魚・単品菓子等の一般食材を含み、まれな境界ケースではない）。この2件はいずれも一意に修正方針が定まる（配線作業そのもの／既存の要件14.7グラム表示フォールバックの拡張）ため、task 15.1/15.2として追加し是正サイクルへ進む。**是正しなかった第3の既知ギャップ**: 週/日ロックの独立性による競合（task 9.2で既にレビューエスカレート済み、要件12.5関連）は、妥当な設計解が複数存在し意図されたセマンティクスが要件文から一意に定まらないため、コードでの一存修正を避け記録に留める（人間の意思決定が必要）。NO-GO是正は`kiro-impl`の規約により最大3ラウンドまで。

- (15.2) `ShoppingListService.buildItem`の`findUnitConversion`失敗時の挙動を、未処理例外（→500）から要件14.7の既存グラム表示フォールバック（`displayQuantity: totalGrams`, `displayUnit: "g"`）への合流に変更した。両分岐（`displayUnitCode === null`／`findUnitConversion`が`null`）を共通の`gramDisplayItem()`ヘルパーに統合し、出力shapeが構造的に同一であることを保証。`findById`（food_id自体が存在しない場合）の例外は不変条件違反として従来通り維持（本タスクの対象外）。ファイル冒頭コメントの「task 2.2のシード保証により必ず存在する」という誤った主張を、実データ検証済みの正確な記述（152食品中73件・48%が該当）へ書き換えた。新規テスト2件（fakeベースの単体テスト、実データ`'01034'`ロールパンを`menuPlanRepository.replaceWeek`で専用週フィクスチャへシードし実HTTPチェーンで200を確認する結合テスト）はレビューが独自にライブmutation test（例外へ差し戻し→2テストのみ失敗確認→復元）で負荷担保性を確認済み。食品シードデータ自体（`010_seed_food_items.sql`/`011_seed_unit_conversions.sql`）は変更していない（task 2.1/2.2の「未確認の換算値は捏造しない」方針を維持）。全930/930（14.3時点928から+2）。

- (15.1) `server/src/index.ts`の`startServer()`に、menu-generationの5 Service（`MenuPlanService`/`ShoppingListService`/`RecipeDetailService`/`FeedbackService`/`EatingOutSuggestionService`）とその依存グラフ全体（`FoodCompositionRepository`→`UnitConversionService`→`MenuPlanRepository`/`RecipeDetailRepository`/`FeedbackRepository`→`NutritionVerificationService`→`ClaudeMenuClient`→3つのmenu-generation専用Gateway（`nutrition/*.gateway.ts`との名前衝突を避けるため`createMenuProfileGateway`/`createMenuNutritionGateway`にエイリアスimport）→`FeedbackService`→各Service）を実際に構築し配線した。既存の`profileService`/`dailyLogService`/`nutritionService`/`db`はそのまま再利用し、`app.ts`・各Service/Repository/Gateway/routeファイルは一切変更していない（純粋な合成ルートの配線タスク）。実機起動疎通（実際にビルドした`dist/index.js`を起動し`curl`）で、`GET /api/menu-plans/:weekStartDate`が既存契約どおり200+null、`GET .../eating-out-suggestion`がアプリの`NotFoundError`ハンドラ経由の404+`{type:"not_found",...}`（Fastify純正の汎用404ではない）を返すことを確認。**レビューが「テストがbuildApp()を直接呼び配線をバイパスすることで今回のNO-GOが見逃された経緯を踏まえ、同種の退行を自動テストで捕捉できない」と非ブロッキングで指摘したため**、`index.ts`の既存の配線タスク（task 6.1/4.2）が確立した「`startServer()`を実際に起動しHTTPで到達性を検証する `(task N.M observable completion condition)` 」規約に倣い、`index.test.ts`に自動回帰テストを追加した（`GET /api/menu-plans/:weekStartDate`→200+null、`GET .../eating-out-suggestion`→404+`{type:"not_found"}`、既存`/api/profile`→200を同一起動インスタンス上で確認）。自らライブmutation test（`registerRoutes`呼び出しから5フィールドを一時的に削除→新規テストのみ失敗確認→復元）で負荷担保性を確認済み。あわせて`index.test.ts`内の「menu-generationの独自ルートはまだ配線されていないため」という陳腐化したコメントも修正した。全931/931（15.2時点930から+1）。**これで38タスク＋是正2タスク（15.1/15.2）が全て完了し、`menu-generation`スペックは実機到達性まで含めて完了した。** 未解決として記録済みの第3のギャップ（週/日ロックの独立性による競合、要件12.5関連、task 9.2）は、複数の正当な設計解が存在し要件文からセマンティクスが一意に定まらないため、本ラウンドでは一存修正せず記録に留めている——次の`/kiro-validate-impl menu-generation`再実行時にMANUAL_VERIFY_REQUIREDまたは非ブロッキングな既知事項として扱う想定。

- (`/kiro-validate-impl menu-generation` 2回目・最終) **GO**。1回目のNO-GO是正（task 15.1/15.2）を、独立した別視点の検証エージェントが最初から全て再検証: (1) `index.ts`配線をソース読解＋実際にビルドした`dist/index.js`の実機起動＋curlで独立確認（`GET .../shopping-list`→200+null、`GET .../eating-out-suggestion`→404+`{type:"not_found",...}`をFastify純正404と対比しながら確認）。(2) `ShoppingListService`の`'01034'`グレースフルデグレードをソース読解＋実データ（`010_seed_food_items.sql`/`011_seed_unit_conversions.sql`）の独立確認＋実結合テストの実行で確認。ルート全体のテスト（`npm run test`）1305件成功（shared 199 / server 931 / web 175）、`npm run build`・`npm run typecheck`いずれも成功、TODO/FIXME/secrets grepクリーン、`_Blocked:_`なし、全40リーフタスク（1.1〜15.2）が`[x]`。**未解決の第3の既知ギャップ（週/日ロックの独立性による競合、要件12.5、task 9.2）についても独立に再検討**した上で、要件文が異なる粒度間の排他制御までは一意に要求しておらず、意図的に人間判断へ委ねられた項目として適切にエスカレート・記録済みであり、影響も「後勝ちの上書き」に留まる（データ破損ではない）ことから、これを理由にNO-GO/MANUAL_VERIFY_REQUIREDとする必要はないと判断——GOとして扱う既知の技術的負債として記録を継続する。**`menu-generation`スペック（全38タスク＋是正2タスク）はこれで完了。**

- (2.1) `010_seed_food_items.sql` seeds 361 REAL food items (主食36/肉類28/魚介類45/卵類8/乳類20/豆類24/野菜類86/果実類28/調味料類58/菓子類28) sourced by bulk-downloading MEXT's own official Excel primary source (「日本食品標準成分表（八訂）増補2023年」本表, `mext.go.jp`) and parsing all 2,538 rows programmatically — NO number was hand-typed. Independently re-verified by review at exceptional depth: a from-scratch re-parse of the same Excel diffed against all 361×13=4,693 committed nutrient values (0 mismatches) AND all 361 food-name comments against the official 食品名 (0 mismatches, catching a wrong-food-selected error a pure number-diff couldn't), PLUS 15 additional items independently cross-checked against the separate `fooddb.mext.go.jp` surface spanning all 10 categories (all matched). A genuine 七訂→八訂 methodology-change discrepancy was caught and correctly resolved (鶏むね肉皮なし: some secondary sites still show the superseded 七訂 value 116kcal; the real current 八訂 value, confirmed independently by review, is 105kcal — the seed correctly uses 105). **Non-obvious data-integrity nuance**: micronutrient columns are legitimately NULL only where the source itself has no measured value (`-`), never a guess; `(数値)`-prefixed estimated values and `Tr`(trace)→0 were used AS MEXT PUBLISHES THEM, not reinterpreted. **Boundary note**: seeding real MEXT food_ids collided with ONE pre-existing dummy fixture in `migrate.test.ts` (`'01004'`, now genuinely オートミール) — fixed by renumbering all 8 placeholder fixture IDs into a `99xxx` range (MEXT's real food groups only span 01-18, permanently collision-proof), a real, assertion-preserving, independently-confirmed necessary fix, not scope creep. **Judgment calls, independently assessed by review as reasonable but flagged for downstream tasks**: (1) non-named MEXT groups were folded into the closest of the 10 task-specified category labels (いも類/きのこ類/藻類→野菜類; 砂糖類/油脂類/ごま/でん粉→調味料類; ナッツ→菓子類 — reviewer's weakest-but-still-acceptable call, since nuts sit in the snack aisle for shopping-list purposes even though 豆類 is botanically closer; バター→乳類, correctly prioritizing shopping-list shelf-placement over a literal "seasoning" reading) — task 13.2 (category→shopping-list-display-group mapping) needs only a 10-entry map thanks to this closed, deliberately small label set. (2) `display_unit_code` follows a real-world-accurate nuanced rule, NOT a blanket "meat/fish=NULL": raw whole cuts are NULL, but genuinely piece-counted items — including some RAW cuts households actually count by 本 (鶏ささみ/手羽/手羽先/手羽元) as well as processed items (ロースハム=枚, ウインナー=本, 焼き竹輪=本, 固形ブイヨン=個) — carry a real unit code (152 of 361 rows, across 8 distinct codes: 個/本/枚/パック/玉/束/缶/丁). **Concrete, load-bearing obligation for task 2.2**: `unit_conversions` must supply a resolvable `(food_id, unit_code)` (or matching generic) entry for ALL 152 of these unit-code pairs — design.md's shopping-list flow divides grams by `unit_conversions.grams_per_unit` for exactly these rows, so any gap leaves that conversion unresolvable. **Lesson**: for a large real-data seeding task, a single implementer's own "I verified it" claim — even when methodologically sound (re-parsing the same source file) — is not sufficient; independent review must fetch from a SEPARATE, independent surface (not just re-run the same parser against the same file, which would agree even if the whole file were the wrong edition) to catch a class of error self-verification structurally cannot see.

- (16.1, amendment) `results-dashboard`のtask 4.2（レシピ詳細モーダル）実装中に、要件6.2「材料一覧（食材名と分量）」を満たせないギャップ（`IngredientSelection`がfoodIdのみで食材名を持たない）が判明し、要件4.8を追加する小規模なspec amendment（requirements.md/design.md/research.md/tasks.mdの再オープン、ユーザーへの3択提示の上「再オープンして修正」を選択）を経て実施。`MealSlot`/`IngredientSelectionSchema`自体（Claudeのtool呼び出し入力スキーマであり、この既にshipped済みのspec全体で広く使われる中核型）は一切変更せず、`RecipeDetail`（新設フィールド`ingredients: ResolvedIngredient[]`）と`SupplementarySuggestion.ingredients`の型のみを`ResolvedIngredient`（`IngredientSelection`+`name`）へ変更——`results-dashboard`側でこの型を消費するコードがまだ存在しない（task 4.2はこの直後に着手予定）ため、既存の表示ロジックを一切壊さない最小の変更範囲に収めた。永続化層（`recipe-detail.repository.ts`）は新しいローカル型`PersistedRecipeDetail`/`PersistedSupplementarySuggestion`（食材名を持たない、旧`RecipeDetail`/`SupplementarySuggestion`と同一形状）を導入し、SQL/ロジックは一切変更せず型注釈のみ変更——新規マイグレーション不要（`ShoppingListService`が`ShoppingListItem.name`を解決する既存パターンと同一の設計判断）。`recipe-detail.service.ts`は`FoodCompositionRepository`への新規依存を追加し、`recipeDetailRepository.upsert`には未解決の`ingredients`を渡した上で、戻り値を実際に呼び出し元へ返す前に主菜の`mealSlot.ingredients`と各補助副菜の`ingredients`の両方を`FoodCompositionRepository.findById(...).name`で名前解決してから合成する。この名前解決が安全である根拠（全てのfoodIdは既に存在確認済み——主菜側は元の献立生成時の永続化前検証、補助副菜側は同一関数内で先行する`nutritionVerificationService.verifyDish`呼び出し）をレビューが実際のコードの変数の同一性を辿って確認済み（`!`による非nullアサーションではなく、`shopping-list.service.ts`/`recipe-detail.repository.ts`と同じ「説明的なthrow」規約を踏襲）。`server/src/index.ts`は既存の`foodCompositionRepository`インスタンスを`createRecipeDetailService`の依存に1行追加するのみ。レビューが実際にfoodId↔食材名の取り違えを注入するmutation testで、新設された食材名解決専用テストが正しく検知することを確認（3テストが期待通り失敗→復元）。影響範囲は`shared/src/menu.schema.ts`・`server/src/menu-generation/recipe-detail.{service,repository}.ts`・`server/src/index.ts`と関連テスト7ファイル（`web/src/api/mealSlotClient.test.ts`のフィクスチャ更新を含む、`results-dashboard`のUIコンポーネント自体は無変更）に限定され、マイグレーションファイルは一切変更していない。全shared 207/207・server 932/932・web 292/292、typecheck/buildクリーン。

- (17.1, `NOT_AI`) ユーザーがAPI課金回避のため`AI_API`（Claude API版、無変更）と`NOT_AI`（本amendment適用先）の2独立コピーへ分岐。本タスクは今後のtask 17.2以降（非AI版`RuleBasedMenuGenerator`）を実装可能にする土台となる、挙動を一切変更しない純粋なリファクタリング。`server/src/menu-generation/menu-generator.ts`（新規）に構造化入力（`profile: MenuProfileSnapshot`, `targets: Record<IsoDate, NutritionTargetSnapshot>`, `dislikedSummary: DislikedItemSummary[]`）を受け取る`MenuGenerator`インターフェース（`generateWeek`/`generateDay`/`generateRecipe`）を定義し、`WeeklyGenerationToolResult`等の結果型・`ClaudeGenerationError`は`claude-menu.client.ts`から再利用（重複定義しない、AI固有ではなく「生成結果の形状」を表す型のため）。`generateDay`の`otherDays: OtherDayContext[]`・`generateRecipe`の`mealSlot: MealSlot & { id: number }`は、実際に`menu-plan.repository.ts`の`findOtherDays`/`findMealSlot`の戻り値型を確認した上でそのまま採用（レビューが独立に型定義元と突き合わせ確認済み）。`server/src/menu-generation/claude-menu.generator.ts`（新規）に`createClaudeMenuGenerator(claudeMenuClient): MenuGenerator`アダプタを実装し、既存のプロンプト構築（`menu-prompt.builder.ts`）+Claude呼び出しの2ステップをそのまま移動（`menu-prompt.builder.ts`/`claude-menu.client.ts`自体は一切変更せず、レビューが`git diff --stat`でゼロ差分を確認）。`menu-plan.service.ts`/`recipe-detail.service.ts`の`deps.claudeMenuClient`+個別プロンプト構築呼び出しを`deps.menuGenerator`への単一呼び出しへ統合し、分岐・エラーハンドリング・検証ロジックは一切変更していないことをレビューが差分を直読して確認。プロンプトのテキスト内容を検査していた配線検証テスト（苦手サマリ伝播・他日コンテキスト伝播）は構造化引数への直接アサーションへ書き換えたが、レビューが「同じデータのプロンプトテキストレベルのカバレッジは`menu-prompt.builder.test.ts`に既に別途存在し続けている」ことを確認した上で、検証の弱体化ではなく適切な責務再配置と判定。スコープ外だが実行時に必要だった4件の統合テストファイル（`createMenuPlanService`/`createRecipeDetailService`の依存注入変更への追随のみ、アサーション内容は無変更）、および`node_modules/@nutrition/*`のsymlink欠落を解消する`npm install`（`package-lock.json`に差分なし）も、レビューが機械的な追随・副作用なしと確認した上で許容。レビューは2件のライブmutation test（`dislikedSummary`引数を空配列に固定→伝播テストが正しく失敗、アダプタ内の`dislikedSummary`をClaudeへの実プロンプトから欠落させる改変→統合テスト2件が正しく失敗）で、この土台の配線が実際にテストで担保されていることを確認した上で復元。`AI_API`側は一切変更していないことを`git -C ../AI_API status --porcelain`で確認済み。全server 932/932、typecheck/buildクリーン。

- (17.2, `NOT_AI`) `server/src/menu-generation/rule-based-recipe.data.ts`（主菜48件、各mealType 12件）・`rule-based-side-dish.data.ts`（副菜20件、各mealType 5件）を新規作成。実装前に`010_seed_food_items.sql`（361件の実在食品ID）を実際に読み、存在しないfoodIdを一切使わないことを徹底——専用のテスト（`food-composition.repository.test.ts`と同じ実SQLite一時DBパターン、モックなし）で全68件・全ingredientsのfoodIdが実在することを機械的に検証（レビューが361件のリストを自分でも把握した上で全数突き合わせを実施し、不一致0件を確認）。`restrictionSuitability`（`low_carb`/`low_fat`/`high_protein`/`calorie_only`/`none`）は各料理の実際の栄養価から誠実に判定——レビューが実装コードとは独立に68件全料理のPFC・カロリーをNode.jsスクリプトで再計算し、`low_carb`表示料理の炭水化物最大6.9g、`low_fat`表示料理の脂質最大10.7g等、申告値と矛盾がないことを確認。`dishName`68件の一意性も全数確認済み（重複0件）。**round 1でREJECTED**: `tags`（NG食材フィルタ用、`RuleBasedMenuGenerator`が完全一致で除外判定に使う前提）で、foodId`01088`（ごはん、120〜180g、料理の主要食材）を含む68件中13件（昼食5件・夕食8件）で"米"タグが系統的に欠落していることをレビューが発見（同程度の米使用量の他料理では正しくタグ付けされており、意図的な省略ではなく単純な付け漏れと判定）。round 2で該当13件全てに"米"タグを追加し、実装者自身が要求された全数再監査でさらに1件（「牛肉と豆腐のすき焼き風煮」のfoodId`02005`しらたき）の同種のタグ漏れを自主的に発見・修正、回帰防止テスト（「foodId 01088を含む全エントリは"米"タグを持つ、かつ対象0件では通過しない」）も新規追加。レビューが13件全ての修正・新規回帰テストのmutation test（既存エントリから"米"タグを一時削除→正しく失敗→復元）を再確認した上でAPPROVED。レビューは独自の全数再監査（食品成分DBのカテゴリ情報を機械的に突き合わせ）で新たに小規模な懸念（"しょうが"、foodId`06103`、2件・各5gの薬味量、うち1件は料理名に「生姜」を含む）を発見したが、米タグ問題（主要食材の系統的欠落）とは性質が異なる軽微な薬味レベルの分量であるため、ブロッキングとせず今後の任意対応として記録するに留めAPPROVED——将来この2件に"しょうが"タグを追加する軽微な改善余地が残っている。調味料類（しょうゆ/みりん/砂糖/油/だし/わさび/トウバンジャン等）は両ファイル全体で意図的・一貫してタグ付けを省略する方針（NG食材フィルタ対象外）であることをレビューが独自の全数監査で確認済み。全server 960/960、typecheckクリーン。

- (17.3, `NOT_AI`) `server/src/menu-generation/rule-based-menu.generator.ts`+`.test.ts`を新規作成。`(dayIndex, mealType)`スロットごとに4段階のフィルタで選定する: (1)必須フィルタ（mealType一致+NG食材タグの完全一致除外+苦手料理除外、`eating-out-suggestion.service.ts`の既存precedentに倣う。0件でも緩和しない）、(2)食事制限フィルタ（`restrictionSuitability`、0件ならフォールバックで解除）、(3)バラエティフィルタ（週内/他日で既に選んだ`dishName`回避、0件ならフォールバックで解除）、(4)注入可能な乱数関数で1件選定。候補が(1)の時点で0件という極端なケースのみ`dayIndex`/`mealType`を含む`schema_validation_failed`エラーを返す。分量スケーリングは朝食25%/昼食30%/夕食35%/間食10%という一般的な配分目安で1日の`calorieTarget`を配分し、`FoodCompositionRepository`から算出した素の合計kcalとの比率を[0.5, 2.0]にクランプして適用。**round 1でREJECTED**: `MenuGenerator.generateWeek`が受け取る`targets: Record<IsoDate, NutritionTargetSnapshot>`（日付キー、dayIndexを直接持たない）から`Object.keys(targets).sort()`でdayIndexを復元する設計（`menu-plan.service.ts`の実際の構築順序・`addDaysIso`の固定幅ISO日付文字列という前提に基づく、レビューが月またぎ・年またぎ・うるう年境界を含む6ケースで独立検証し正当と確認済み）自体は正しかったが、レビューがこの復元ロジックを実際に反転させるmutation testを行ったところ**既存15テスト全てが通過してしまう**という重大なテストカバレッジの欠落を発見（既存のdayIndex網羅性テストがdayIndexの「集合」のみを見ており「対応関係」を検証していなかったため、フィクスチャが全7日同一の`calorieTarget`を使っていたことも一因）。round 2で7日分に明確に異なる`calorieTarget`（1400〜2000、日付昇順対応）を設定し、週内バラエティ回避の影響を`dislikedSummary`で排除した上でdayIndex 0/6を明示的に区別する回帰テストを1件追加、レビューが自らも同じ反転mutationを再現して新規テストのみが正しく失敗し（既存15件は無傷）、復元後976/976へ回復することを確認した上でAPPROVEDと判定。この一連の経緯は、「dayIndexの集合網羅性」と「dayIndexと中身の対応関係の正しさ」は別物であり、後者を検証するには日毎に区別可能な値を持つフィクスチャが必須である、という教訓として記録に値する。`generateRecipe`はtask 17.4で実装予定のためこのタスクでは未実装のプレースホルダ（呼ばれると例外）とした。全server 976/976、typecheckクリーン。

- (17.4, `NOT_AI`) `rule-based-menu.generator.ts`に`generateRecipe`を実装。`dishName`完全一致で`RULE_BASED_RECIPES`（task 17.2）から`servings`/`cookingTimeMinutes`/`steps`をそのまま返す。**フォールバック方針の解決済み設計判断**: 一致するエントリが見つからない場合（他日再生成等で`dishName`が古いまま残る整合性エッジケース、Claude実装では`dishName`の検索・照合を一切行わないため原理的に発生しない非対称なエッジケース）でも、エラーを返さず汎用デフォルト値（servings:1、cookingTimeMinutes:15、料理名非依存の汎用3手順）で常に成功する設計とした——レビューが`recipe-detail.service.ts`を確認し、フォールバック発生時も栄養価は既に確定済みの`mealSlot.nutrition`をそのまま使うため表示テキストにのみ影響し、既存の`GenerationFailureReason`エラー分岐契約を汚染しない妥当な判断と評価。`supplementarySuggestions`（`RecipeDetailSchema`が要求する1〜2件必須制約）は`RULE_BASED_SIDE_DISHES`（task 17.2、20件・各mealType5件）から対象mealType一致+NG食材非重複（task 17.3の`filterMandatoryCandidates`から`isFreeOfNgIngredients`として共通化・再利用、コピペではなく実際に共有）で候補を絞り、1件目を乱数選定・2件目を50%確率で重複なく追加選定する2分岐のみ（0件・3件以上になる分岐は構造的に存在しない）。NG食材フィルタで候補が全滅した場合はそのフィルタのみ緩和しmealType一致のみの候補（常に非空、最低5件）へフォールバックし、1〜2件保証を維持。レビューはこの1〜2件保証を自己申告の鵜呑みではなくコードパスを実際に追って数学的に確認した上で（`RecipeDetailSchema`自体はサーバー内のどこでも実行時`.parse`されておらず、この保証は完全にハンドコードのロジックのみに依存している点をFYIとして記録）、3件のライブmutation test（dishName一致ロジック無効化、副菜NGフィルタ無効化、常に3件返すよう改変）でいずれも既存テストが正しく検知することを確認しAPPROVED。副菜提案の`ingredients`はtask 17.3のようなカロリースケーリングを行わない（`recipe-detail.service.ts`が栄養増分算出にのみ使い対象カロリーへの調整ロジックを持たないことをレビューが確認）。全server 980/980、typecheckクリーン。

- (17.5, `NOT_AI`) `server/src/index.ts`の`menuGenerator`構築を`createClaudeMenuGenerator(claudeMenuClient)`から`createRuleBasedMenuGenerator({ foodCompositionRepository })`へ切り替え。旧Claude版の構築コードは削除せず「将来Claude版へ切り替える場合に残置」と明記した上でコメントアウトのみに留め、`@anthropic-ai/sdk`のimport・`package.json`の依存自体は変更していない（将来の切替可能性を維持）。`checkAnthropicApiKeyConfigured`は`console.warn`→`console.info`、文言を「本アプリは非AIモード（RuleBasedMenuGenerator）で動作するため、ANTHROPIC_API_KEY の設定は不要です」という情報提供トーンへ変更（キー未設定を不安を煽る警告として扱うべきではないという判断）。**観測可能な完了条件**（`ANTHROPIC_API_KEY`未設定でサーバー起動→献立生成が502にならず成功すること）を、実装者・レビュー・私自身の3者がそれぞれ独立に実サーバー起動+実HTTPリクエストで確認済み——レビューは対照実験として、配線を一時的に旧Claude版へ戻すmutation testを行い、その場合は**実際に502**（`claude_request_failed`、`Could not resolve authentication method`）になることを確認した上で復元し、この配線変更が真に効果を発揮していることを証明した（370回超の追試を含む）。レビューは検証序盤に自身のポート競合後始末に起因すると思われる1回限りの再現しない500エラーを観測したが、370回超の追試で再現せずコード上の欠陥の証拠が得られなかったため非ブロッキングのFYIとして記録。私自身も別途、APIキー未設定でサーバーを起動しプロフィール登録→`2026-09-07`（月曜日）週の献立生成を実行し、7日×4食枠すべてに実在の料理名（「鮭おにぎりと豆腐わかめの味噌汁」「エビとブロッコリーの塩炒め定食」「牛肉と根菜の煮物」等）が入った有効な献立が返ることを確認済み。`server/src/index.test.ts`の2テストは`console.warn`スパイ→`console.info`スパイ・期待文言の更新に合わせて改修（レビューが実際に旧文言へ戻すmutation testで新テストが正しく失敗することを確認済み）。全server 980/980、typecheck/buildクリーン。**これでtask 17.1-17.5により、`NOT_AI`はAPIキーなしで実際に献立生成が完結するようになった。**

- (17.6, `NOT_AI`) `server/src/menu-generation/rule-based-week-generation.integration.test.ts`（新規、実DB・実Fastifyアプリで完結、Claude/Anthropicクライアントは一切登場しない）を追加。通常プロフィールでの生成・永続化・再取得の一致確認、NG食材"卵"指定時の除外確認（foodId直接確認+`RULE_BASED_RECIPES`タグの逆引き確認の両方）、`low_fat`/`high_protein`/`calorie_only`各制限タイプでの生成成功確認、`low_carb`制限時の`restrictionSuitability`逆引き確認、`POST /generate`→`GET /shopping-list`→`POST /recipe-detail`のHTTP一気通貫確認、の計6テスト。**round 1でREJECTED**: この結合テストの実行そのものが、`rule-based-recipe.data.ts`（task 17.2）の夕食(dinner)12件**全件**が"米"タグを持つという実データ上の欠陥を顕在化させた——NG食材フィルタは安全性のため絶対に緩めない設計（正しい）のため、ユーザーが"米"をNG食材登録した瞬間（米アレルギー・低糖質志向等で十分あり得る自然な入力）、`server/src/index.ts`でClaude版が既にコメントアウト済みでフォールバック手段もないため、**非AI献立生成機能全体が100%決定論的かつ恒久的に使用不能**になることが実機再現で判明した（同根で夕食の`low_carb`適合も0件、糖質制限ユーザーが夕食について常にサイレントに制限無視のフォールバックを受け取る問題も判明）。実装者は当初「task 17.2の境界外、報告のみ」としたが、レビューは「17.6自身の実DB検証で初めて判明した具体的発生条件であり、是正コストも夕食への低リスクな追加データのみで小さい」ことを根拠にREJECTEDと判定——是正コスト・発見の主体（17.6自身）・実害の重大性（機能全損）を踏まえた妥当な判断とし、この線引き自体を今後の類似ケースの判断材料として記録する。round 2で`rule-based-recipe.data.ts`の夕食に、実在食品IDのみを使い米・パン・麺を一切含まない新規2件（「鶏むね肉と彩り野菜のグリル」「鮭のハーブソテーと温野菜」、両方とも`restrictionSuitability`に`low_carb`含む）を追加（夕食12→14件、全体48→50件）、`rule-based-recipe.data.test.ts`に回帰防止テスト2件（夕食に"米"タグなしエントリ・`low_carb`適合エントリがそれぞれ最低1件存在すること）を追加、結合テストの該当2件を「失敗するはず」から「成功し新規2件のいずれかが選ばれる」という正しい期待値へ更新。レビューは新規2件の実在食品ID・栄養構成の妥当性を確認した上で、"米"NG食材設定時の週間献立生成が**実際に成功する**ことを実機（実サーバー起動+実HTTPリクエスト）で自ら再現確認し、4件のライブmutation test（新規2件へ"米"タグを再付与+`low_carb`を除去→対象4テスト全てが正しく失敗→復元）で新規/更新テストの実効性も確認した上でAPPROVED。私自身も別途、実サーバーで"米"をNG食材に設定したプロフィールから週間献立生成を実行し、200 OKで7日分全てが生成され食材に米(foodId 01088)が一切含まれず、夕食が新規追加の2品（「鮭のハーブソテーと温野菜」×3、「鶏むね肉と彩り野菜のグリル」×4）で構成されることを確認済み。なお実装者がround 1完了報告時に`tasks.md`のtask 17.6チェックボックスを自ら`[x]`にしていたことをレビューが発見し指摘——タスク完了のチェックは独立レビュー+parent controllerの個人検証完了後にのみ行うべき運用であることを実装者に明示的に伝え、round 2では遵守を確認済み。全server 988/988、typecheckクリーン。**これでmenu-generation specの非AI献立生成機能（task 17.1-17.6）が完了し、`NOT_AI`はAPIキーなしで実際に週間献立の生成・レシピ詳細・買い物リストまで一気通貫で機能するようになった。**
